---
phase: 04-source-mode-readme-launch-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
user_setup: []

must_haves:
  truths:
    - "README.md is rewritten end-to-end (Phase 1 stub replaced) into the full Delfi-targeted document per D4-09 (target reader: non-technical first-time user; reading time ≤5 min; explains terms when introduced)."
    - "README.md follows the D4-10 9-section structure in this exact order: (1) What deshtml is, (2) Install, (3) First run — `/deshtml`, (4) The five doc types, (5) Source mode — `/deshtml @file.md`, (6) Uninstall, (7) Known Limitations, (8) Design system credit, (9) License."
    - "Section 7 (Known Limitations) explicitly documents the three known limitations per DOCS-02: offline behavior (Inter via Google Fonts → system-font fallback without internet); macOS-first auto-open (`open <file>` is macOS-only; Linux writes the file but does not auto-open); one file per run (the skill writes one HTML and stops; iteration goes through normal Claude conversation)."
    - "Section 2 (Install) and Section 6 (Uninstall) include the verbatim install / uninstall one-liners — byte-for-byte identical to what `bin/install.sh` and `bin/uninstall.sh` actually expose (D-14 verbatim discipline). The install one-liner is `curl -fsSL https://raw.githubusercontent.com/sperezasis/deshtml/main/bin/install.sh | bash`. The uninstall one-liner is `curl -fsSL https://raw.githubusercontent.com/sperezasis/deshtml/main/bin/uninstall.sh | bash` PLUS the `rm -rf ~/.claude/skills/deshtml` direct alternative."
    - "README is written in English (D4-11). No Spanish strings, no mixed-language content."
    - "README is written in handbook tone, not pitch tone (D4-12, CLAUDE.md root rule). No `revolutionary`, `game-changing`, `effortlessly`, `powerful`, `seamlessly`, `easily`, `in seconds`, `everything you need`, `out of the box`, `next-generation`, `cutting-edge`, `breakthrough` vocabulary. Section TITLES describe what IS (per CLAUDE.md Section Writing Rules)."
    - "README does NOT contain a 'What's New' / changelog section (D4-12 — that's the GitHub Release notes' job)."
    - "README does NOT contain a CONTRIBUTING / development docs section (D4-13 — V2)."
    - "Section 8 (Design system credit) links to the Caseproof Documentation System per the OQ-2 resolution from CONTEXT: there is no public Caseproof repo URL for `~/work/caseproof/DOCUMENTATION-SYSTEM.md`. Ship the section as a text reference: `Based on the Caseproof Documentation System (internal — see Santiago for access).` Document this disposition in 04-02-SUMMARY.md as a V2 carryover (publish a public copy of the design system, then update the README)."
    - "CHANGELOG.md exists at the repo root with a v0.1.0 seed entry. The seed is finalized by Plan 04-03 Task 2 (post-merge, after the actual phase summaries are known) — Plan 04-02 ships the file structure + the v0.1.0 placeholder section so the file exists in the merged PR for users browsing the repo offline (D4-cl Claude's-Discretion default)."
    - "Both files written in handbook tone — Section Writing Rules from CLAUDE.md apply (titles are structural facts or directives; subtitles explain don't add; name the thing; causality chain follows from previous section)."
  artifacts:
    - path: "README.md"
      provides: "Public-facing project README written for Delfi as the target reader. 9 sections in the D4-10 order. ~150-250 lines."
      contains: "deshtml is"
    - path: "CHANGELOG.md"
      provides: "Repo-root changelog seed. v0.1.0 entry placeholder; finalized in Plan 04-03 Task 2."
      contains: "## v0.1.0"
  key_links:
    - from: "README.md (Section 2 Install)"
      to: "bin/install.sh"
      via: "Verbatim install one-liner; byte-for-byte identical to the script's actual fetch URL"
      pattern: "curl -fsSL https://raw.githubusercontent.com/sperezasis/deshtml/main/bin/install.sh \\| bash"
    - from: "README.md (Section 6 Uninstall)"
      to: "bin/uninstall.sh"
      via: "Verbatim uninstall one-liner; byte-for-byte identical to the script's actual fetch URL"
      pattern: "curl -fsSL https://raw.githubusercontent.com/sperezasis/deshtml/main/bin/uninstall.sh \\| bash"
    - from: "README.md (Section 5 Source mode)"
      to: "skill/source-mode.md (Plan 04-01)"
      via: "README documents `/deshtml @file.md` as the source-mode invocation; source-mode.md is the actual implementation"
      pattern: "/deshtml @"
    - from: "README.md (Section 7 Known Limitations)"
      to: "skill/SKILL.md Step 8 (open + path-print)"
      via: "macOS-first auto-open is documented in README and is the actual `open <file>` behavior in SKILL.md Step 8"
      pattern: "macOS"
---

<objective>
Replace the Phase-1 stub `README.md` with the full Delfi-targeted public README per D4-10's 9-section structure, and seed `CHANGELOG.md` at the repo root for the v0.1.0 release. Closes DOCS-01 (README explains all required pieces), DOCS-02 (Known Limitations section), DOCS-03 (Delfi as target reader).

Plan 04-01 ships the source-mode wiring (no file overlap; runs in parallel in Wave 1). Plan 04-03 hardens the launch (depends on this plan because the live install must include the new README in the published payload).

Two tasks:
1. **Rewrite `README.md`** — replace the entire current file with the D4-10 9-section document. Section order is locked. Section content is written for Delfi: ≤5 min reading time, no assumed knowledge of skill packaging / bash / Claude Code internals / GitHub releases / semver / design systems. Terms are explained the first time they appear ("a *skill* is a Claude Code add-on that gives Claude a specific job to do"). Handbook tone in titles AND body. No pitch vocabulary. Verbatim install / uninstall one-liners (D-14 verbatim discipline).
2. **Create `CHANGELOG.md`** — repo root, v0.1.0 placeholder section. The actual changelog content for v0.1.0 is finalized in Plan 04-03 Task 2 (post-merge) once the four phase summaries are known. Plan 04-02 ships the file structure + a placeholder so the file exists in the merged PR for offline-readers (per CONTEXT D4 Claude's-Discretion default).

Purpose: The README is the only file outsiders read first. A pitch-tone README contradicts the handbook-tone moat the skill itself enforces (CLAUDE.md root rule "describe what IS" applies recursively). DOCS-03's Delfi-targeting is the binding constraint — every section must be understandable to a non-technical first-time reader, with no follow-up questions needed. The CHANGELOG.md seed lets users browsing the repo offline see version history without leaving the repo (GitHub Releases is the canonical source, the file is a convenience copy).

Output: one rewritten file (`README.md`), one new file (`CHANGELOG.md`). After this plan + Plan 04-01, Wave 2 (Plan 04-03) can dry-run the local install and verify the published payload includes the new README.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-source-mode-readme-launch-hardening/04-CONTEXT.md
@.planning/phases/04-source-mode-readme-launch-hardening/04-RESEARCH.md
@.planning/phases/01-foundation-installer-design-assets/01-SUMMARY.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-SUMMARY.md
@.planning/phases/03-remaining-four-doc-types/03-SUMMARY.md
@README.md
@bin/install.sh
@bin/uninstall.sh
@LICENSE
@VERSION
@/Users/sperezasis/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite README.md as the D4-10 9-section Delfi-targeted public README</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/04-source-mode-readme-launch-hardening/04-CONTEXT.md §"D4-09, D4-10, D4-11, D4-12, D4-13" (target reader, 9-section structure, English, no changelog section, no contributing section)
    - .planning/phases/04-source-mode-readme-launch-hardening/04-CONTEXT.md §"Specific Ideas" ("The README is the only file outsiders read first. A pitch-tone README contradicts the handbook-tone moat.")
    - .planning/phases/04-source-mode-readme-launch-hardening/04-RESEARCH.md §"DOCS-01..03" (the OQ-2 resolution: Caseproof design system has no public URL — ship as text reference + V2 carryover)
    - /Users/sperezasis/CLAUDE.md §"Documentation Methodology — Story First" (Section Writing Rules — handbook tone, name the thing, causality chain, subtitles explain don't add)
    - bin/install.sh (the install command's actual fetch URL — for verbatim quotation in Section 2)
    - bin/uninstall.sh (the uninstall command's actual fetch URL — for verbatim quotation in Section 6)
    - skill/SKILL.md (the actual flow — Step 8's `open <file>` is macOS-only; Section 7 Known Limitations documents this)
    - skill/source-mode.md (Plan 04-01's output — Section 5 documents `/deshtml @file.md` as the source-mode invocation)
    - README.md (the current Phase-1 stub — to be replaced entirely)
  </read_first>
  <action>
    Replace the entire `README.md` with the structure below. Use the Write tool (full rewrite — the Phase-1 stub becomes the v0.1.0 README).

    The README is written for Delfi: a non-technical first-time reader. Reading time ≤5 min. No assumed knowledge of skill packaging, bash, Claude Code internals, GitHub releases, semver, or design systems. When a term is introduced, explain it inline (one short clause, not a footnote).

    **Tone rule (CLAUDE.md root):** every section title and body sentence describes what IS. No pitch vocabulary. The handbook-tone rubric from `skill/story-arc.md` applies — same words flagged there are flagged here. Concretely BANNED in the README:
    - `revolutionary`, `game-changing`, `breakthrough`, `next-generation`, `cutting-edge`
    - `easily`, `seamlessly`, `effortlessly`
    - `powerful`, `in seconds`, `everything you need`, `out of the box`

    **Verbatim discipline (D-14):** the install / uninstall one-liners are byte-for-byte identical to what the scripts actually fetch. Copy from `bin/install.sh` line 7 (the `RAW_VERSION_URL` host) and the documented one-liners — confirmed against the current Phase-1 stub. The literal install one-liner is:

    > `curl -fsSL https://raw.githubusercontent.com/sperezasis/deshtml/main/bin/install.sh | bash`

    The literal uninstall one-liner is:

    > `curl -fsSL https://raw.githubusercontent.com/sperezasis/deshtml/main/bin/uninstall.sh | bash`

    Plus the direct alternative:

    > `rm -rf ~/.claude/skills/deshtml`

    ### Section 1 — What deshtml is

    Title: `# deshtml`
    Subtitle / one-paragraph lead: explain what the project IS in story-first form. ≤5 sentences. Names the thing (`a Claude Code skill`), states what the user does (`runs one command, answers a few questions, gets back an HTML document`), names the design source (`the Caseproof Documentation System`), and ends with the file output (`a single .html file in your current directory`).

    Suggested body (rewrite in your own words; the constraint is the shape, not the wording):

    > deshtml is a Claude Code skill — an add-on that gives Claude a specific job to do. You run `/deshtml`, answer five short questions about your topic, approve the proposed story arc, and Claude writes a single self-contained HTML file to the current directory. The HTML follows the Caseproof Documentation System: a fixed palette, fixed typography, a closed component library. You did not need to know the design system; the skill knows it.

    ### Section 2 — Install

    Title: `## Install`
    Body: ONE sentence above the code block, ONE sentence below. No prose around it. The code block contains the verbatim install one-liner.

    Suggested body:

    > Paste this in a terminal:
    >
    > ```bash
    > curl -fsSL https://raw.githubusercontent.com/sperezasis/deshtml/main/bin/install.sh | bash
    > ```
    >
    > The skill installs to `~/.claude/skills/deshtml/`. Re-running the same command updates an existing install in place.

    ### Section 3 — First run — `/deshtml`

    Title: `## First run`
    Body: walk through what happens when the user runs `/deshtml` with no arguments. One paragraph + numbered list (1-5).

    Suggested body:

    > Open Claude Code in any directory and type `/deshtml`. The skill walks you through five short questions, then proposes a story arc as a table. Approve the arc with `approve`, and Claude writes the HTML file and opens it in your browser.
    >
    > 1. Claude asks the document type — pick `handbook`, `pitch`, `technical brief`, `presentation`, or `meeting prep`.
    > 2. Claude asks four more questions about your audience, your material, your sections, and your tone.
    > 3. Claude shows a story-arc table with five columns and a flowing paragraph reading the arc as a single read.
    > 4. Reply `approve` (or describe changes — Claude regenerates the arc until you approve).
    > 5. Claude writes a `YYYY-MM-DD-<slug>-<type>.html` file to your current directory and opens it in your default browser. The absolute path is the last line of output.

    ### Section 4 — The five doc types

    Title: `## The five doc types`
    Body: one-line description per type. Show which format each lands in. No code samples, no comparison tables.

    Suggested body:

    > Each doc type runs a short tailored interview and lands in the format that fits its content shape:
    >
    > - **Handbook** — multi-section reference doc. Sidebar layout (960px wide).
    > - **Pitch** — problem → solution → ask narrative. Linear layout (1440px wide).
    > - **Technical brief** — architecture or decision write-up for engineers. Sidebar layout.
    > - **Presentation** — single-page slide deck with anchor navigation. Full-viewport slides.
    > - **Meeting prep** — briefing doc with context, talking points, and anticipated questions. Linear layout.
    >
    > The skill picks the format from the doc type and the section count automatically — you do not pick it.

    ### Section 5 — Source mode — `/deshtml @file.md`

    Title: `## Source mode`
    Body: one paragraph. When to use it (you already have a draft). What changes (skips the interview, jumps to the arc proposal grounded in the source).

    Suggested body:

    > If you already have a draft, run `/deshtml @path/to/draft.md` instead. The skill skips the interview entirely, reads the file, infers the document type from the source's shape, and proposes a story arc grounded in the source content. The same arc-approval gate runs — nothing is rendered until you approve. You can also paste raw text longer than ~200 characters as the prompt and the skill will use it the same way.

    ### Section 6 — Uninstall

    Title: `## Uninstall`
    Body: ONE sentence above the code block, ONE alternative below.

    Suggested body:

    > Paste this in a terminal:
    >
    > ```bash
    > curl -fsSL https://raw.githubusercontent.com/sperezasis/deshtml/main/bin/uninstall.sh | bash
    > ```
    >
    > Or remove the directory directly:
    >
    > ```bash
    > rm -rf ~/.claude/skills/deshtml
    > ```

    ### Section 7 — Known Limitations

    Title: `## Known limitations`
    Body: a bulleted list of the three documented limitations. Each item: one sentence stating the limitation, one sentence stating the consequence.

    Suggested body:

    > - **Offline:** the generated HTML loads the Inter font from Google Fonts. Without internet, the document falls back to your system font and still renders correctly.
    > - **macOS-first:** after writing the file, the skill runs `open <file>` to launch your default browser. `open` is macOS-only; on Linux the file is written and the absolute path is printed, but the browser does not auto-open. Open the file manually.
    > - **One file per run:** the skill writes one HTML file and stops. To revise, ask Claude to edit the file in normal conversation — there is no in-skill revision loop.

    ### Section 8 — Design system credit

    Title: `## Design system`
    Body: ONE-paragraph credit naming the source.

    Suggested body (per OQ-2 resolution — no public URL exists for the Caseproof Documentation System; ship as text reference):

    > The palette, typography, and component library are based on the **Caseproof Documentation System**, an internal Caseproof design system. Public access is not yet available — contact Santiago Perez Asis for design-system reference materials.

    ### Section 9 — License

    Title: `## License`
    Body: ONE-line link to the LICENSE file.

    Suggested body:

    > MIT. See [LICENSE](LICENSE).

    ### Final structural check

    After writing, verify (mechanical, before the `<verify>` block runs):

    1. Exactly 9 sections (the H1 deshtml title block + 8 H2 sections — re-count: Section 1 is the H1 + lead paragraph, Sections 2-9 are H2s. Total: 1 H1 + 8 H2s = the D4-10 9-section structure).
    2. Section order matches D4-10 verbatim.
    3. The install one-liner appears once, in Section 2, in a fenced ```bash``` block.
    4. The uninstall one-liner appears once, in Section 6, in a fenced ```bash``` block, plus the `rm -rf` alternative in a second fenced ```bash``` block.
    5. No banned pitch vocabulary anywhere (regex: `revolutionary|game-changing|breakthrough|next-generation|cutting-edge|easily|seamlessly|effortlessly|powerful|in seconds|out of the box|everything you need`).
    6. No `What's New` / `Changelog` section title.
    7. No `Contributing` / `Development` / `Setup` section title.
    8. Reading time ≤5 min — proxy: line count ≤300 (matches the actual content density of a Delfi-targeted ≤5-min doc).
    9. English only — no Spanish strings.

    Use the Write tool to overwrite the existing `README.md` with the rewritten content.
  </action>
  <behavior>
    - File `README.md` exists.
    - File contains all 9 D4-10 sections in order: an `# deshtml` H1 + lead paragraph, then `## Install`, `## First run`, `## The five doc types`, `## Source mode`, `## Uninstall`, `## Known limitations`, `## Design system`, `## License`.
    - File contains the verbatim install one-liner `curl -fsSL https://raw.githubusercontent.com/sperezasis/deshtml/main/bin/install.sh | bash`.
    - File contains the verbatim uninstall one-liner `curl -fsSL https://raw.githubusercontent.com/sperezasis/deshtml/main/bin/uninstall.sh | bash`.
    - File contains the alternative `rm -rf ~/.claude/skills/deshtml` direct uninstall.
    - File documents the three Known Limitations: offline-font fallback, macOS-first auto-open, one-file-per-run.
    - File names the five doc types: handbook, pitch, technical brief, presentation, meeting prep.
    - File credits the Caseproof Documentation System with the OQ-2 text-reference disposition (no public URL claim).
    - File contains NO pitch vocabulary (banned word regex below).
    - File contains NO `What's New`, `Changelog`, `Contributing`, or `Development` section.
    - File is ≤300 lines (Delfi reading-time proxy).
    - File is in English (no Spanish strings).
    - File ends with the `## License` section linking to `LICENSE`.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s README.md &&
      command grep -qF '# deshtml' README.md &&
      command grep -qF '## Install' README.md &&
      command grep -qF '## First run' README.md &&
      command grep -qF '## The five doc types' README.md &&
      command grep -qF '## Source mode' README.md &&
      command grep -qF '## Uninstall' README.md &&
      command grep -qF '## Known limitations' README.md &&
      command grep -qF '## Design system' README.md &&
      command grep -qF '## License' README.md &&
      command grep -qF 'curl -fsSL https://raw.githubusercontent.com/sperezasis/deshtml/main/bin/install.sh | bash' README.md &&
      command grep -qF 'curl -fsSL https://raw.githubusercontent.com/sperezasis/deshtml/main/bin/uninstall.sh | bash' README.md &&
      command grep -qF 'rm -rf ~/.claude/skills/deshtml' README.md &&
      command grep -qiF 'handbook' README.md &&
      command grep -qiF 'pitch' README.md &&
      command grep -qiF 'technical brief' README.md &&
      command grep -qiF 'presentation' README.md &&
      command grep -qiF 'meeting prep' README.md &&
      command grep -qiF 'macOS' README.md &&
      command grep -qiF 'offline' README.md &&
      command grep -qiF 'inter' README.md &&
      command grep -qF 'Caseproof Documentation System' README.md &&
      command grep -qF 'MIT' README.md &&
      command grep -qF '/deshtml @' README.md &&
      ! command grep -qiE 'revolutionary|game-changing|breakthrough|next-generation|cutting-edge|seamlessly|effortlessly|in seconds|out of the box|everything you need|easily|powerful' README.md &&
      ! command grep -qiE "^## (What's New|Changelog|Contributing|Development|Setup)" README.md &&
      lc=$(wc -l < README.md) && test "$lc" -le 300 && test "$lc" -ge 30 &&
      echo "ALL CHECKS PASS — line count: $lc"
    </automated>
  </verify>
  <acceptance_criteria>
    - `README.md` is rewritten end-to-end (Phase-1 stub fully replaced).
    - All 9 D4-10 sections are present in order.
    - The verbatim install + uninstall one-liners are byte-for-byte identical to what the scripts fetch.
    - The three Known Limitations are documented (offline, macOS-first, one-file-per-run).
    - All five doc types are named.
    - The Caseproof Documentation System is credited per the OQ-2 text-reference disposition.
    - No pitch vocabulary (regex check passes).
    - No `What's New` / `Changelog` / `Contributing` / `Development` / `Setup` sections.
    - File is ≤300 lines, English-only.
    - License section links to the LICENSE file.
  </acceptance_criteria>
  <done>
    `README.md` is rewritten as the v0.1.0 public README. Delfi can read it in ≤5 min, install deshtml from Section 2, and run `/deshtml` from Section 3 without follow-up questions. The handbook-tone moat holds (no pitch vocabulary). All three DOCS requirements (DOCS-01, DOCS-02, DOCS-03) are mechanically verifiable.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create CHANGELOG.md at repo root with v0.1.0 placeholder section</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/phases/04-source-mode-readme-launch-hardening/04-CONTEXT.md §"Claude's Discretion" ("Whether to ship a small CHANGELOG.md at the repo root… Recommendation: ship a minimal CHANGELOG.md (just the v0.1.0 entry) — small file, cheap, useful for users browsing the repo offline.")
    - .planning/phases/01-foundation-installer-design-assets/01-SUMMARY.md (Phase 1 ships)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-SUMMARY.md (Phase 2 ships)
    - .planning/phases/03-remaining-four-doc-types/03-SUMMARY.md (Phase 3 ships)
    - VERSION (currently 0.0.1; bumped to 0.1.0 in Plan 04-03)
  </read_first>
  <action>
    Create `CHANGELOG.md` at the repo root with a v0.1.0 placeholder section. The actual content for v0.1.0 is finalized in Plan 04-03 Task 2 (post-merge), once all four phase summaries are confirmed and the tag commit is on main.

    Use Keep-a-Changelog format ([keepachangelog.com/en/1.1.0/](https://keepachangelog.com/en/1.1.0/)) — the de facto standard. Top of file documents the format. Below: an `## [Unreleased]` placeholder (intentionally empty for now) and a `## [0.1.0] — TBD` section with the four bullet points from the four phase summaries.

    Write this exact content:

    ```markdown
    # Changelog

    All notable changes to deshtml are documented here. The format follows
    [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and the project
    follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

    ## [Unreleased]

    Nothing yet.

    ## [0.1.0] — TBD

    First public release. Date and final notes are filled in by the launch
    plan (Plan 04-03 Task 2) once the v0.1.0 tag lands on main.

    ### Added

    - **Installer (Phase 1).** Atomic, idempotent, no-sudo curl-pipe-bash
      one-liner that drops the skill into `~/.claude/skills/deshtml/` and
      refuses to run as root. Reads the pinned tag from the `VERSION` file
      and stages to a temp directory before swapping atomically.

    - **Caseproof Documentation System assets (Phase 1).** Verbatim palette,
      typography, component library, and two layout skeletons (Handbook
      960px sidebar, Overview 1440px linear) shipped as design tokens that
      Claude pastes — never paraphrases — into output.

    - **Story-arc gate + Handbook flow end-to-end (Phase 2).** `/deshtml`
      runs a 5-question interview, builds a 5-column story-arc table with
      a flowing-paragraph diagnostic, runs an automated self-review pass,
      and blocks HTML rendering until the user types `approve` (or one of
      the whitelist phrases). The post-generation audit rejects hex literals
      outside the variable block, unknown CSS class names, and markup not
      in the component library.

    - **All five doc types (Phase 3).** Pitch, handbook, technical brief,
      presentation, and meeting prep each ship a tailored 5-question
      interview file. Format auto-selects from the approved arc: Handbook
      (4+ sections), Overview (1-3), Presentation (slide decks). Audit
      auto-grows for new format skeletons.

    - **Source mode (Phase 4).** `/deshtml @path/to/draft.md` (or pasted
      prose >200 characters) is detected at turn 1, infers the document
      type from the source's shape, and proposes a story arc grounded in
      the source content. The same arc-approval gate runs.

    - **Public README and launch hardening (Phase 4).** The repo README is
      written for a non-technical first-time reader (target: Delfi),
      explains the install one-liner, the five doc types, source mode,
      uninstall, the three known limitations (offline, macOS-first,
      one file per run), and credits the Caseproof Documentation System.
      The install one-liner has been verified end-to-end against the live
      public URL.

    ### Known limitations

    - Inter font loads via Google Fonts; offline runs fall back to the
      system font.
    - The skill auto-opens the file via `open` (macOS-only). On Linux,
      the file is written and the absolute path is printed; the browser
      does not auto-open.
    - One file per run. Iterate via normal Claude conversation.

    [Unreleased]: https://github.com/sperezasis/deshtml/compare/v0.1.0...HEAD
    [0.1.0]: https://github.com/sperezasis/deshtml/releases/tag/v0.1.0
    ```

    Use the Write tool. The TBD date and any deviations from this seed are filled in by Plan 04-03 Task 2 (after the tag is cut, the actual release date is known).
  </action>
  <behavior>
    - File `CHANGELOG.md` exists at repo root.
    - File starts with `# Changelog`.
    - File references Keep a Changelog and Semantic Versioning by name.
    - File contains an `## [Unreleased]` section.
    - File contains a `## [0.1.0]` section with `TBD` as the date placeholder.
    - The v0.1.0 section contains an `### Added` subsection with at least 6 bullet points (one for the installer, design assets, story-arc gate, all five doc types, source mode, README + launch hardening).
    - The v0.1.0 section contains a `### Known limitations` subsection naming the three documented limits (offline, macOS-only auto-open, one file per run).
    - File contains the comparison link references at the bottom.
    - File is ≤120 lines.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s CHANGELOG.md &&
      command grep -qF '# Changelog' CHANGELOG.md &&
      command grep -qF 'Keep a Changelog' CHANGELOG.md &&
      command grep -qF 'Semantic Versioning' CHANGELOG.md &&
      command grep -qF '## [Unreleased]' CHANGELOG.md &&
      command grep -qF '## [0.1.0]' CHANGELOG.md &&
      command grep -qF 'TBD' CHANGELOG.md &&
      command grep -qF '### Added' CHANGELOG.md &&
      command grep -qF '### Known limitations' CHANGELOG.md &&
      command grep -qF 'Phase 1' CHANGELOG.md &&
      command grep -qF 'Phase 2' CHANGELOG.md &&
      command grep -qF 'Phase 3' CHANGELOG.md &&
      command grep -qF 'Phase 4' CHANGELOG.md &&
      command grep -qF 'Source mode' CHANGELOG.md &&
      command grep -qF 'sperezasis/deshtml' CHANGELOG.md &&
      lc=$(wc -l < CHANGELOG.md) && test "$lc" -le 120 && test "$lc" -ge 30 &&
      echo "ALL CHECKS PASS — line count: $lc"
    </automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` exists at repo root.
    - File follows Keep-a-Changelog format with the documented header.
    - File contains `## [Unreleased]` and `## [0.1.0]` sections.
    - The `## [0.1.0]` section is dated `TBD` (Plan 04-03 Task 2 fills in the actual date).
    - The `### Added` subsection enumerates Phases 1-4's headline ships (installer, design assets, story-arc gate, all five doc types, source mode, README + launch hardening).
    - The `### Known limitations` subsection names all three documented limitations.
    - Link-reference targets at the bottom point to `sperezasis/deshtml`.
    - File is ≤120 lines.
  </acceptance_criteria>
  <done>
    `CHANGELOG.md` exists at the repo root in Keep-a-Changelog format with the v0.1.0 placeholder section. Plan 04-03 Task 2 will replace the `TBD` date with the actual release date once the tag is cut on main.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → public viewers | Anyone visiting `github.com/sperezasis/deshtml` reads README.md. The README is the trust signal — pitch tone undercuts the methodology the skill itself enforces. |
| Install one-liner in README → user's terminal | The README is the canonical source for the install command. A wrong URL in the README (typo, branch drift) sends users to the wrong endpoint. The verbatim discipline (D-14) is the mitigation. |
| CHANGELOG.md → repo browsers | Users without GitHub Releases access (offline, scripted clones) read CHANGELOG.md in the file tree. The file is a cached-locally view of release notes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | README install one-liner drifts from `bin/install.sh` (e.g., URL typo, branch swap, wrong filename) | mitigate | Task 1 verify block greps for the byte-exact one-liner. The literal must match `bin/install.sh`'s `RAW_VERSION_URL` host pattern. Plan 04-03 Task 5 (live verification) re-tests the one-liner end-to-end against the live URL — any drift fails the live test. |
| T-04-10 | Spoofing | README contains pitch vocabulary that contradicts the handbook-tone moat the skill enforces | mitigate | Task 1 verify block runs a banned-word regex against the file. The list mirrors the regex in `skill/story-arc.md` Step C — single source of truth for "what does pitch tone look like." |
| T-04-11 | Information Disclosure | README documents the wrong path / wrong scope (e.g., claims project-scope install when only user-scope works) | mitigate | Task 1's `<read_first>` includes the actual SKILL.md and bin/install.sh files. The README's install path (`~/.claude/skills/deshtml/`) matches the installer's `DEST` literal. Out-of-scope items (project-scope install — Claude Code issue #36155) are documented in REQUIREMENTS.md "Out of Scope" and are NOT mentioned in the README. |
| T-04-12 | Tampering | A future plan adds a "What's New" section to README.md, duplicating GitHub Releases | mitigate | Task 1 verify block greps for `^## (What's New|Changelog|Contributing|Development|Setup)` and fails if any of those headings appear. D4-12 + D4-13 are mechanically enforced. |
| T-04-13 | Spoofing | Source mode is documented as macOS-first when it actually works on Linux too (the only macOS-only piece is the `open` command) | mitigate | Section 5 (Source mode) does NOT mention macOS — source mode is platform-agnostic. Section 7 (Known Limitations) mentions macOS-first ONLY for the `open <file>` auto-open, not for the rest of the flow. The wording is precise: "the file is written and the absolute path is printed, but the browser does not auto-open." |
| T-04-14 | Information Disclosure | README claims a public Caseproof Documentation System URL that does not exist | mitigate | Section 8 ships the OQ-2 text-reference disposition: "(internal — see Santiago for access)." No URL is claimed. The 04-02-SUMMARY.md V2 carryover documents this so a future plan can update the README when a public copy is published. |
| T-04-15 | Tampering | CHANGELOG.md drifts from GitHub Releases (different content, conflicting version numbers) | accept | The repo-root CHANGELOG.md is a convenience copy. GitHub Releases is the canonical source. Drift between the two is recoverable (overwrite the file in a follow-up commit). The V1 risk is bounded; V2 may add a release script that auto-syncs both. |
| T-04-16 | Repudiation | The TBD date in CHANGELOG.md `## [0.1.0]` is never updated; users see a permanently-dated v0.1.0 | mitigate | Plan 04-03 Task 2 explicitly updates the TBD date to the actual release date in the same commit that bumps `VERSION` to `0.1.0`. The post-merge sequencing in Plan 04-03 enforces the update. If 04-03 Task 2 forgets, the verify block in 04-03 catches it (greps for `TBD` in CHANGELOG.md and fails if present after the tag-bump commit). |
</threat_model>

<verification>
Both task `<verify>` blocks must pass. Specifically:

- Task 1: `README.md` contains all 9 D4-10 sections in order; verbatim install + uninstall one-liners; three Known Limitations; all five doc type names; the Caseproof Documentation System credit (text reference, no URL claim); no pitch vocabulary; no banned section headings (`What's New`, `Changelog`, `Contributing`, `Development`, `Setup`); ≤300 lines; English-only.
- Task 2: `CHANGELOG.md` exists at repo root, in Keep-a-Changelog format, with `## [Unreleased]` and `## [0.1.0] — TBD` sections; lists all four phases' headline ships in `### Added`; documents the three known limitations; ≤120 lines.

The Phase-4 DOCS-01 / DOCS-02 / DOCS-03 contracts are verified by the combination of task completions plus Plan 04-03 Task 1 (pre-merge dry run) when the README's install one-liner is exercised end-to-end against the local install.
</verification>

<success_criteria>
1. **DOCS-01 closed.** README explains: what deshtml is (Section 1), the install one-liner (Section 2), basic usage (Sections 3 + 5), the five doc types (Section 4), the uninstall command (Section 6), and links the Caseproof Documentation System (Section 8).
2. **DOCS-02 closed.** Section 7 (Known Limitations) documents offline-font fallback, macOS-first auto-open, and one-file-per-run.
3. **DOCS-03 closed.** README is written for Delfi as the target reader: ≤300 lines, ≤5 min reading time, no assumed knowledge of skill packaging / bash / Claude Code internals / GitHub releases / semver / design systems. Terms are explained on first use.
4. **Verbatim discipline preserved (D-14).** Install + uninstall one-liners are byte-for-byte identical to what `bin/install.sh` and `bin/uninstall.sh` actually fetch.
5. **Handbook tone preserved (CLAUDE.md root rule).** No pitch vocabulary anywhere. Section titles describe what IS. The README itself follows the methodology the skill enforces.
6. **D4-12 + D4-13 enforced.** No `What's New` / `Changelog` / `Contributing` / `Development` / `Setup` sections in README. Those are GitHub Releases' job (changelog) or V2 (contributing).
7. **CHANGELOG.md seed shipped.** Repo-root file in Keep-a-Changelog format with v0.1.0 placeholder. Plan 04-03 Task 2 fills in the TBD date.
8. **No file overlap with Plan 04-01.** `files_modified` for this plan: `README.md`, `CHANGELOG.md`. Plan 04-01 modifies `skill/source-mode.md` and `skill/SKILL.md`. Zero overlap. Both plans run in Wave 1 in parallel.
9. **Plan 04-03 unblocked.** After this plan + Plan 04-01 complete and the PR merges, Plan 04-03 can dry-run the local install (Task 1) and verify the live URL serves the new README + new install path (Task 5).
</success_criteria>

<output>
After completion, create `.planning/phases/04-source-mode-readme-launch-hardening/04-02-SUMMARY.md` documenting:
- The two files shipped (`README.md` REWRITTEN, `CHANGELOG.md` NEW) with line counts and the actual content shape that landed.
- Confirmation that the verbatim install + uninstall one-liners in README.md match `bin/install.sh` and `bin/uninstall.sh` byte-for-byte (paste both literals from each file).
- The OQ-2 disposition: Section 8 (Design system) ships as text reference (no public URL); a V2 carryover note is recorded for when a public copy of the Caseproof Documentation System is published.
- The pitch-vocabulary check result (banned-word regex hits: 0 expected).
- The CHANGELOG.md `TBD` date placeholder — confirmed will be filled by Plan 04-03 Task 2.
- Any deviations from D4-10's section structure with rationale (none expected; flag if any landed).
- What Plan 04-03 inherits: a complete Delfi-targeted README ready for the live install verification, and a CHANGELOG.md seed waiting for the v0.1.0 tag-date.
</output>
</content>
