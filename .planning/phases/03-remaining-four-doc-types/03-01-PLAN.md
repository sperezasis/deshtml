---
phase: 03-remaining-four-doc-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/design/formats/presentation.html
  - skill/SKILL.md
autonomous: true
requirements:
  - DOC-04
  - DESIGN-04
user_setup: []

must_haves:
  truths:
    - "A 30-min scroll-snap spike has been run in Chrome AND Safari (macOS) and the outcome is recorded in `skill/design/formats/presentation.html` as a header comment (PASS = full scroll-snap shipped; FAIL = `:target`-only fallback shipped, snap properties dropped)."
    - "skill/design/formats/presentation.html exists, declares the same Phase-1 light-mode meta tags as handbook.html and overview.html (`color-scheme: light` + `supported-color-schemes: light`), and uses the same `<link rel=\"stylesheet\" href=\"../palette.css\">` + `<link rel=\"stylesheet\" href=\"../typography.css\">` dev-time references as the other two skeletons."
    - "presentation.html's snap container is `<main class=\"deck\">`, NOT `<html>` or `<body>` (Safari fragility mitigation per RESEARCH §Pattern 3)."
    - "presentation.html sets `html { scroll-padding-top: 0; }` (Pitfall 9 refinement — Presentation has no sticky header, copying handbook.html's 80px would leave an 80px gap on every anchor click)."
    - "presentation.html introduces no new CSS variables or color tokens — every color is `var(--token)` from palette.css; no hex literal appears outside the upstream `:root` (Phase 2 audit Rule 1 must continue to pass on the skeleton)."
    - "presentation.html omits the `<aside class=\"sidebar\">` slot entirely (D3-09 — full-viewport slides; no sidebar)."
    - "skill/SKILL.md is updated so Step 2 routes all five doc types to real interview files (handbook → interview/handbook.md; pitch → interview/pitch.md; technical brief → interview/technical-brief.md; presentation → interview/presentation.md; meeting prep → interview/meeting-prep.md). The four `is coming in Phase 3` stubs are removed."
    - "skill/SKILL.md gains a new Step 5b (between existing Step 5 filename-collision and existing Step 6 render) that runs the D3-01 mechanical format-selection logic (presentation type → presentation; arc rows ≥4 → handbook; else → overview) and prints exactly one line `Format: <format>` to the user."
    - "skill/SKILL.md Step 6 reads `${CLAUDE_SKILL_DIR}/design/formats/${format}.html` (variable load) instead of the hard-coded `formats/handbook.html` reference."
    - "skill/SKILL.md still ≤200 lines (D3-17 hard cap) — the file grows by ~25 lines (Step 5b plus the four stub→route flips) and stays under cap."
    - "All Phase-2 SKILL.md content that is NOT the Step 2 stubs / Step 6 skeleton path / new Step 5b is preserved byte-for-byte (frontmatter, mode-detection regex in Step 1, interview hand-off in Step 3, story-arc hand-off in Step 4, Step 5 collision loop, Step 7 audit retry, Step 8 open + path-print)."
  artifacts:
    - path: "skill/design/formats/presentation.html"
      provides: "Slide-deck format skeleton — full-viewport `100vh` slides, `scroll-snap-type: y mandatory` on `<main>`, CSS-only `slide-num` counter with `TOTAL_SLIDES_LITERAL` placeholder, floating `slide-nav` reusing `.nav-a`. No JS, no sidebar."
      contains: "main.deck"
    - path: "skill/SKILL.md"
      provides: "Updated 8-step (now 9-step with 5b) flow controller. Four type stubs → routes; new format-auto-selection step; variable skeleton load."
      contains: "Step 5b"
  key_links:
    - from: "skill/SKILL.md (Step 2)"
      to: "skill/interview/{pitch,technical-brief,presentation,meeting-prep}.md"
      via: "Step 3 reads `${CLAUDE_SKILL_DIR}/interview/${type}.md` where ${type} is the normalized Step 2 answer"
      pattern: "interview/.*\\.md"
    - from: "skill/SKILL.md (Step 5b)"
      to: "skill/SKILL.md (Step 6)"
      via: "Step 5b sets ${format}; Step 6 loads `formats/${format}.html`"
      pattern: "formats/\\$\\{format\\}\\.html|formats/.*\\.html"
    - from: "skill/design/formats/presentation.html"
      to: "skill/design/components.css (.nav-a) + skill/design/palette.css (--white, --g6, --g8)"
      via: "Three-CSS-file inline at SKILL.md Step 6 supplies tokens; .nav-a is reused from the handbook sidebar pattern"
      pattern: "var\\(--g6\\)|var\\(--white\\)|class=\"nav-a"
---

<objective>
Ship the Presentation format skeleton AND wire it into SKILL.md alongside the other two formats so Phase 3's downstream plans (03-02 interview files, 03-04 fixture runs) can drive a real `/deshtml` invocation that ends in a full-viewport slide deck. Closes DOC-04 (Presentation format) and one half of DESIGN-04 (auto-format selection).

Plan 03-02 ships the four interview files. Plan 03-03 extends the audit harvester to pick up presentation.html's classes. Plan 03-04 runs the per-type fixtures end-to-end. This plan is the upstream piece — without `formats/presentation.html` and SKILL.md's Step 5b + variable skeleton load, there is no Phase 3 flow to test.

Three tasks:
1. **30-min scroll-snap spike** (D3-03 — non-negotiable per CONTEXT.md §"Specific Ideas": *"Presentation is the squishiest piece — the 30-min spike is non-negotiable"*). Build a stub HTML, open in Chrome + Safari, run the 7-step test sequence from RESEARCH §"Spike Build Sheet". Outcome dictates whether `presentation.html` ships full scroll-snap (PASS) or `:target`-only fallback (FAIL).
2. **Build `skill/design/formats/presentation.html`** verbatim from RESEARCH §"Code Examples → Verified Pattern: presentation.html skeleton (full)". Two `<style>` blocks (layout + slide-deck typography), `<main class="deck">` snap container, `id="slide-N"` anchors, `slide-counter::before` with `TOTAL_SLIDES_LITERAL` placeholder, floating `<nav class="slide-nav">` reusing `.nav-a`. No `<aside class="sidebar">` (D3-09).
3. **Update `skill/SKILL.md`** — flip the four Phase-3 stubs in Step 2 to real interview routes (D3-14), insert a new Step 5b for D3-01 format selection (D3-15), change Step 6's hard-coded `formats/handbook.html` to a variable `formats/${format}.html` load (D3-16). Stay ≤200 lines (D3-17).

Purpose: Presentation is the only format where the design system has no Caseproof reference. The spike is the only mechanical signal we have that the snap pattern works in Safari before plan 03-04's fixture run; failing late means a Phase 3 redo. SKILL.md is the integration point — the four routes plus Step 5b plus the variable skeleton path collectively switch deshtml from "handbook-only with stubs for the rest" to "all five doc types working."

Output: one new file (`presentation.html`), one modified file (`SKILL.md`). After this plan, Wave-1 plans 03-02 (interview files) and 03-03 (audit harvester) are unblocked; their `files_modified` do not overlap with this plan, so they run in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-remaining-four-doc-types/03-CONTEXT.md
@.planning/phases/03-remaining-four-doc-types/03-RESEARCH.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-SUMMARY.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-02-SUMMARY.md
@.planning/research/PITFALLS.md
@skill/SKILL.md
@skill/design/SYSTEM.md
@skill/design/palette.css
@skill/design/typography.css
@skill/design/components.css
@skill/design/formats/handbook.html
@skill/design/formats/overview.html
@/Users/sperezasis/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run the 30-min scroll-snap spike (D3-03) and record the outcome</name>
  <files>(no checked-in artifact for the spike itself — outcome is recorded in Task 2's `presentation.html` header comment)</files>
  <read_first>
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Spike Build Sheet (D3-03 — Plan 03-01 Task 1)" (the exact stub HTML to build, the 7-step test sequence, the fallback design if any test fails)
    - .planning/phases/03-remaining-four-doc-types/03-CONTEXT.md §"D3-03" (spike-before-build mandate)
    - .planning/phases/03-remaining-four-doc-types/03-CONTEXT.md §"Specific Ideas" ("Presentation is the squishiest piece — the 30-min spike is non-negotiable")
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Pattern 3: Presentation format — scroll-snap pattern" (why `<main>` not `<html>`)
  </read_first>
  <action>
    Time-budgeted spike. **Hard timeout: 30 minutes.** If the 30-min budget elapses without a conclusive Chrome+Safari pass, fall back to `:target`-only mode and proceed to Task 2 with the snap properties dropped.

    ### Step 1 — Build the stub

    Create a temporary file at `/tmp/deshtml-snap-spike.html` (NOT committed) containing the verbatim spike HTML from RESEARCH §"Spike Build Sheet" (lines 691-743 of `03-RESEARCH.md`). Three slides, raw colors `#F0F5FE` / `#F0FAF0` / `#FFF5E6` (no design tokens — this is a one-off spike), `system-ui` font (no Inter `@import` — keeps the spike self-contained and offline-safe).

    ```bash
    # Don't paste the file content here as a heredoc — use Write tool.
    # File path: /tmp/deshtml-snap-spike.html
    # Source: 03-RESEARCH.md §"Spike Build Sheet" verbatim
    ```

    ### Step 2 — Run the 7-step test sequence

    Per RESEARCH §"Spike Build Sheet" test table:

    | # | Browser | Action | Expected | Pass criteria |
    |---|---------|--------|----------|---------------|
    | 1 | Chrome (latest macOS) | Open file via `file://` | First slide fills viewport, blue bg | Visual match |
    | 2 | Chrome | Scroll wheel down once | Snaps to slide 2 (green), no half-state | No partial scroll lingering |
    | 3 | Chrome | Trackpad fast-scroll all the way down | Lands on slide 3, NOT skipped past | `scroll-snap-stop: always` confirmed |
    | 4 | Chrome | Click `<a href="#slide-2">` in nav | Jumps to slide 2 cleanly, no overshoot | Anchor + snap composes |
    | 5 | Safari (latest macOS) | Repeat steps 1-4 | Same behavior | **PRIMARY VERIFICATION** |
    | 6 | Safari | Resize window to ~600px height, scroll | Slides resize, snap still works | Responsive viewport handling |
    | 7 | Safari | DevTools → throttle CPU 4x → fast-scroll | Snap fires, may be jittery but completes | iOS-Safari proxy |

    Open the spike file in both browsers via:

    ```bash
    open -a "Google Chrome" /tmp/deshtml-snap-spike.html
    open -a Safari /tmp/deshtml-snap-spike.html
    ```

    Run each step. Record PASS/FAIL on each.

    ### Step 3 — Decide outcome

    **PASS** (all 7 steps pass in both browsers): Task 2 ships the full scroll-snap CSS verbatim from RESEARCH §"Code Examples → Verified Pattern: presentation.html skeleton (full)". `presentation.html`'s header comment records `Spike outcome: PASS — Chrome + Safari macOS scroll-snap verified <date>`.

    **FAIL** (any step fails on either browser): Task 2 ships the `:target`-only fallback. Drop these properties from the CSS:
    - `scroll-snap-type: y mandatory` from `main.deck`
    - `scroll-snap-align: start` and `scroll-snap-stop: always` from `.slide`

    Keep everything else: `id="slide-N"`, the floating `<nav class="slide-nav">`, the `100vh` slide height, the slide-counter, the typography. Behavior degrades to "anchor click scrolls to target; user can free-scroll between slides." `presentation.html`'s header comment records `Spike outcome: FAIL on <browser/step>; :target-only fallback engaged. Snap properties intentionally omitted. See README Phase 4 for known limitation.`

    ### Step 4 — Clean up

    Delete `/tmp/deshtml-snap-spike.html` after recording the outcome. The spike is a one-off; no commit, no checked-in artifact. The outcome lives in `presentation.html`'s header comment (Task 2 owns that file).

    ```bash
    rm -f /tmp/deshtml-snap-spike.html
    ```

    ### What this task produces (handoff to Task 2)

    A boolean (PASS/FAIL) plus, if FAIL, which browser and which step failed. Format the handoff as a one-line summary that Task 2 will paste into `presentation.html`'s header comment:

    - PASS: `Spike outcome: PASS — Chrome + Safari macOS scroll-snap verified 2026-04-28. All 7 test steps passed.`
    - FAIL: `Spike outcome: FAIL on <browser> step <N>; :target-only fallback engaged. Snap properties intentionally omitted. See README Phase 4 for known limitation.`

    No file is created or modified by this task — the artifact is the decision recorded in Task 2's file header. The verify block below confirms the spike was actually run by checking that Task 2's file (created next) contains a `Spike outcome:` line in its top-of-file comment.
  </action>
  <behavior>
    - Spike file `/tmp/deshtml-snap-spike.html` was created, opened in both browsers, the 7-step sequence was attempted, and the file was deleted.
    - The PASS/FAIL outcome is propagated to Task 2 verbatim and appears in `presentation.html`'s header comment (verified by Task 2's verify block).
    - **IMPORTANT — automated verification of this task is intentionally skipped here.** This task's only artifact is the spike outcome decision, which is propagated to Task 2's file. The Task 2 verify block proves the spike was run by checking for the `Spike outcome:` line.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      ! test -f /tmp/deshtml-snap-spike.html &&
      echo "Spike file cleaned up. Outcome recorded in Task 2's presentation.html header (verified by Task 2)."
    </automated>
  </verify>
  <acceptance_criteria>
    - The 7-step test sequence from RESEARCH §"Spike Build Sheet" was run in Chrome AND Safari (macOS).
    - The PASS/FAIL outcome is communicated to Task 2 (via the human running both tasks in sequence) and appears verbatim in `skill/design/formats/presentation.html`'s top-of-file comment.
    - `/tmp/deshtml-snap-spike.html` does NOT remain on disk after the spike (single-use artifact).
    - The 30-min hard timeout was respected. If the timeout was hit without a clean Chrome+Safari PASS, the FAIL fallback was engaged in Task 2.
  </acceptance_criteria>
  <done>
    The 30-min spike has been run, the outcome (PASS or FAIL with browser+step detail) has been determined, and Task 2 has been instructed which `presentation.html` shape to ship (full snap or `:target`-only fallback). The spike's temporary file has been cleaned up.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build skill/design/formats/presentation.html (verbatim from RESEARCH, with spike-outcome header comment)</name>
  <files>skill/design/formats/presentation.html</files>
  <read_first>
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Code Examples → Verified Pattern: presentation.html skeleton (full)" (lines 908-1017 — the verbatim source)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Pattern 3" (snap container scope), §"Pattern 4" (CSS-only counter), §"Pattern 5" (anchor nav reusing .nav-a), §"Pattern 6" (slide-deck typography)
    - .planning/phases/03-remaining-four-doc-types/03-CONTEXT.md §"D3-04, D3-05, D3-06, D3-07, D3-08, D3-09" (slide structure, counter, anchor nav, no transitions, type scale, no sidebar)
    - skill/design/formats/handbook.html (the Phase-1 sibling — copy header structure verbatim: doctype, lang, charset, viewport, color-scheme meta tags, two `<link rel="stylesheet">` references)
    - skill/design/formats/overview.html (the second Phase-1 sibling — confirm `scroll-padding-top: 0` pattern Pitfall 9 mitigation)
    - skill/design/components.css (confirm `.nav-a` is defined; this skeleton REUSES it — no new class needed)
    - The PASS/FAIL outcome from Task 1 (the human verifier propagates this to the file header comment)
  </read_first>
  <action>
    Create `skill/design/formats/presentation.html` with the verbatim skeleton from RESEARCH §"Code Examples → Verified Pattern: presentation.html skeleton (full)" plus a top-of-file header comment that records the Task 1 spike outcome.

    ### Step 1 — Header comment (FIRST line of file body, after `<!DOCTYPE html>` and `<html>`)

    Insert this comment block at the top of the `<head>` section, immediately after `<meta name="viewport"...>`:

    ```html
    <!--
      Caseproof Documentation System — Presentation format skeleton.
      Verbatim from .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md
      §"Code Examples → Verified Pattern: presentation.html skeleton (full)".

      Spike outcome (D3-03, plan 03-01 Task 1):
      <PASTE THE TASK 1 OUTCOME LINE HERE — exactly one of:
       - "Spike outcome: PASS — Chrome + Safari macOS scroll-snap verified <YYYY-MM-DD>. All 7 test steps passed."
       - "Spike outcome: FAIL on <browser> step <N>; :target-only fallback engaged. Snap properties intentionally omitted. See README Phase 4 for known limitation.">

      Sister formats:
        - design/formats/handbook.html — 220px sidebar + 960px main, sticky bar.
        - design/formats/overview.html — 1440px linear, no sidebar, no sticky.
        - design/formats/presentation.html (this file) — full-viewport slides,
          NO sidebar, NO sticky bar (D3-09), CSS-only `scroll-snap-type: y mandatory`
          on `<main>` (Safari fragility mitigation per RESEARCH §Pattern 3).

      SKILL.md Step 6 inlines palette.css + typography.css + components.css into
      a single <style> block (D2-15 three-file inlining contract). The two
      <link> tags below are dev-time references that SKILL.md replaces at
      assembly time. Do not edit them; the audit's Rule 4 catches any leftover
      <link rel="stylesheet"> in generated output.
    -->
    ```

    ### Step 2 — Decide CSS shape from Task 1 outcome

    **If Task 1 returned PASS:** ship the full scroll-snap CSS verbatim from RESEARCH §"Code Examples → Verified Pattern: presentation.html skeleton (full)" lines 921-995.

    **If Task 1 returned FAIL:** ship the same skeleton with these three properties REMOVED (per RESEARCH §"Spike Build Sheet → On fail — fallback design"):
    - From `main.deck`: `scroll-snap-type: y mandatory;`
    - From `.slide`: `scroll-snap-align: start;`
    - From `.slide`: `scroll-snap-stop: always;`

    Keep everything else: `overflow-y: scroll`, `height: 100vh` on `main.deck`; `height: 100vh`, `display: flex; flex-direction: column; justify-content: center; padding: 80px 120px; box-sizing: border-box; counter-increment: slide-num; position: relative` on `.slide`. Anchor links still work via the browser's default fragment-navigation behavior.

    ### Step 3 — Full file content (PASS case)

    Below is the verbatim content for the PASS case. Adjust per Step 2 if Task 1 returned FAIL.

    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <!-- HEADER COMMENT FROM STEP 1 GOES HERE -->
      <meta name="color-scheme" content="light">
      <meta name="supported-color-schemes" content="light">
      <title><!-- DOC TITLE --></title>
      <link rel="stylesheet" href="../palette.css">
      <link rel="stylesheet" href="../typography.css">
      <style>
        /* Presentation layout — full-viewport vertical slide deck.
           NO sidebar, NO sticky bar. Snap container scoped to <main>, not <html>,
           to avoid Safari's html-as-snap-container fragility. */
        html { scroll-padding-top: 0; }
        body { background: var(--white); margin: 0; }

        main.deck {
          scroll-snap-type: y mandatory;
          overflow-y: scroll;
          height: 100vh;
          margin: 0;
          counter-reset: slide-num;
        }

        .slide {
          scroll-snap-align: start;
          scroll-snap-stop: always;
          height: 100vh;
          width: 100%;
          display: flex;
          flex-direction: column;
          justify-content: center;
          padding: 80px 120px;
          box-sizing: border-box;
          counter-increment: slide-num;
          position: relative;
        }

        .slide-counter {
          position: absolute;
          bottom: 24px;
          right: 32px;
          font-size: 14px;
          font-weight: 700;
          color: var(--g6);
          letter-spacing: 1.2px;
        }
        .slide-counter::before {
          /* TOTAL_SLIDES_LITERAL is replaced by Claude at render time with the literal slide count. */
          content: counter(slide-num) " / TOTAL_SLIDES_LITERAL";
        }

        .slide-nav {
          position: fixed;
          top: 24px;
          right: 32px;
          display: flex;
          gap: 8px;
          z-index: 10;
        }
      </style>
      <style>
        /* Slide-deck type scale. Rides on Inter @import from typography.css.
           Larger than the document scale because slides are read at distance. */
        .slide h1 {
          font-size: 80px;
          font-weight: 800;
          letter-spacing: -3px;
          line-height: 1.05;
        }
        .slide h2 {
          font-size: 56px;
          font-weight: 800;
          letter-spacing: -2px;
          line-height: 1.1;
        }
        .slide p,
        .slide li {
          font-size: 22px;
          font-weight: 400;
          line-height: 1.5;
          color: var(--g8);
        }
      </style>
    </head>
    <body>

    <!-- SLIDE NAV: floating top-right anchor links to each slide. Reuses .nav-a from components.css. -->
    <nav class="slide-nav">
      <!-- SLIDE NAV ITEMS SLOT -->
    </nav>

    <main class="deck">

      <!-- SLIDE 1..N: each section is a full-viewport slide with optional .slide-counter pill. -->
      <section class="slide" id="slide-1">
        <!-- SLIDE 1 H1 SLOT -->
        <!-- SLIDE 1 BODY SLOT (components from components.html allowlist) -->
        <div class="slide-counter"></div>
      </section>

    </main>

    </body>
    </html>
    ```

    ### Step 4 — Substitute the spike outcome line

    Replace the `<PASTE THE TASK 1 OUTCOME LINE HERE ...>` placeholder in the header comment with the literal outcome string from Task 1. Exactly ONE of the two formats Task 1 specified must appear, with no `<PASTE>` placeholders left in the file.

    ### Step 5 — Verify no `<aside>` or sticky bar leaked in

    The skeleton MUST NOT contain `<aside class="sidebar">` (D3-09 — Presentation has no sidebar). It MUST NOT contain any `position: sticky` element. The verify block below greps both.

    ### Step 6 — Verify no new color tokens were introduced

    Every color reference in the inline `<style>` blocks is `var(--white)`, `var(--g6)`, or `var(--g8)` — all three already exist in `palette.css` (Phase 2 extended palette). NO hex literal appears outside the upstream `:root` block (which lives in palette.css, not this file). The verify block greps for hex literals.

    ### What this skeleton must NOT do

    - Must not redefine `:root` (palette.css owns it).
    - Must not redefine body typography (typography.css owns it).
    - Must not introduce a new component class — `slide`, `slide-counter`, `slide-nav` are introduced HERE; `.nav-a` is REUSED from components.css. Plan 03-03's audit harvester picks up `.slide`, `.slide-counter`, `.slide-nav` automatically via the wildcard glob (D3-18).
    - Must not include any JavaScript (D-17 + Phase 2 audit Rule 3).
    - Must not set `html { scroll-padding-top: 80px; }` — that's Handbook's pattern and would leave an 80px gap on every anchor click (Pitfall 9 refinement). Use `scroll-padding-top: 0` (matching overview.html) or omit.
    - Must not modify typography.css or any other file. Slide-deck typography lives in this file's second `<style>` block (per Pattern 6 — `typography.css` is the document scale; slide scale is presentation-format-only).
  </action>
  <behavior>
    - File `skill/design/formats/presentation.html` exists and is non-empty.
    - First top-of-file comment block contains: `Caseproof Documentation System — Presentation format skeleton`, `Verbatim from .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md`, AND a `Spike outcome:` line that begins with either `Spike outcome: PASS` or `Spike outcome: FAIL`.
    - File contains `<meta name="color-scheme" content="light">` AND `<meta name="supported-color-schemes" content="light">` (DESIGN-07 carry-over).
    - File contains `<link rel="stylesheet" href="../palette.css">` and `<link rel="stylesheet" href="../typography.css">` (dev-time references — SKILL.md Step 6 replaces these at render time per D2-15).
    - File contains `<main class="deck">` (snap container) — NOT `<html>` or `<body>` as the snap container.
    - File contains `id="slide-1"` (at least one slide with anchor ID).
    - File contains `<nav class="slide-nav">` AND a `<!-- SLIDE NAV ITEMS SLOT -->` comment.
    - File contains `<div class="slide-counter">` AND `.slide-counter::before { ... TOTAL_SLIDES_LITERAL ... }` literal placeholder.
    - File contains `html { scroll-padding-top: 0; }` (Pitfall 9 refinement — NOT Handbook's 80px).
    - File does NOT contain `<aside class="sidebar">` (D3-09 — no sidebar).
    - File does NOT contain `position: sticky` anywhere.
    - File does NOT contain any hex literal (`#XXXXXX`) outside HTML comments — colors are `var(--white)`, `var(--g6)`, `var(--g8)`.
    - File does NOT contain `<script>`, `<iframe>`, `<object>`, `<embed>`, `on*=`, or `javascript:` URLs.
    - File contains exactly two `<style>` blocks (layout + slide-deck typography per Pattern 6).
    - File total length is ≤120 lines (the verbatim skeleton is ~110 lines + header comment).
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s skill/design/formats/presentation.html &&
      [ "$(wc -l < skill/design/formats/presentation.html)" -le 120 ] &&
      command grep -qF 'Caseproof Documentation System — Presentation format skeleton' skill/design/formats/presentation.html &&
      command grep -qE 'Spike outcome: (PASS|FAIL)' skill/design/formats/presentation.html &&
      ! command grep -qF 'PASTE THE TASK 1 OUTCOME LINE HERE' skill/design/formats/presentation.html &&
      command grep -qF '<meta name="color-scheme" content="light">' skill/design/formats/presentation.html &&
      command grep -qF '<meta name="supported-color-schemes" content="light">' skill/design/formats/presentation.html &&
      command grep -qF '<link rel="stylesheet" href="../palette.css">' skill/design/formats/presentation.html &&
      command grep -qF '<link rel="stylesheet" href="../typography.css">' skill/design/formats/presentation.html &&
      command grep -qF '<main class="deck">' skill/design/formats/presentation.html &&
      command grep -qF 'id="slide-1"' skill/design/formats/presentation.html &&
      command grep -qF '<nav class="slide-nav">' skill/design/formats/presentation.html &&
      command grep -qF '<!-- SLIDE NAV ITEMS SLOT -->' skill/design/formats/presentation.html &&
      command grep -qF 'class="slide-counter"' skill/design/formats/presentation.html &&
      command grep -qF 'TOTAL_SLIDES_LITERAL' skill/design/formats/presentation.html &&
      command grep -qF 'scroll-padding-top: 0' skill/design/formats/presentation.html &&
      ! command grep -qF '<aside class="sidebar">' skill/design/formats/presentation.html &&
      ! command grep -qE 'position:[[:space:]]*sticky' skill/design/formats/presentation.html &&
      ! command grep -nE '<(script|iframe|object|embed)\b' skill/design/formats/presentation.html &&
      ! command grep -nEi '(href|src|action|formaction|xlink:href)[[:space:]]*=[[:space:]]*["'"'"']?[[:space:]]*javascript:' skill/design/formats/presentation.html &&
      ! command grep -nEi '(^|[[:space:]])on[a-z]+[[:space:]]*=' skill/design/formats/presentation.html &&
      ! awk '/<!--/,/-->/{next} {print}' skill/design/formats/presentation.html | command grep -E '#[0-9a-fA-F]{3,8}\b' &&
      [ "$(command grep -c '<style>' skill/design/formats/presentation.html)" = "2" ] &&
      command grep -qF 'counter-reset: slide-num' skill/design/formats/presentation.html &&
      command grep -qF 'counter-increment: slide-num' skill/design/formats/presentation.html &&
      command grep -qE 'font-size:[[:space:]]*80px' skill/design/formats/presentation.html &&
      command grep -qE 'font-size:[[:space:]]*56px' skill/design/formats/presentation.html &&
      command grep -qE 'font-size:[[:space:]]*22px' skill/design/formats/presentation.html &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `skill/design/formats/presentation.html` exists, ≤120 lines.
    - Top-of-file header comment includes the Phase 2-style provenance line plus a literal `Spike outcome: PASS` or `Spike outcome: FAIL` line (no `PASTE` placeholders remain).
    - Header structure matches handbook.html / overview.html: doctype, lang="en", charset, viewport, color-scheme + supported-color-schemes meta tags, title slot, two `<link rel="stylesheet">` dev-time references.
    - Snap container is `<main class="deck">` — not `<html>` or `<body>`.
    - At least one `<section class="slide" id="slide-1">` exists.
    - `<nav class="slide-nav">` floating-top-right structure is present (with a NAV ITEMS slot comment).
    - Slide counter uses CSS counter-increment with `TOTAL_SLIDES_LITERAL` placeholder (Claude substitutes at render time per Pattern 4).
    - `html { scroll-padding-top: 0; }` is set (NOT 80px — Pitfall 9 refinement).
    - No sidebar, no sticky bar (D3-09).
    - Two `<style>` blocks present (layout + slide-deck typography per Pattern 6).
    - Slide typography scale is H1 80px / H2 56px / body 22px per D3-08.
    - Zero hex literals outside HTML comments (`<!--...-->`); every color is `var(--white)`, `var(--g6)`, or `var(--g8)`.
    - Zero banned tags / handlers / `javascript:` URLs (would fail the audit).
    - If Task 1 returned FAIL: `scroll-snap-type`, `scroll-snap-align`, `scroll-snap-stop` are absent from the inline `<style>` (`:target`-only fallback). Otherwise all three are present.
  </acceptance_criteria>
  <done>
    `skill/design/formats/presentation.html` ships as the third format skeleton in `design/formats/`. Its CSS shape matches the Task 1 spike outcome: full scroll-snap on PASS, `:target`-only fallback on FAIL, with the outcome documented in the file's header comment. Header structure mirrors the two Phase-1 sibling skeletons (handbook.html, overview.html). All 26 enumerated automated checks pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update skill/SKILL.md — flip 4 type stubs to interview routes (D3-14), insert Step 5b (D3-15), variable skeleton load (D3-16), preserve ≤200 cap (D3-17)</name>
  <files>skill/SKILL.md</files>
  <read_first>
    - skill/SKILL.md (the current Phase-2 file — 174 lines per Phase 2 SUMMARY; the Step 2 stubs at lines 36-54, Step 5 collision loop at 71-90, Step 6 hard-coded handbook.html at 92-133)
    - .planning/phases/03-remaining-four-doc-types/03-CONTEXT.md §"D3-14, D3-15, D3-16, D3-17" (the four updates and the 200-line cap)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Code Examples → Verified Pattern: SKILL.md Step 2 type-branch (full Phase 3 form)" (the verbatim replacement for Step 2's stub block)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Code Examples → Verified Pattern: SKILL.md Step 5b (NEW)" (the verbatim insertion between existing Step 5 and Step 6)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Pattern 2: Skeleton routing in SKILL.md Step 6 (D3-16)" (the one-line change: `formats/handbook.html` → `formats/${format}.html`)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-02-SUMMARY.md (Phase 2 working assumptions about `$ARGUMENTS` — preserved unchanged)
  </read_first>
  <action>
    Three edits to `skill/SKILL.md`. Everything else stays byte-for-byte.

    ### Edit 1 — Replace Step 2 stub block with full type-branch routes (D3-14)

    Find the current Step 2 block (currently lines ~35-53 in the working copy). Replace this section:

    ```markdown
    - `handbook` → continue to Step 3.
    - `pitch`, `technical brief`, `presentation`, `meeting prep` → reply with EXACTLY:
      > `<type>` is coming in Phase 3. Try `handbook` for now.

      Substitute the user's choice in `<type>`. Stop. Do not silently fall back.

    - Anything else → ask once more, listing the same five options. If the second
      reply is still not in the list, reply with EXACTLY:
      > That is not one of the five document types. Run `/deshtml` and pick `handbook` to continue.

      Then stop.
    ```

    With this verbatim replacement (per RESEARCH §"Code Examples → Verified Pattern: SKILL.md Step 2 type-branch"):

    ```markdown
    - `handbook` → continue to Step 3 (load `interview/handbook.md`).
    - `pitch` → continue to Step 3 (load `interview/pitch.md`).
    - `technical brief` → continue to Step 3 (load `interview/technical-brief.md`).
    - `presentation` → continue to Step 3 (load `interview/presentation.md`).
    - `meeting prep` → continue to Step 3 (load `interview/meeting-prep.md`).

    - Anything else → ask once more, listing the same five options. If the second
      reply is still not in the list, reply with EXACTLY:
      > That is not one of the five document types. Run `/deshtml` and pick one to continue.

      Then stop.
    ```

    The four `is coming in Phase 3` stubs are removed. The catch-all "Anything else" branch's stop message changes `pick \`handbook\` to continue` → `pick one to continue` (the user can now pick any of the five).

    ### Edit 2 — Update Step 3 to load the per-type interview file (D3-14 follow-through)

    The current Step 3 (lines ~55-62) reads `interview/handbook.md` hard-coded. Phase 3 makes this variable. Replace this paragraph:

    ```markdown
    Read `${CLAUDE_SKILL_DIR}/interview/handbook.md` now. Follow it end-to-end.
    Return here only after the user has answered the five questions (or stopped
    early per that file's instructions).
    ```

    With this:

    ```markdown
    Read `${CLAUDE_SKILL_DIR}/interview/${type}.md` now (where `${type}` is the
    normalized doc type from Step 2 — `handbook`, `pitch`, `technical-brief`,
    `presentation`, or `meeting-prep`). Follow it end-to-end. Return here only
    after the user has answered the questions (or stopped early per that file's
    instructions).
    ```

    Note the kebab-cased filenames: the user types `technical brief` (with space) and `meeting prep` (with space) at Step 2; SKILL.md normalizes those to `technical-brief` and `meeting-prep` for the file path. State this normalization explicitly. The four interview files (plan 03-02) ship with kebab-cased filenames matching this convention.

    Do NOT change the "Do NOT read `story-arc.md` yet (Pitfall 15)" line — preserve it.

    ### Edit 3 — Insert Step 5b between existing Step 5 and Step 6 (D3-15)

    After the existing Step 5 block ends (after the closing `Use the final $target for Step 6. Compute its absolute path: ${PWD}/${target}.` line), insert this NEW step verbatim from RESEARCH §"Code Examples → Verified Pattern: SKILL.md Step 5b (NEW)":

    ```markdown
    ## Step 5b — Select format

    Determine which format skeleton Step 6 will use. The decision is mechanical:

    1. If the document type from Step 2 == `presentation` → format = `presentation`.
    2. Else if the approved arc has 4 or more rows → format = `handbook`.
    3. Else → format = `overview`.

    Print exactly one line to the user (no other prose, no decoration):

    > Format: <format>

    Example output: `Format: handbook` or `Format: overview` or `Format: presentation`.

    Then continue to Step 6.
    ```

    The blank line before `## Step 6` stays. The numbering of subsequent steps does NOT change — Step 6 is still Step 6, Step 7 is still Step 7, Step 8 is still Step 8. "Step 5b" is intentional (5+a-letter), keeping the existing step numbers stable.

    ### Edit 4 — Replace Step 6's hard-coded `formats/handbook.html` with variable load (D3-16)

    In Step 6's "Read these five files in parallel" list, find this line:

    ```markdown
    - `${CLAUDE_SKILL_DIR}/design/formats/handbook.html` (skeleton)
    ```

    Replace with:

    ```markdown
    - `${CLAUDE_SKILL_DIR}/design/formats/${format}.html` (skeleton — `${format}` was set in Step 5b: `handbook`, `overview`, or `presentation`)
    ```

    The slot-fill instructions in Step 6 already use generic slot-comment vocabulary (`<!-- HERO H1 SLOT -->`, `<!-- SECTION 1 EYEBROW/H2/BODY -->`, etc.) which is shared between handbook.html and overview.html. presentation.html introduces additional slide-specific slots (`<!-- SLIDE NAV ITEMS SLOT -->`, `<!-- SLIDE 1 H1 SLOT -->`, `<!-- SLIDE 1 BODY SLOT -->`). Step 6 should mention these new slots so executors know what to fill when format == presentation. Append this paragraph to Step 6's slot-fill section (after the existing slot enumeration):

    ```markdown
    When `${format}` is `presentation`, the skeleton has slide-specific slots instead of section slots:
    - `<!-- SLIDE NAV ITEMS SLOT -->` — one `<a class="nav-a" href="#slide-N">N</a>` per slide.
    - `<!-- SLIDE 1 H1 SLOT -->` (one per arc row) — the slide's H1.
    - `<!-- SLIDE 1 BODY SLOT -->` (one per arc row) — body content from the components.html allowlist.
    - The `TOTAL_SLIDES_LITERAL` literal in the inline `<style>` block must be replaced with the literal slide count from the approved arc (e.g., `5`). The `<div class="slide-counter">` inside each slide does NOT need to be filled — its content is generated by the CSS `::before` pseudo-element.
    ```

    ### Edit 5 — Verify ≤200-line cap (D3-17)

    The Phase 2 SKILL.md is 171 lines (per 02-SUMMARY.md). The four edits above add approximately:
    - Edit 1: ~3 net lines (5 routes − 1 stub block; minor rewording).
    - Edit 2: ~1 net line (parameterized variable in prose).
    - Edit 3: ~14 lines (the entire Step 5b block).
    - Edit 4: ~5 lines (variable load + presentation-slot paragraph).

    Total: ~23 net new lines → ~194 lines. Under the 200-line cap.

    If the file exceeds 200 lines, trim PROSE in the existing Step 6 narrative (e.g., shorten the "no scripts, no link rel=stylesheet, no on*= handlers, no javascript: URLs" enumeration to a one-line referenced check). Do NOT trim flow-control content. Do NOT factor route bodies into sub-files unless absolutely necessary — D3-17 says factoring is the LAST resort.

    ### What does NOT change

    - Frontmatter (`name: deshtml`, `disable-model-invocation: true`, `allowed-tools` line).
    - Step 1 (mode detection regex `(^|[[:space:]])@\S+` and the source-mode stub message).
    - Step 4 (story-arc.md hand-off).
    - Step 5 (filename + collision loop).
    - Step 6's three-CSS-file inline order (palette → typography → components).
    - Step 7 (audit invocation + max-2-retry rule).
    - Step 8 (open + path-print as LAST output, no banner, no emoji).
    - The "Constraints this file enforces" block at the bottom.
  </action>
  <behavior>
    - File `skill/SKILL.md` exists and is ≤200 lines (D3-17 hard cap).
    - Frontmatter unchanged: `name: deshtml`, `disable-model-invocation: true`, the same `allowed-tools` line as Phase 2.
    - Step 1's mode-detection regex `(^|[[:space:]])@\S+` is preserved.
    - Step 1's source-mode stub message is preserved verbatim.
    - Step 2 lists all five doc types (handbook, pitch, technical brief, presentation, meeting prep) in the prompt.
    - Step 2 does NOT contain the phrase `is coming in Phase 3` (all four stubs flipped per D3-14).
    - Step 2 does NOT contain `Try \`handbook\` for now` (the catch-all message is updated).
    - Step 3 reads `${CLAUDE_SKILL_DIR}/interview/${type}.md` (variable interview file path).
    - Step 3 enumerates the five normalized type values: `handbook`, `pitch`, `technical-brief`, `presentation`, `meeting-prep` (kebab-cased).
    - File contains a new `## Step 5b — Select format` heading (verbatim from RESEARCH).
    - Step 5b contains the three-rule decision tree (presentation → presentation; ≥4 rows → handbook; else overview).
    - Step 5b instructs printing `Format: <format>` as a single line.
    - Step 6 reads `${CLAUDE_SKILL_DIR}/design/formats/${format}.html` (variable skeleton load — D3-16).
    - Step 6 mentions the presentation-specific slots (SLIDE NAV ITEMS, SLIDE N H1/BODY, TOTAL_SLIDES_LITERAL).
    - Step 7 invocation `bash "${CLAUDE_SKILL_DIR}/audit/run.sh"` is preserved.
    - Step 8 path-print + LAST output language is preserved.
    - File contains zero copies of the phrase `Try \`handbook\` for now`.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s skill/SKILL.md &&
      [ "$(wc -l < skill/SKILL.md)" -le 200 ] &&
      command grep -q '^name: deshtml$' skill/SKILL.md &&
      command grep -q '^disable-model-invocation: true$' skill/SKILL.md &&
      command grep -q '^allowed-tools:' skill/SKILL.md &&
      command grep -qF '(^|[[:space:]])@\S+' skill/SKILL.md &&
      command grep -qF 'Source mode is coming in Phase 4' skill/SKILL.md &&
      command grep -q '## Step 1' skill/SKILL.md &&
      command grep -q '## Step 2' skill/SKILL.md &&
      command grep -q '## Step 3' skill/SKILL.md &&
      command grep -q '## Step 4' skill/SKILL.md &&
      command grep -q '## Step 5' skill/SKILL.md &&
      command grep -q '## Step 5b' skill/SKILL.md &&
      command grep -q '## Step 6' skill/SKILL.md &&
      command grep -q '## Step 7' skill/SKILL.md &&
      command grep -q '## Step 8' skill/SKILL.md &&
      command grep -qF 'interview/handbook.md' skill/SKILL.md &&
      command grep -qF 'interview/pitch.md' skill/SKILL.md &&
      command grep -qF 'interview/technical-brief.md' skill/SKILL.md &&
      command grep -qF 'interview/presentation.md' skill/SKILL.md &&
      command grep -qF 'interview/meeting-prep.md' skill/SKILL.md &&
      ! command grep -qF 'is coming in Phase 3' skill/SKILL.md &&
      ! command grep -qF 'Try `handbook` for now' skill/SKILL.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/interview/${type}.md' skill/SKILL.md &&
      command grep -qiE 'Select format' skill/SKILL.md &&
      command grep -qiE 'arc has 4 or more rows|arc.*4.*rows|4\+ rows|>= 4 rows' skill/SKILL.md &&
      command grep -qF 'Format: <format>' skill/SKILL.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/design/formats/${format}.html' skill/SKILL.md &&
      command grep -qF 'TOTAL_SLIDES_LITERAL' skill/SKILL.md &&
      command grep -qiE 'SLIDE NAV ITEMS SLOT|SLIDE 1 H1 SLOT|SLIDE.*BODY SLOT' skill/SKILL.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/audit/run.sh' skill/SKILL.md &&
      command grep -qiE 'maximum 2 retry|max.*2.*retry|2 retry rounds' skill/SKILL.md &&
      command grep -qiE 'last output|LAST output|stop\. your work' skill/SKILL.md &&
      [ "$(command grep -c '^## Step ' skill/SKILL.md)" = "9" ] &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `skill/SKILL.md` exists, ≤200 lines (D3-17 hard cap).
    - File has 9 step headings: Step 1, 2, 3, 4, 5, 5b, 6, 7, 8 (`grep -c '^## Step '` returns 9).
    - Frontmatter is byte-for-byte identical to Phase 2 (`name`, `disable-model-invocation`, `allowed-tools` line).
    - Step 1's mode-detection regex and source-mode stub are preserved verbatim.
    - Step 2 enumerates all five doc types and routes each to its interview file (zero `is coming in Phase 3` stubs remain).
    - Step 3 reads the per-type interview file via `${CLAUDE_SKILL_DIR}/interview/${type}.md`.
    - Step 5b is present with the three-rule decision tree and the `Format: <format>` print instruction.
    - Step 6 loads `${CLAUDE_SKILL_DIR}/design/formats/${format}.html` (variable, not hard-coded handbook.html).
    - Step 6 documents the presentation-specific slots (SLIDE NAV ITEMS, SLIDE N H1/BODY) and the `TOTAL_SLIDES_LITERAL` substitution.
    - Steps 7 and 8 are byte-for-byte preserved (audit retry rule, path-print as LAST output).
    - File contains zero references to a sixth doc type (PROJECT.md scope-lock).
    - File contains no `Try \`handbook\` for now` text (catch-all message rewritten).
  </acceptance_criteria>
  <done>
    `skill/SKILL.md` is updated with four mechanical edits: Step 2 stubs flipped to interview routes (D3-14), Step 3 reads the per-type interview file via `${type}` substitution, new Step 5b runs the format-selection logic and prints `Format: <format>` (D3-15), Step 6's skeleton load uses the variable `${format}` (D3-16). File length stays ≤200 (D3-17). All Phase-2 content not touched by these four edits is byte-for-byte preserved. The 33 enumerated automated checks pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spike (`/tmp/deshtml-snap-spike.html`) → presentation.html design decision | The 30-min spike's PASS/FAIL is the single signal that decides whether full scroll-snap ships. The decision is permanent (the file's CSS shape locks in); reversing it requires another spike. |
| presentation.html (skeleton) → SKILL.md Step 6 (variable load) | Step 6 will load this skeleton at every Phase-3 invocation when format == presentation. A bad slot, a wrong meta tag, or an extra hex literal here propagates to every Presentation output. |
| SKILL.md Step 5b (format selection) → Step 6 (skeleton path) | Step 5b's mechanical decision tree is the user's only signal for which format ships. A bug in the decision logic (e.g., `>= 4 rows` becomes `> 4 rows`) flips Pitch from Overview to Handbook silently. |
| SKILL.md Step 2 (type-branch) → interview file path | Phase 3 introduces four new file paths; if any is mistyped (e.g., `technical_brief.md` instead of `technical-brief.md`), Step 3's Read fails at runtime, mid-conversation, with no recovery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | A future commit changes `presentation.html`'s snap container from `<main>` to `<html>` "to simplify" — re-introducing the Safari fragility scope (RESEARCH §Pattern 3) | mitigate | The file's header comment says `Snap container scoped to <main>, not <html>, to avoid Safari's html-as-snap-container fragility.` Plan 03-04's visual-gate fixture re-runs the spike's 7 steps on the real skeleton — a regression on Safari surfaces immediately. |
| T-03-02 | Tampering | A future commit removes the `Spike outcome:` line from `presentation.html`'s header, hiding whether the file ships full snap or `:target` fallback | mitigate | The verify block in Task 2 greps for `Spike outcome: (PASS\|FAIL)`. Removing the line fails plan 03-01 acceptance. Plan 03-04's FIXTURE-NOTES.md also references the spike outcome — drift surfaces in two places. |
| T-03-03 | Spoofing | A future Phase-3 plan adds a new doc type without adding a Step 2 route — the type silently falls through to the catch-all "not one of the five" branch | mitigate | Step 2's catch-all branch terminates with a stop message; no fallback to handbook. Plan 03-02 ships the four new interview files; their Step 2 routes are already wired in this plan. The per-type fixture in plan 03-04 verifies all 5 routes fire correctly. |
| T-03-04 | Tampering | The `${type}.md` substitution in Step 3 receives a malformed value (e.g., `pitch ` with trailing space) and the Read fails | mitigate | Step 2's normalization step (`trim whitespace, lowercase`) is preserved from Phase 2. The verify block doesn't test runtime substitution (that's plan 03-04's fixture domain) but the static check confirms the variable substitution syntax is correct. |
| T-03-05 | Tampering | Step 5b's format-selection decision tree is fuzzed in a future revision (e.g., `>= 4 rows` → `> 4 rows`) — silently flips edge-case behavior | mitigate | Step 5b documents the three-rule logic verbatim from RESEARCH §"Code Examples → Verified Pattern: SKILL.md Step 5b (NEW)". A diff of Step 5b against the verbatim source catches drift. Pitfall 21 (RESEARCH §"Pitfall 21") documents the user-facing failure mode. |
| T-03-06 | Information Disclosure | The `Format: <format>` print line at Step 5b leaks the format choice to logs / transcripts | accept | The format choice is a public output of the skill flow; the user is supposed to see it (D3-02 mandate). No PII, no secrets, no internal Caseproof references in the format name. |
| T-03-07 | Denial of Service | The 30-min spike timeout is hit and the verifier (a) doesn't engage the `:target`-only fallback or (b) re-runs the spike for "5 more minutes" indefinitely | mitigate | D3-03 is unambiguous: the timeout IS the gate. CONTEXT.md §"Specific Ideas" reinforces. The Task 1 acceptance criterion explicitly says "the 30-min hard timeout was respected." |
| T-03-08 | Elevation of Privilege | n/a (no executable code in this plan; all changes are HTML/Markdown content) | n/a | presentation.html is pure HTML+CSS. SKILL.md is markdown. No script invocation. The audit's Rule 3 (banned tags) catches any future regression. |
| T-03-09 | Tampering | A future plan adds a sixth doc type to PROJECT.md / Step 2; the auto-format selection logic in Step 5b doesn't account for it | mitigate | PROJECT.md row 6 explicitly enumerates the V1 set as five doc types — adding a sixth is out of scope per CONTEXT.md §"Code Context → Phase 3 → no-skill-extension is locked." If V2 ever adds a sixth, Step 5b's rules need an explicit type-mapping addition; the comment "if type == presentation" makes this an obvious extension point. |
</threat_model>

<verification>
All three task `<verify>` blocks must pass. Specifically:

- Task 1: the spike was actually run in Chrome AND Safari (macOS), the 7-step sequence was attempted, the `/tmp/deshtml-snap-spike.html` was cleaned up, and the outcome was propagated to Task 2's `presentation.html` header.
- Task 2: `presentation.html` exists ≤120 lines, header comment includes a literal `Spike outcome: (PASS|FAIL)` line (no `PASTE` placeholder), `<main class="deck">` is the snap container (not `<html>`), `id="slide-1"` is present, slide-counter and slide-nav are wired, no sidebar, no sticky bar, no hex literals outside HTML comments, no banned tags / handlers / `javascript:` URLs.
- Task 3: `skill/SKILL.md` ≤200 lines (D3-17), 9 step headings (5b inserted), 4 new interview routes (zero `is coming in Phase 3` stubs), variable `${type}.md` interview load, Step 5b decision tree present with `Format: <format>` print, Step 6 loads `formats/${format}.html` plus presentation-specific slot documentation, Steps 1/4/5/7/8 byte-for-byte preserved.

End-to-end Phase 3 flow verification (a `/deshtml` invocation on the locally-installed skill that picks pitch / technical-brief / presentation / meeting-prep and produces a Caseproof-faithful output) is intentionally deferred to plan 03-04 (the per-type fixture plan).
</verification>

<success_criteria>
1. **Presentation skeleton ships and is calibrated to the spike outcome.** `skill/design/formats/presentation.html` exists with full scroll-snap (PASS) or `:target`-only fallback (FAIL), exactly matching what the 30-min Chrome+Safari spike proved.
2. **Skill flow is wired for all five doc types.** SKILL.md Step 2 routes each of the five types to its own interview file. The four `is coming in Phase 3` stubs are gone. Step 3 reads `interview/${type}.md` via variable substitution.
3. **Format auto-selection is mechanical.** Step 5b's three-rule decision tree (presentation → presentation; ≥4 rows → handbook; else overview) is a pure type-and-row-count check. Predictable. Testable. Reversible (user edits arc to flip format).
4. **Variable skeleton routing.** Step 6 reads `formats/${format}.html` instead of the hard-coded handbook path. Slot-fill mechanism is unchanged (presentation.html shares the existing slot-comment vocabulary plus its own slide slots, all documented in Step 6).
5. **SKILL.md ≤200 lines (D3-17).** The four new edits add ~23 net lines; the file lands at ~194 lines. Hard cap respected.
6. **Phase 2 contracts preserved.** Frontmatter, mode detection, story-arc gate, filename + collision loop, audit retry, open + path-print as LAST output — all byte-for-byte unchanged.
7. **Plans 03-02 and 03-03 are unblocked.** Their `files_modified` lists do not overlap with this plan's, so they can run in parallel as Wave 1. Plan 03-04 then runs as Wave 2 and exercises the full Phase 3 flow.
</success_criteria>

<output>
After completion, create `.planning/phases/03-remaining-four-doc-types/03-01-SUMMARY.md` documenting:
- The Task 1 spike outcome (PASS/FAIL with browser+step detail) and its propagation to presentation.html's header comment.
- The exact CSS shape that landed in presentation.html (full scroll-snap vs `:target`-only fallback).
- The four edits applied to SKILL.md and the resulting line count (vs the 200-line cap).
- The five new normalized type values introduced by Step 2 → Step 3 substitution (`handbook`, `pitch`, `technical-brief`, `presentation`, `meeting-prep`) — kebab-cased filenames that plan 03-02 must match.
- The presentation-specific slot vocabulary added to Step 6 (`SLIDE NAV ITEMS SLOT`, `SLIDE 1 H1 SLOT`, `SLIDE 1 BODY SLOT`, `TOTAL_SLIDES_LITERAL`) — input to plan 03-04's Presentation fixture.
- The threat-model carry-over: snap container must stay `<main>` (not `<html>`); future maintainers must re-spike if changing the container scope.
- Dependencies handed off to plan 03-02: filename convention (kebab-case) and the schema-lineage comment recommendation (RESEARCH §"Open Questions OQ-4").
- Dependencies handed off to plan 03-03: the wildcard glob over `formats/*.html` will pick up presentation.html's classes (`.slide`, `.slide-counter`, `.slide-nav`) automatically — no hand-maintained allowlist edit needed.
- What plan 03-04's Presentation fixture must verify in Chrome AND Safari (the 7-step spike sequence on a REAL generated output, not the spike stub).
</output>
</content>
