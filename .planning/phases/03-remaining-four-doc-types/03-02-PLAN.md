---
phase: 03-remaining-four-doc-types
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/interview/pitch.md
  - skill/interview/technical-brief.md
  - skill/interview/presentation.md
  - skill/interview/meeting-prep.md
autonomous: true
requirements:
  - DOC-01
  - DOC-03
  - DOC-04
  - DOC-05
  - DOC-06
user_setup: []

must_haves:
  truths:
    - "All four new interview files exist at the kebab-cased paths SKILL.md (after plan 03-01) routes to: skill/interview/pitch.md, skill/interview/technical-brief.md, skill/interview/presentation.md, skill/interview/meeting-prep.md."
    - "Every file follows the DOC-06 schema verbatim: opening prose explaining the schema lineage to handbook.md, a `## The N questions` heading (numbered list, ≤5 entries), a `## Hand-off` section that points Claude at `${CLAUDE_SKILL_DIR}/story-arc.md`, and a `## What this interview must NOT do` section."
    - "Every file caps questions at ≤5 (DOC-07 carry-over). Pitch ships 5 questions per D3-12 recommendation; the other three ship 5 as well — none exceeds the cap."
    - "Every question follows the same shape as handbook.md: bold label + question prose, optional one-line context, an italic / parenthesized `Default if blank:` clause that documents the empty-answer behavior (D3-13)."
    - "Every file's tone-default text matches the per-type calibration in RESEARCH §Pattern 8: handbook + technical-brief + meeting-prep use the verbatim CLAUDE.md phrase (`\"Handbook, not pitch. Describe what IS.\"`); pitch and presentation diverge in BODY tone but keep handbook tone in TITLES."
    - "Every file contains an inline schema-lineage comment in the opening prose (per RESEARCH §\"Open Questions → OQ-4\" recommendation): a sentence stating the file mirrors the audience → material → sections → tone → handoff schema from `interview/handbook.md`."
    - "Each file's questions are TYPE-DISTINCTIVE per Pitfall 19 — pitch asks `the ask` and `the problem`; technical-brief asks `alternatives considered` and `trade-offs`; presentation asks `the takeaway` and `slide outline`; meeting-prep asks `meeting purpose` and `talking points` plus `open questions / risks`. No two files ask the same five questions with cosmetic rewording."
    - "Every file mentions zero of the OTHER doc types (each interview file is single-purpose; it does not enumerate or compare with other types — those are SKILL.md's job)."
    - "Every file is ≤80 lines (matches handbook.md's lean shape — DOC-07 produces lean files; padding violates the moat)."
  artifacts:
    - path: "skill/interview/pitch.md"
      provides: "Pitch-tailored interview — 5 questions following the DOC-06 schema. Sequences audience → the ask → the problem → solution → tone-defaults handoff. Body tone may run sales-y; section TITLES inherit handbook tone."
      contains: "The ask"
    - path: "skill/interview/technical-brief.md"
      provides: "Technical-brief-tailored interview — 5 questions. Sequences audience → decision → alternatives → trade-offs → tone-defaults. Engineers read for facts; tone-default is verbatim CLAUDE.md handbook phrase."
      contains: "Alternatives considered"
    - path: "skill/interview/presentation.md"
      provides: "Presentation-tailored interview — 5 questions. Sequences audience → takeaway → slide outline → tone (energetic body; handbook titles) → inclusions. Forces format=presentation regardless of section count (D3-01)."
      contains: "The takeaway"
    - path: "skill/interview/meeting-prep.md"
      provides: "Meeting-prep-tailored interview — 5 questions. Sequences meeting purpose → audience → talking points → open questions/risks → tone-defaults. Briefings deliver facts; tone-default is verbatim CLAUDE.md handbook phrase."
      contains: "Open questions"
  key_links:
    - from: "skill/interview/{pitch,technical-brief,presentation,meeting-prep}.md"
      to: "skill/SKILL.md (Step 3)"
      via: "Step 3 reads `${CLAUDE_SKILL_DIR}/interview/${type}.md` after plan 03-01's edit; the four new files complete the route map"
      pattern: "Read.*\\$\\{CLAUDE_SKILL_DIR\\}/story-arc\\.md"
    - from: "skill/interview/*.md (all four new files)"
      to: "skill/interview/handbook.md"
      via: "Schema lineage — every file's opening prose references handbook.md as the schema source of truth (DOC-06 mandate)"
      pattern: "handbook\\.md|DOC-06"
    - from: "skill/interview/{pitch,presentation}.md"
      to: "/Users/sperezasis/CLAUDE.md (Section Writing Rules)"
      via: "Tone defaults diverge from handbook in BODY only; titles inherit handbook tone — verbatim CLAUDE.md guidance"
      pattern: "section TITLES|describes what IS|titles still describe"
---

<objective>
Ship the four type-tailored interview files Phase 3 needs to make the doc-type branch in SKILL.md (plan 03-01 Step 2) actually do something. Closes DOC-01 (pitch interview), DOC-03 (technical-brief interview), DOC-04 (presentation interview, partially — the format skeleton is owned by plan 03-01), DOC-05 (meeting-prep interview), and DOC-06 (uniform schema across all 5 interview files — the existing handbook.md plus these four).

Plan 03-01 wires SKILL.md Step 3 to read `interview/${type}.md`. Without these four files, plan 03-01's flow chart leads to "file not found" mid-conversation. Without plan 03-01's variable substitution, these four files are unreachable. The two plans pair as Wave 1 — they do not share files, so they ship in parallel — but the runtime contract only holds when BOTH have landed.

Four tasks, one per file. Every file is mechanical: copy handbook.md's STRUCTURE (the same six headings: title, opening prose, `## The N questions`, `## Hand-off`, `## What this interview must NOT do`); fill the QUESTIONS per the type-tailored question order in CONTEXT.md D3-12; calibrate tone defaults per RESEARCH §Pattern 8; ensure each file is genuinely DISTINCTIVE (Pitfall 19 — type-labeled clones are the biggest Phase-3 risk).

Purpose: the interview is where the type-tailoring happens. SKILL.md is generic flow control; story-arc.md is universal; format selection is mechanical. The interview is the only piece of the skill that asks the user different questions per type. If pitch.md and meeting-prep.md ask "who's the audience? what's the material? what sections? tone? inclusions?" with three-word reformulations, plan 03-04's visual-gate fixture catches it (D3-22 sequential read), but the fix is here, not there.

Output: four new files at `skill/interview/{pitch,technical-brief,presentation,meeting-prep}.md`. Each ≤80 lines, each ≤5 questions, each genuinely type-distinctive. After this plan + plan 03-01 land in Wave 1, plan 03-04 (Wave 2) can run all four fixtures end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-remaining-four-doc-types/03-CONTEXT.md
@.planning/phases/03-remaining-four-doc-types/03-RESEARCH.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-SUMMARY.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-02-SUMMARY.md
@skill/SKILL.md
@skill/interview/handbook.md
@skill/story-arc.md
@/Users/sperezasis/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write skill/interview/pitch.md (5 questions, Overview-format expected, body-tone divergence allowed)</name>
  <files>skill/interview/pitch.md</files>
  <read_first>
    - skill/interview/handbook.md (the schema reference — same shape, different questions; do NOT cp + edit, AUTHOR from D3-12 to avoid Pitfall 19)
    - .planning/phases/03-remaining-four-doc-types/03-CONTEXT.md §"D3-10, D3-11, D3-12, D3-13" (schema, cap, type-tailored question order, empty-answer policy)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Pattern 8: Interview file template (D3-10, D3-12)" (the recommended template + per-type tone-default table)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Pitfall 19: Type-labeled clones" (the Phase-3 mitigation requirement)
    - /Users/sperezasis/CLAUDE.md §"Documentation Methodology — Story First" + §"Section Writing Rules" (the source of truth for tone calibration; pitch's body-tone divergence still keeps title-tone in handbook mode)
  </read_first>
  <action>
    Create `skill/interview/pitch.md`. Use handbook.md as the SHAPE reference, not the content reference. The five questions are sequenced to elicit pitch-distinctive material (the ask, the problem, the solution) — questions a handbook interview does NOT ask.

    Use this exact content:

    ```markdown
    # Pitch interview

    SKILL.md reads this file when the user picks `pitch` as the document type.
    Ask the questions one at a time, in order. Wait for each answer before asking
    the next. Empty answers are accepted — proceed with sensible defaults. Do
    not enforce length, do not retry, do not validate. The arc-gate is where
    quality is enforced, not the interview.

    The schema follows DOC-06's mandate (audience → material → section conventions
    → tone notes → handoff to story-arc) — same shape as `interview/handbook.md`.
    Pitch is structured as problem → solution → ask, and typically renders as a
    1-3 section Overview (the format auto-selects in SKILL.md Step 5b).

    ## The 5 questions

    1. **Audience.** Who is hearing this pitch? One line is plenty.
       (Default if blank: "A small-team decision-maker who hasn't seen this idea yet.")

    2. **The ask.** What specific outcome do you want from this audience? One sentence.
       (Default if blank: "An explicit yes to building / buying / approving the proposal as described.")

    3. **The problem.** In 1-2 sentences, what is broken without your solution?
       (Default if blank: "The audience has the problem already — they may not have named it. Claude proposes a 1-sentence framing from the ask.")

    4. **Your solution.** In 1-2 sentences, what you are offering, in plain terms.
       (Default if blank: ask Claude to derive from the audience answer + the ask. Do not block.)

    5. **Inclusions / exclusions.** Anything to definitely include or definitely avoid?
       (Default if blank: skip — no inclusions, no exclusions. Tone-default applies: section TITLES describe what IS (handbook tone); the BODY may run direct and confident — selling is OK in body prose, never in titles.)

    ## Hand-off

    After question 5 (or earlier if the user says "go ahead, propose"), do NOT
    write HTML. Read `${CLAUDE_SKILL_DIR}/story-arc.md` and follow it end-to-end.
    The story-arc gate decides when HTML is allowed. The arc gate's title-tone
    self-review applies even when this interview's body-tone default is more
    energetic — TITLES always describe what IS, regardless of doc type.

    ## What this interview must NOT do

    - Do not validate answers ("that's too short", "please clarify"). Empty
      answers proceed with the documented defaults.
    - Do not loop on a question. One ask, one answer, move on.
    - Do not re-ask the document type. SKILL.md handled that — by the time
      Claude is reading this file, pitch is already locked.
    - Do not paraphrase the questions. The wording is the contract.
    - Do not mention any other doc type elsewhere. Those are stubbed in SKILL.md.
    ```

    The file MUST be ≤80 lines. The five questions are deliberately type-distinctive: `the ask`, `the problem`, `your solution` are pitch-specific frames that a handbook interview does not pose. Pitfall 19 mitigation: this file's questions diverge from handbook.md's by ≥40% of question content (audience is shared; the other four are pitch-specific).
  </action>
  <behavior>
    - File `skill/interview/pitch.md` exists.
    - File contains exactly 5 numbered questions (`^1\. ` through `^5\. ` at line starts) — DOC-07 cap respected.
    - File contains the type-distinctive labels: `**Audience.**`, `**The ask.**`, `**The problem.**`, `**Your solution.**`, `**Inclusions / exclusions.**`.
    - File contains the schema-lineage line referencing `interview/handbook.md` and `DOC-06`.
    - File mentions section TITLES describe what IS (handbook tone in titles; body tone may diverge — RESEARCH §Pattern 8 pitch row).
    - File contains a `## Hand-off` section pointing at `${CLAUDE_SKILL_DIR}/story-arc.md`.
    - File contains a `## What this interview must NOT do` section.
    - File mentions zero of the four other doc types (handbook, technical brief, presentation, meeting prep) by name in question text — single-purpose interview file.
    - File length ≤80 lines.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s skill/interview/pitch.md &&
      [ "$(wc -l < skill/interview/pitch.md)" -le 80 ] &&
      [ "$(command grep -cE '^[1-5]\. ' skill/interview/pitch.md)" = "5" ] &&
      command grep -qF '**Audience.**' skill/interview/pitch.md &&
      command grep -qF '**The ask.**' skill/interview/pitch.md &&
      command grep -qF '**The problem.**' skill/interview/pitch.md &&
      command grep -qF '**Your solution.**' skill/interview/pitch.md &&
      command grep -qF '**Inclusions / exclusions.**' skill/interview/pitch.md &&
      command grep -qF 'interview/handbook.md' skill/interview/pitch.md &&
      command grep -qF 'DOC-06' skill/interview/pitch.md &&
      command grep -qiE 'TITLES.*describe what IS|titles.*handbook tone|titles still describe' skill/interview/pitch.md &&
      command grep -qF '## Hand-off' skill/interview/pitch.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/story-arc.md' skill/interview/pitch.md &&
      command grep -qF '## What this interview must NOT do' skill/interview/pitch.md &&
      ! command grep -qE 'technical brief|technical-brief|meeting prep|meeting-prep' skill/interview/pitch.md &&
      ! command grep -qE '^[6-9]\. |^[1-9][0-9]+\. ' skill/interview/pitch.md &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `skill/interview/pitch.md` exists, ≤80 lines.
    - Exactly 5 numbered questions in this order: Audience → The ask → The problem → Your solution → Inclusions / exclusions.
    - Each question has a `Default if blank:` clause documenting the empty-answer behavior (D3-13).
    - Schema-lineage comment present: references `interview/handbook.md` and `DOC-06` in opening prose.
    - Tone-default text states section TITLES inherit handbook tone (describe what IS); BODY tone may diverge (RESEARCH §Pattern 8 pitch divergence).
    - `## Hand-off` section points at `${CLAUDE_SKILL_DIR}/story-arc.md`.
    - `## What this interview must NOT do` section listing 5 prohibitions matches handbook.md's structure.
    - Zero references to handbook, technical brief, presentation, or meeting prep by name in question text (file is single-purpose).
    - No question numbered 6+ (DOC-07 cap).
  </acceptance_criteria>
  <done>
    `skill/interview/pitch.md` exists with 5 type-distinctive questions, the DOC-06 schema preserved verbatim from handbook.md's structure (six section headings), the tone-default calibrated per RESEARCH §Pattern 8 (titles handbook, body sales-OK), and the schema-lineage comment in the opening prose. The 16 enumerated automated checks pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write skill/interview/technical-brief.md (5 questions, Handbook-format expected, verbatim CLAUDE.md tone)</name>
  <files>skill/interview/technical-brief.md</files>
  <read_first>
    - skill/interview/handbook.md (schema reference)
    - .planning/phases/03-remaining-four-doc-types/03-CONTEXT.md §"D3-12 Technical brief block" (the recommended question order)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Pattern 8" (technical-brief uses the verbatim CLAUDE.md tone phrase — engineers read for facts)
    - /Users/sperezasis/CLAUDE.md §"Section Writing Rules" (handbook tone — every section title describes what IS)
  </read_first>
  <action>
    Create `skill/interview/technical-brief.md`. Engineering audience, decision write-up shape — alternatives + trade-offs are the type-distinctive material.

    Use this exact content:

    ```markdown
    # Technical brief interview

    SKILL.md reads this file when the user picks `technical brief` as the document type.
    Ask the questions one at a time, in order. Wait for each answer before asking
    the next. Empty answers are accepted — proceed with sensible defaults. Do
    not enforce length, do not retry, do not validate. The arc-gate is where
    quality is enforced, not the interview.

    The schema follows DOC-06's mandate (audience → material → section conventions
    → tone notes → handoff to story-arc) — same shape as `interview/handbook.md`.
    Technical briefs are decision write-ups for engineers and typically render as
    a 4+ section Handbook (the format auto-selects in SKILL.md Step 5b).

    ## The 5 questions

    1. **Audience.** Which engineers will read this, and what context do they have? One or two lines.
       (Default if blank: "Engineers on the same team, familiar with the codebase but not with this specific decision.")

    2. **The decision.** What was decided, in one sentence?
       (Default if blank: ask Claude to summarize from the H1 the user implies. Do not block.)

    3. **Alternatives considered.** Free list — at least 2 options that were on the table and rejected.
       (Default if blank: "Claude proposes 2-3 plausible alternatives derived from the decision context. Mark them as `[derived]` so the user can correct.")

    4. **Trade-offs that drove the choice.** Free text — what made the decided option win?
       (Default if blank: ask Claude to derive from the alternatives. The trade-offs section is the heart of a tech brief; do not skip it silently — flag with `[derived]`.)

    5. **Inclusions / exclusions.** Anything to definitely include or definitely avoid?
       (Default if blank: skip — no inclusions, no exclusions. Tone-default applies: "Handbook, not pitch. Describe what IS." — verbatim from CLAUDE.md. Engineers read for facts, not enthusiasm; selling is wrong here.)

    ## Hand-off

    After question 5 (or earlier if the user says "go ahead, propose"), do NOT
    write HTML. Read `${CLAUDE_SKILL_DIR}/story-arc.md` and follow it end-to-end.
    The story-arc gate decides when HTML is allowed.

    ## What this interview must NOT do

    - Do not validate answers ("that's too short", "please clarify"). Empty
      answers proceed with the documented defaults.
    - Do not loop on a question. One ask, one answer, move on.
    - Do not re-ask the document type. SKILL.md handled that — by the time
      Claude is reading this file, technical brief is already locked.
    - Do not paraphrase the questions. The wording is the contract.
    - Do not mention any other doc type elsewhere. Those are stubbed in SKILL.md.
    ```

    The file MUST be ≤80 lines. Pitfall 19 mitigation: questions 2-4 (decision / alternatives / trade-offs) are tech-brief-specific. The `[derived]` annotation pattern is unique to this file and acknowledges that engineers want explicit signals for "I made this up" vs "user told me." Verbatim CLAUDE.md tone phrase preserved (D2-14 / D3-12 verbatim discipline).
  </action>
  <behavior>
    - File `skill/interview/technical-brief.md` exists.
    - File contains exactly 5 numbered questions.
    - File contains the type-distinctive labels: `**Audience.**`, `**The decision.**`, `**Alternatives considered.**`, `**Trade-offs that drove the choice.**`, `**Inclusions / exclusions.**`.
    - File contains the verbatim CLAUDE.md tone phrase: `"Handbook, not pitch. Describe what IS."`.
    - File contains the schema-lineage line referencing `interview/handbook.md` and `DOC-06`.
    - File contains the `[derived]` annotation pattern (tech-brief-specific signal for Claude-derived content).
    - File contains a `## Hand-off` section pointing at `${CLAUDE_SKILL_DIR}/story-arc.md`.
    - File contains a `## What this interview must NOT do` section.
    - File mentions zero of the four other doc types by name.
    - File length ≤80 lines.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s skill/interview/technical-brief.md &&
      [ "$(wc -l < skill/interview/technical-brief.md)" -le 80 ] &&
      [ "$(command grep -cE '^[1-5]\. ' skill/interview/technical-brief.md)" = "5" ] &&
      command grep -qF '**Audience.**' skill/interview/technical-brief.md &&
      command grep -qF '**The decision.**' skill/interview/technical-brief.md &&
      command grep -qF '**Alternatives considered.**' skill/interview/technical-brief.md &&
      command grep -qF '**Trade-offs that drove the choice.**' skill/interview/technical-brief.md &&
      command grep -qF '**Inclusions / exclusions.**' skill/interview/technical-brief.md &&
      command grep -qF 'Handbook, not pitch. Describe what IS.' skill/interview/technical-brief.md &&
      command grep -qF 'verbatim from CLAUDE.md' skill/interview/technical-brief.md &&
      command grep -qF 'interview/handbook.md' skill/interview/technical-brief.md &&
      command grep -qF 'DOC-06' skill/interview/technical-brief.md &&
      command grep -qF '[derived]' skill/interview/technical-brief.md &&
      command grep -qF '## Hand-off' skill/interview/technical-brief.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/story-arc.md' skill/interview/technical-brief.md &&
      command grep -qF '## What this interview must NOT do' skill/interview/technical-brief.md &&
      ! command grep -qiE '^.*\bpitch\b|^.*\bpresentation\b|^.*\bmeeting prep\b|^.*\bmeeting-prep\b' skill/interview/technical-brief.md &&
      ! command grep -qE '^[6-9]\. |^[1-9][0-9]+\. ' skill/interview/technical-brief.md &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `skill/interview/technical-brief.md` exists, ≤80 lines.
    - Exactly 5 numbered questions in this order: Audience → The decision → Alternatives considered → Trade-offs → Inclusions / exclusions.
    - Each question has a `Default if blank:` clause.
    - Verbatim CLAUDE.md tone phrase present (`"Handbook, not pitch. Describe what IS."`).
    - Schema-lineage comment references `interview/handbook.md` and `DOC-06`.
    - `[derived]` annotation pattern present in the alternatives / trade-offs default text (tech-brief-specific signal).
    - `## Hand-off` section points at `${CLAUDE_SKILL_DIR}/story-arc.md`.
    - `## What this interview must NOT do` section present.
    - Zero references to pitch, presentation, or meeting-prep by name (single-purpose).
    - No question numbered 6+.
  </acceptance_criteria>
  <done>
    `skill/interview/technical-brief.md` exists with 5 tech-brief-distinctive questions (decision / alternatives / trade-offs are the type-specific material), the verbatim CLAUDE.md tone phrase, the `[derived]` annotation pattern unique to tech briefs, and the DOC-06 schema preserved. The 17 enumerated automated checks pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write skill/interview/presentation.md (5 questions, Presentation format always, energetic body / handbook titles)</name>
  <files>skill/interview/presentation.md</files>
  <read_first>
    - skill/interview/handbook.md (schema reference)
    - .planning/phases/03-remaining-four-doc-types/03-CONTEXT.md §"D3-12 Presentation block" (recommended question order)
    - .planning/phases/03-remaining-four-doc-types/03-CONTEXT.md §"D3-01" (presentation type forces format=presentation regardless of section count)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Pattern 8" (presentation tone-default — handbook in titles, more energetic in body)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Open Questions → OQ-3" (Presentation fixture H1 wording — `Phase 3 status update — deshtml`)
    - /Users/sperezasis/CLAUDE.md §"Section Writing Rules"
  </read_first>
  <action>
    Create `skill/interview/presentation.md`. Slide-deck audience focus — the takeaway and slide outline are the type-distinctive material. Note the file flags that format=presentation is forced (D3-01) — the user does NOT need to size the section count to land Presentation; it lands automatically because the type is presentation.

    Use this exact content:

    ```markdown
    # Presentation interview

    SKILL.md reads this file when the user picks `presentation` as the document type.
    Ask the questions one at a time, in order. Wait for each answer before asking
    the next. Empty answers are accepted — proceed with sensible defaults. Do
    not enforce length, do not retry, do not validate. The arc-gate is where
    quality is enforced, not the interview.

    The schema follows DOC-06's mandate (audience → material → section conventions
    → tone notes → handoff to story-arc) — same shape as `interview/handbook.md`.
    Presentations always render in the Presentation format (full-viewport slides,
    scroll-snap, anchor nav) regardless of section count — SKILL.md Step 5b
    short-circuits to format=presentation when type=presentation (D3-01).

    ## The 5 questions

    1. **Audience.** Who is watching this deck? One line.
       (Default if blank: "A small group seeing the deck live, with the presenter narrating.")

    2. **The takeaway.** In one sentence, what should the audience leave with?
       (Default if blank: ask Claude to derive from the H1 + audience. The takeaway is the takeaway slide; do not skip it silently — flag with `[derived]`.)

    3. **Slide outline.** Free list of slide titles in order, or "let Claude propose."
       (Default if blank: "Claude proposes." Claude proposes 4-7 slides sized for live presentation — opening, context, mechanism, evidence, takeaway. Each slide is one beat.)

    4. **Tone.** Anything specific about voice or register for the slide body?
       (Default if blank: "Handbook tone in TITLES — describe what IS, never sell. Body may run more energetic — slides reward visual punch — but never pitch-y. Direct nouns, short clauses." Title-tone discipline always wins; the story-arc self-review pass enforces this on every section title regardless of doc type.)

    5. **Inclusions / exclusions.** Anything to definitely include or definitely avoid?
       (Default if blank: skip — no inclusions, no exclusions.)

    ## Hand-off

    After question 5 (or earlier if the user says "go ahead, propose"), do NOT
    write HTML. Read `${CLAUDE_SKILL_DIR}/story-arc.md` and follow it end-to-end.
    The story-arc gate decides when HTML is allowed. Each arc row corresponds to
    one slide; the slide count is the arc row count. The slide counter is wired
    automatically by the format skeleton (CSS `counter-increment`).

    ## What this interview must NOT do

    - Do not validate answers ("that's too short", "please clarify"). Empty
      answers proceed with the documented defaults.
    - Do not loop on a question. One ask, one answer, move on.
    - Do not re-ask the document type. SKILL.md handled that — by the time
      Claude is reading this file, presentation is already locked.
    - Do not paraphrase the questions. The wording is the contract.
    - Do not mention any other doc type elsewhere. Those are stubbed in SKILL.md.
    - Do not propose more than 7 slides on the empty-default path. Live decks
      with 8+ slides typically lose the audience; 4-7 is the operating range.
    ```

    The file MUST be ≤80 lines. Pitfall 19 mitigation: questions 2 (takeaway) and 3 (slide outline) are presentation-specific. The "each arc row = one slide" mapping in the Hand-off section is the bridge between the universal arc gate and the presentation-specific slide counter — this is something the other three new files don't say because they don't have slides. Tone calibration per RESEARCH §Pattern 8 (titles handbook; body more energetic).
  </action>
  <behavior>
    - File `skill/interview/presentation.md` exists.
    - File contains exactly 5 numbered questions.
    - File contains the type-distinctive labels: `**Audience.**`, `**The takeaway.**`, `**Slide outline.**`, `**Tone.**`, `**Inclusions / exclusions.**`.
    - File explicitly states format=presentation is forced regardless of section count (D3-01) — references SKILL.md Step 5b.
    - File mentions the slide-count → arc-row mapping in the Hand-off section.
    - File mentions the 4-7 slide range in the empty-default path (matches RESEARCH §Pattern 6 calibration; live-deck UX guidance).
    - File contains the schema-lineage line referencing `interview/handbook.md` and `DOC-06`.
    - File contains a `## Hand-off` section pointing at `${CLAUDE_SKILL_DIR}/story-arc.md`.
    - File contains a `## What this interview must NOT do` section with 6 prohibitions (one extra: max 7 slides on empty-default).
    - File mentions zero of the four other doc types by name.
    - File length ≤80 lines.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s skill/interview/presentation.md &&
      [ "$(wc -l < skill/interview/presentation.md)" -le 80 ] &&
      [ "$(command grep -cE '^[1-5]\. ' skill/interview/presentation.md)" = "5" ] &&
      command grep -qF '**Audience.**' skill/interview/presentation.md &&
      command grep -qF '**The takeaway.**' skill/interview/presentation.md &&
      command grep -qF '**Slide outline.**' skill/interview/presentation.md &&
      command grep -qF '**Tone.**' skill/interview/presentation.md &&
      command grep -qF '**Inclusions / exclusions.**' skill/interview/presentation.md &&
      command grep -qF 'D3-01' skill/interview/presentation.md &&
      command grep -qF 'interview/handbook.md' skill/interview/presentation.md &&
      command grep -qF 'DOC-06' skill/interview/presentation.md &&
      command grep -qiE 'TITLES.*describe what IS|titles.*describe|handbook tone in TITLES' skill/interview/presentation.md &&
      command grep -qiE '4-7 slides|4 to 7|four to seven|4..*7' skill/interview/presentation.md &&
      command grep -qF '## Hand-off' skill/interview/presentation.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/story-arc.md' skill/interview/presentation.md &&
      command grep -qF '## What this interview must NOT do' skill/interview/presentation.md &&
      ! command grep -qiE '\bpitch\b|\btechnical brief\b|\btechnical-brief\b|\bmeeting prep\b|\bmeeting-prep\b' skill/interview/presentation.md &&
      ! command grep -qE '^[6-9]\. |^[1-9][0-9]+\. ' skill/interview/presentation.md &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `skill/interview/presentation.md` exists, ≤80 lines.
    - Exactly 5 numbered questions in this order: Audience → The takeaway → Slide outline → Tone → Inclusions / exclusions.
    - Each question has a `Default if blank:` clause.
    - File references D3-01 and explicitly states format=presentation is forced regardless of section count.
    - File documents the arc-row → slide mapping in the Hand-off section.
    - Empty-default path mentions the 4-7 slide operating range.
    - Tone-default text states TITLES inherit handbook tone; BODY may run more energetic (RESEARCH §Pattern 8 presentation row).
    - `## Hand-off` section points at `${CLAUDE_SKILL_DIR}/story-arc.md`.
    - `## What this interview must NOT do` section present (6 prohibitions vs the standard 5 — extra: max 7 slides).
    - Zero references to pitch, technical brief, or meeting-prep by name.
    - No question numbered 6+.
  </acceptance_criteria>
  <done>
    `skill/interview/presentation.md` exists with 5 presentation-distinctive questions (takeaway and slide outline are the type-specific material), the format=presentation forced-selection note (D3-01), the arc-row → slide-count bridge in the Hand-off, and the title-handbook-body-energetic tone calibration per RESEARCH §Pattern 8. The 18 enumerated automated checks pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write skill/interview/meeting-prep.md (5 questions, Overview-format expected, briefing tone)</name>
  <files>skill/interview/meeting-prep.md</files>
  <read_first>
    - skill/interview/handbook.md (schema reference)
    - .planning/phases/03-remaining-four-doc-types/03-CONTEXT.md §"D3-12 Meeting prep block" (recommended question order)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Pattern 8" (meeting-prep uses verbatim CLAUDE.md tone phrase)
    - /Users/sperezasis/CLAUDE.md §"Section Writing Rules"
  </read_first>
  <action>
    Create `skill/interview/meeting-prep.md`. Briefing-doc shape — meeting purpose, talking points, open questions/risks are the type-distinctive material. Three sections is the typical render shape (context / what to cover / what to watch for); Overview format auto-selects.

    Use this exact content:

    ```markdown
    # Meeting prep interview

    SKILL.md reads this file when the user picks `meeting prep` as the document type.
    Ask the questions one at a time, in order. Wait for each answer before asking
    the next. Empty answers are accepted — proceed with sensible defaults. Do
    not enforce length, do not retry, do not validate. The arc-gate is where
    quality is enforced, not the interview.

    The schema follows DOC-06's mandate (audience → material → section conventions
    → tone notes → handoff to story-arc) — same shape as `interview/handbook.md`.
    Meeting prep is a briefing doc and typically renders as a 1-3 section
    Overview (the format auto-selects in SKILL.md Step 5b).

    ## The 5 questions

    1. **Meeting purpose.** In one sentence, what is this meeting deciding or accomplishing?
       (Default if blank: ask Claude to derive from the audience answer + the H1. Do not block — but flag with `[derived]`.)

    2. **Audience.** Who is in the room? One or two lines.
       (Default if blank: "Internal team plus one external stakeholder, mid-tenure context.")

    3. **Talking points.** Free list — what you need to cover. Order matters.
       (Default if blank: "Claude proposes 3-5 talking points derived from the meeting purpose. Mark them as `[derived]`.")

    4. **Open questions / risks.** Free text — what is unresolved, what could go sideways?
       (Default if blank: skip — leave the open-questions section empty rather than fabricating risks. Empty is honest; fabricated risks waste meeting time.)

    5. **Inclusions / exclusions.** Anything to definitely include or definitely avoid?
       (Default if blank: skip — no inclusions, no exclusions. Tone-default applies: "Handbook, not pitch. Describe what IS." — verbatim from CLAUDE.md. Briefings deliver facts; selling is wrong here.)

    ## Hand-off

    After question 5 (or earlier if the user says "go ahead, propose"), do NOT
    write HTML. Read `${CLAUDE_SKILL_DIR}/story-arc.md` and follow it end-to-end.
    The story-arc gate decides when HTML is allowed.

    ## What this interview must NOT do

    - Do not validate answers ("that's too short", "please clarify"). Empty
      answers proceed with the documented defaults.
    - Do not loop on a question. One ask, one answer, move on.
    - Do not re-ask the document type. SKILL.md handled that — by the time
      Claude is reading this file, meeting prep is already locked.
    - Do not paraphrase the questions. The wording is the contract.
    - Do not mention any other doc type elsewhere. Those are stubbed in SKILL.md.
    - Do not fabricate risks when question 4 is empty. An empty open-questions
      section is honest; manufactured risks waste the audience's time.
    ```

    The file MUST be ≤80 lines. Pitfall 19 mitigation: question 1 (meeting purpose, distinct from generic audience/material) and question 4 (open questions / risks — uniquely meeting-prep) anchor the type-distinctiveness. The "do not fabricate risks" prohibition is meeting-prep-specific (other interviews don't ask about risks). Verbatim CLAUDE.md tone phrase preserved.

    Also: the order of questions deliberately puts Meeting purpose FIRST (not Audience) because the meeting purpose anchors everything else. This is one of the few cases where the schema field "audience" doesn't lead — purpose leads, audience follows. This is allowed under DOC-06: the schema dictates that all five fields appear, not their internal ordering. Document this in the file's opening prose so the schema-lineage signal stays clear.
  </action>
  <behavior>
    - File `skill/interview/meeting-prep.md` exists.
    - File contains exactly 5 numbered questions.
    - File contains the type-distinctive labels: `**Meeting purpose.**`, `**Audience.**`, `**Talking points.**`, `**Open questions / risks.**`, `**Inclusions / exclusions.**`.
    - File contains the verbatim CLAUDE.md tone phrase: `"Handbook, not pitch. Describe what IS."`.
    - File contains the schema-lineage line referencing `interview/handbook.md` and `DOC-06`.
    - File contains the `[derived]` annotation pattern (matches technical-brief.md's signal — but anchored on different questions, meeting-purpose + talking-points instead of alternatives + trade-offs).
    - File contains the meeting-prep-specific prohibition "do not fabricate risks" in the must-NOT-do section (6 prohibitions vs the standard 5).
    - File contains a `## Hand-off` section pointing at `${CLAUDE_SKILL_DIR}/story-arc.md`.
    - File contains a `## What this interview must NOT do` section.
    - File mentions zero of the four other doc types by name.
    - File length ≤80 lines.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s skill/interview/meeting-prep.md &&
      [ "$(wc -l < skill/interview/meeting-prep.md)" -le 80 ] &&
      [ "$(command grep -cE '^[1-5]\. ' skill/interview/meeting-prep.md)" = "5" ] &&
      command grep -qF '**Meeting purpose.**' skill/interview/meeting-prep.md &&
      command grep -qF '**Audience.**' skill/interview/meeting-prep.md &&
      command grep -qF '**Talking points.**' skill/interview/meeting-prep.md &&
      command grep -qF '**Open questions / risks.**' skill/interview/meeting-prep.md &&
      command grep -qF '**Inclusions / exclusions.**' skill/interview/meeting-prep.md &&
      command grep -qF 'Handbook, not pitch. Describe what IS.' skill/interview/meeting-prep.md &&
      command grep -qF 'verbatim from CLAUDE.md' skill/interview/meeting-prep.md &&
      command grep -qF 'interview/handbook.md' skill/interview/meeting-prep.md &&
      command grep -qF 'DOC-06' skill/interview/meeting-prep.md &&
      command grep -qF '[derived]' skill/interview/meeting-prep.md &&
      command grep -qiE 'do not fabricate risks|empty.*honest|fabricat' skill/interview/meeting-prep.md &&
      command grep -qF '## Hand-off' skill/interview/meeting-prep.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/story-arc.md' skill/interview/meeting-prep.md &&
      command grep -qF '## What this interview must NOT do' skill/interview/meeting-prep.md &&
      ! command grep -qiE '\bpitch\b|\btechnical brief\b|\btechnical-brief\b|\bpresentation\b' skill/interview/meeting-prep.md &&
      ! command grep -qE '^[6-9]\. |^[1-9][0-9]+\. ' skill/interview/meeting-prep.md &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `skill/interview/meeting-prep.md` exists, ≤80 lines.
    - Exactly 5 numbered questions in this order: Meeting purpose → Audience → Talking points → Open questions / risks → Inclusions / exclusions.
    - Each question has a `Default if blank:` clause.
    - Verbatim CLAUDE.md tone phrase present.
    - Schema-lineage comment references `interview/handbook.md` and `DOC-06`.
    - `[derived]` annotation pattern present in question 1 / question 3 default text.
    - `## Hand-off` section points at `${CLAUDE_SKILL_DIR}/story-arc.md`.
    - `## What this interview must NOT do` section includes the meeting-prep-specific "do not fabricate risks" prohibition (6 prohibitions vs the standard 5).
    - Zero references to pitch, technical brief, or presentation by name.
    - No question numbered 6+.
  </acceptance_criteria>
  <done>
    `skill/interview/meeting-prep.md` exists with 5 meeting-prep-distinctive questions (meeting purpose / talking points / open questions are the type-specific material), the verbatim CLAUDE.md tone phrase, the `[derived]` annotation pattern, and the meeting-prep-specific "do not fabricate risks" prohibition. Question order intentionally leads with Meeting purpose (not Audience) because purpose anchors the rest of the briefing — the schema fields are all present; only their order varies. The 18 enumerated automated checks pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Interview file → SKILL.md Step 3 (variable load) | After plan 03-01's edit, Step 3 reads `interview/${type}.md`. A typo in the filename here (e.g., `technical_brief.md` instead of `technical-brief.md`) breaks the runtime contract mid-conversation. |
| Interview file → user (asking questions) | The user reads the questions verbatim. Bad wording (e.g., a leading question that biases the answer) shapes every output downstream. The wording IS the UX contract for this doc type. |
| Interview file → story-arc.md (hand-off) | All four files end by handing off to the universal arc gate. A broken hand-off (missing `${CLAUDE_SKILL_DIR}/story-arc.md` reference) would let the interview pretend to complete without the arc gate, breaking ARC-04 (mandatory approval). |
| handbook.md → all four new files (schema lineage) | Pitfall 20: schema drift. If a future plan modifies handbook.md's schema, the four new files don't auto-update. The schema-lineage comment per file is the only anchor pointing at the source of truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | A future plan adds a question to one interview file (e.g., pitch grows from 5 to 6 questions) without auditing whether the question is genuinely necessary | mitigate | DOC-07 caps questions at ≤5. Plan 03-03 ships an optional schema-drift check (RESEARCH §Pattern 9 / OQ-1) that fails CI if any interview file exceeds 5 questions. Even without that check, the per-file verify block here catches `^[6-9]\. ` patterns. |
| T-03-11 | Tampering | A future "improvement" rewrites the questions in one file using copy-paste from another — Pitfall 19 type-labeled clones | mitigate | Each file's question labels are unique to its type (Pitch: `The ask`; Tech brief: `The decision`; Presentation: `The takeaway`; Meeting prep: `Meeting purpose`). The verify blocks check for these distinctive labels via `grep -qF`. Plan 03-04's sequential read (D3-22) is the empirical gate: if two outputs read the same, the questions converged silently. |
| T-03-12 | Spoofing | A user types `technical brief` (with space) at SKILL.md Step 2 and the file is at `technical-brief.md` (kebab) — load fails | mitigate | Plan 03-01's Step 2 normalization explicitly converts `technical brief` → `technical-brief` and `meeting prep` → `meeting-prep` before the Read in Step 3. The four new files are kebab-cased to match. The file paths in this plan's `files_modified` (kebab) match plan 03-01's normalization. |
| T-03-13 | Information Disclosure | An interview file leaks user-private context from a previous run via cached prompt history | accept | Each `/deshtml` invocation runs in a fresh Claude Code session per Phase 2 fixture-pattern documentation (FIXTURE-NOTES.md mentions "fresh Claude Code session"). No cross-session state. |
| T-03-14 | Tampering | The schema-lineage comment is dropped in a future revision — Pitfall 20 silent schema drift | mitigate | Each verify block in this plan greps for `interview/handbook.md` AND `DOC-06` references. Removing either fails plan acceptance. Plan 03-03's optional Rule 5 (schema-drift check) is the runtime safety net. |
| T-03-15 | Tampering | A future revision drops the verbatim CLAUDE.md tone phrase from technical-brief.md or meeting-prep.md, weakening Pitfall 4 (design-token-drift-equivalent for prose tone) | mitigate | The verify block greps `Handbook, not pitch. Describe what IS.` verbatim plus `verbatim from CLAUDE.md`. D2-14 / D3-12 verbatim discipline applies recursively. |
| T-03-16 | Tampering | An empty user answer in question 4 of meeting-prep.md silently fabricates risks anyway (the LLM does what the LLM does) | mitigate | The "do not fabricate risks" prohibition is in the `## What this interview must NOT do` section AND in the question's `Default if blank:` clause itself. Two-place statement reduces the silent-override risk. |
| T-03-17 | Denial of Service | Four files with overlapping content patterns inflate the audit's allowlist harvest cost | accept | Audit harvests classes from CSS / HTML / format skeletons, NOT from interview files. Interview files are markdown and never appear in the audit's input set. The allowlist cost is unaffected by this plan. |
| T-03-18 | Elevation of Privilege | n/a (markdown-only changes; no executable code) | n/a | All four files are pure markdown. No script invocation. The audit's Rule 3 enforces this on generated output. |
</threat_model>

<verification>
All four task `<verify>` blocks must pass. Specifically:

- Each file exists at the kebab-cased path SKILL.md Step 3 (after plan 03-01) routes to.
- Each file is ≤80 lines and contains exactly 5 numbered questions.
- Each file's question labels match the D3-12 question order (Pitch: Audience / The ask / The problem / Your solution / Inclusions; Tech brief: Audience / The decision / Alternatives / Trade-offs / Inclusions; Presentation: Audience / The takeaway / Slide outline / Tone / Inclusions; Meeting prep: Meeting purpose / Audience / Talking points / Open questions / Inclusions).
- Each file contains the schema-lineage comment referencing `interview/handbook.md` and `DOC-06`.
- Each file contains a `## Hand-off` section pointing at `${CLAUDE_SKILL_DIR}/story-arc.md`.
- Each file contains a `## What this interview must NOT do` section with type-appropriate prohibitions.
- Each file contains zero references to OTHER doc types by name in question text (pitch.md doesn't say `presentation`; technical-brief.md doesn't say `pitch`; etc.).
- Three files (handbook + tech-brief + meeting-prep) contain the verbatim CLAUDE.md tone phrase. Two files (pitch + presentation) document the title-handbook-body-energetic tone divergence.

End-to-end interview-to-arc verification (a real `/deshtml` invocation that picks pitch and walks through the 5 questions, then story-arc.md, then renders Overview HTML) is intentionally deferred to plan 03-04 (the per-type fixture plan).
</verification>

<success_criteria>
1. **Four interview files ship in the kebab-cased paths SKILL.md routes to.** `interview/pitch.md`, `interview/technical-brief.md`, `interview/presentation.md`, `interview/meeting-prep.md` — exactly the paths plan 03-01's `${type}.md` substitution produces after Step 2 normalization.
2. **DOC-06 schema preserved across all four files.** Every file follows audience / material / sections / tone / handoff. Pitfall 20 (schema drift) mitigated by the schema-lineage comment in each file's opening prose.
3. **DOC-07 question cap respected.** Every file ships ≤5 questions. None inflates.
4. **Type-distinctive questions per Pitfall 19.** Each file's questions are demonstrably different from the other three. The unique labels (`The ask`, `The decision`, `The takeaway`, `Meeting purpose`) are the surface signal; the substantive difference is what the empty-default clauses elicit (an ask, a decision, a takeaway, a meeting purpose — none derivable from the others).
5. **Tone calibration matches RESEARCH §Pattern 8.** Three files use the verbatim CLAUDE.md handbook phrase (handbook + technical-brief + meeting-prep — engineers and briefing audiences read for facts). Two files document the title-handbook-body-energetic divergence (pitch + presentation — sales-OK in body, never in titles).
6. **Hand-off contract is uniform.** All four files instruct Claude to read `${CLAUDE_SKILL_DIR}/story-arc.md` after the questions complete. The arc gate is universal — no per-type branching at this layer.
7. **Plan 03-04 is unblocked.** The four files exist; plan 03-04's per-type fixture runs can paste the `interview-answers.md` for each type and walk the running skill through to a generated HTML. Plan 03-03 (audit harvester) is also unblocked but does not depend on this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-remaining-four-doc-types/03-02-SUMMARY.md` documenting:
- The four files shipped, their line counts (each ≤80 per DOC-07's lean shape), and the schema-section count per file (six section headings each: title, opening prose, `## The N questions`, `## Hand-off`, `## What this interview must NOT do`, plus the per-question default clauses).
- The five DOC-06 schema fields present in each file (audience / material / sections / tone / handoff) and the per-file question order showing the type-distinctive choices.
- The Pitfall 19 mitigation: each file's distinctive question labels (Pitch: The ask / The problem / Your solution; Tech brief: The decision / Alternatives / Trade-offs; Presentation: The takeaway / Slide outline; Meeting prep: Meeting purpose / Talking points / Open questions).
- The tone-default calibration per RESEARCH §Pattern 8: 3 files use verbatim CLAUDE.md, 2 files document title-handbook-body-energetic.
- The `[derived]` annotation pattern (introduced in technical-brief, reused in meeting-prep): a tech-write-up convention for engineers + briefing readers — a shared signal that a value was inferred, not user-provided.
- The two type-specific extra prohibitions ("max 7 slides" in presentation.md; "do not fabricate risks" in meeting-prep.md) — files where the standard 5 prohibitions weren't sufficient.
- Dependencies handed off to plan 03-04: per-fixture interview-answers.md pastes (one set per type, 4-5 answers each). The fixture run pastes these to verify the questions surface different material per type (D3-22 sequential-read gate).
- Open question for plan 03-03: whether to ship the schema-drift check (RESEARCH §Pattern 9 / OQ-1) as Audit Rule 5 in this Phase 3 audit extension. Recommendation: SHIP — it's ~20 bash lines and locks D3-10's passive constraint into an active check, which closes Pitfall 20 mechanically.
</output>
</content>
