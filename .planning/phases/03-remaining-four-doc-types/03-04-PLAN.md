---
phase: 03-remaining-four-doc-types
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - .planning/phases/03-remaining-four-doc-types/fixture/pitch/interview-answers.md
  - .planning/phases/03-remaining-four-doc-types/fixture/pitch/expected-arc.md
  - .planning/phases/03-remaining-four-doc-types/fixture/technical-brief/interview-answers.md
  - .planning/phases/03-remaining-four-doc-types/fixture/technical-brief/expected-arc.md
  - .planning/phases/03-remaining-four-doc-types/fixture/presentation/interview-answers.md
  - .planning/phases/03-remaining-four-doc-types/fixture/presentation/expected-arc.md
  - .planning/phases/03-remaining-four-doc-types/fixture/meeting-prep/interview-answers.md
  - .planning/phases/03-remaining-four-doc-types/fixture/meeting-prep/expected-arc.md
  - .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md
autonomous: false
requirements:
  - DOC-01
  - DOC-03
  - DOC-04
  - DOC-05
  - DOC-06
  - DESIGN-04
user_setup: []

must_haves:
  truths:
    - "Four reproducible fixture-input bundles exist on disk, one per Phase-3 doc type — pitch / technical-brief / presentation / meeting-prep — each containing an interview-answers.md (canonical inputs) and an expected-arc.md (the shape the verifier compares the generated arc against)."
    - "The local skill install at ~/.claude/skills/deshtml/ has been refreshed via `cp -R skill/ ~/.claude/skills/deshtml/` so the running /deshtml command resolves to the Wave-1 outputs (presentation.html, four interview files, updated audit)."
    - "The four fixtures have been run end-to-end through the running skill: /deshtml → doc type → interview → arc → approve → render → audit → open. Each invocation produces a single self-contained YYYY-MM-DD-<slug>-<type>.html in the verifier's CWD."
    - "Format auto-selection (D3-01) lands the expected format per type: pitch → overview (1-3 sections); technical-brief → handbook (4+ sections); presentation → presentation (always); meeting-prep → overview."
    - "Each generated HTML passes the updated audit (skill/audit/run.sh) — 0 violations across hex / class allowlist / banned tags / leftover link / Rule 5 schema check."
    - "Visual diff PASSES against the expected reference for the three types that have references: pitch + meeting-prep vs bnp-overview.html (Overview format); technical-brief vs pm-system.html (Handbook format). Presentation passes the written rubric from RESEARCH §\"Visual Gate for Presentation\" — slide-shaped, full-viewport, palette match, scroll-snap working in Chrome AND Safari (or `:target` fallback per the spike outcome from plan 03-01)."
    - "Sequential-read check (D3-22): all four generated HTMLs have been read in order. Each reads as a distinct document — different question shape, different content density, different structural rhythm. None reads like a type-labeled clone of another (Pitfall 19 mitigation)."
    - "FIXTURE-NOTES.md records the empirical run for each of the four types: actual filename, audit exit code, retry count, format auto-selection result, sequential-read disposition, deviations / follow-ups, and a single human-verify sign-off (approved / approved with notes / failed: <type>: <reason>)."
  artifacts:
    - path: ".planning/phases/03-remaining-four-doc-types/fixture/pitch/interview-answers.md"
      provides: "Canonical inputs for the pitch fixture: 'Pitching deshtml to a small-team CTO' (audience: technical, ask: 'let me build this for our team', 3 sections per D3-20)."
      contains: "small-team CTO"
    - path: ".planning/phases/03-remaining-four-doc-types/fixture/pitch/expected-arc.md"
      provides: "Shape the verifier compares the proposed pitch arc against: 3 rows, problem → solution → ask narrative, expected format=overview."
      contains: "Format: overview"
    - path: ".planning/phases/03-remaining-four-doc-types/fixture/technical-brief/interview-answers.md"
      provides: "Canonical inputs for the technical-brief fixture: 'Why we chose curl-pipe-bash for distribution' (audience: Caseproof engineers, decision: pipe-bash over packaged installer, 5-6 sections per D3-20)."
      contains: "curl-pipe-bash"
    - path: ".planning/phases/03-remaining-four-doc-types/fixture/technical-brief/expected-arc.md"
      provides: "Shape the verifier compares the proposed technical-brief arc against: 5-6 rows, decision → alternatives → trade-offs narrative, expected format=handbook."
      contains: "Format: handbook"
    - path: ".planning/phases/03-remaining-four-doc-types/fixture/presentation/interview-answers.md"
      provides: "Canonical inputs for the presentation fixture: 'Phase 3 status update — deshtml' (audience: Caseproof team, 5 slides per D3-20 / RESEARCH OQ-3)."
      contains: "Phase 3 status update"
    - path: ".planning/phases/03-remaining-four-doc-types/fixture/presentation/expected-arc.md"
      provides: "Shape the verifier compares the proposed presentation arc against: 5 rows = 5 slides, expected format=presentation, Visual Gate rubric checklist embedded."
      contains: "Format: presentation"
    - path: ".planning/phases/03-remaining-four-doc-types/fixture/meeting-prep/interview-answers.md"
      provides: "Canonical inputs for the meeting-prep fixture: 'Demo run-through with Delfi' (audience: Delfi, 3 sections: context, demo flow, anticipated questions per D3-20)."
      contains: "Delfi"
    - path: ".planning/phases/03-remaining-four-doc-types/fixture/meeting-prep/expected-arc.md"
      provides: "Shape the verifier compares the proposed meeting-prep arc against: 3 rows, context → demo flow → questions, expected format=overview."
      contains: "Format: overview"
    - path: ".planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md"
      provides: "Empirical record of the four-fixture run: per-type filename, audit exit, format selected, sequential-read disposition, deviations, hand-offs to Phase 4. Single human-verify sign-off at the bottom (approved / notes / failed)."
      contains: "Sequential-read check"
  key_links:
    - from: "fixture/<type>/interview-answers.md"
      to: "skill/interview/<type>.md"
      via: "Verifier pastes these answers into the running skill at the per-type interview prompts"
      pattern: "interview-answers\\.md"
    - from: "fixture/<type>/expected-arc.md"
      to: "skill/story-arc.md + skill/SKILL.md (Step 5b)"
      via: "Verifier compares the proposed arc shape AND the printed `Format: <name>` line against expected-arc.md before approval"
      pattern: "Format: (handbook|overview|presentation)"
    - from: "fixture/FIXTURE-NOTES.md"
      to: "skill/audit/run.sh + .planning/research/PITFALLS.md (Pitfalls 19, 20, 21)"
      via: "Audit exit codes and Phase-3 pitfall observations are recorded per type"
      pattern: "audit exit|Pitfall"
---

<objective>
Run the four canonical Phase-3 fixtures end-to-end, prove the Phase-3 contract holds across all five doc types, and close the phase. Closes ROADMAP Phase 3 success criteria 1-4: every doc type has its own interview file (DOC-06 mechanical via plan 03-02 + Rule 5 from plan 03-03 — re-confirmed empirically here), format auto-selects per the D3-01 rule (DESIGN-04), Presentation renders as full-viewport slides with anchor nav and CSS-only counter (DOC-04 final acceptance), and none of the four reads like a type-labeled clone of another (D3-22 sequential read).

Plans 03-01, 03-02, 03-03 ship the parts. This plan stitches them together with the only thing Phase 3 leaves unverified: do the four flows actually produce Caseproof-faithful outputs?

Three tasks:

1. **Write the eight fixture-input files** (one `interview-answers.md` + one `expected-arc.md` per type) under `.planning/phases/03-remaining-four-doc-types/fixture/<type>/`. Each pair follows Phase 2's `02/fixture/` shape (D2-24 → D3-20 carry-over). Stage the local skill install via `cp -R` so `/deshtml` resolves to the Wave-1 outputs.

2. **Single human-verify checkpoint** at the end (`autonomous: false`). The verifier runs each of the four fixtures end-to-end (~15-20 minutes total — 3-5 min per fixture), reads all four outputs sequentially (D3-22 — Pitfall 19 mitigation), and replies with one of: `approved`, `approved with notes: <text>`, `failed: <type>: <reason>`.

3. **Write `fixture/FIXTURE-NOTES.md`** capturing the empirical record of all four runs in one document (not one notes file per type). Phase 4 maintainers read this to understand what "Phase 3 right" looked like — across all four types, in one place.

Per D3-21: ONE checkpoint at the end of the four-run sequence, NOT four separate checkpoints. The verifier opens all four generated HTMLs side-by-side with their respective references (where applicable) and runs the audit on each. The sequential read is mechanical (D3-22): "if two read the same, the interviews converged silently."

Per D3-20: the four canonical fixture subjects are deliberately deshtml-self-referential / Caseproof-internal so the verifier already understands the material — this isolates the test to "does the SKILL render this correctly" rather than "does Claude understand this domain." Same logic as Phase 2's `deshtml-about-itself` fixture.

Purpose: Wave 1 (plans 03-01, 03-02, 03-03) ships every file Phase 3 needs. This plan is the only one that exercises all five doc types end-to-end with real generated output. It is the only place where allowlist-completeness gaps can surface (Phase 2's 02-04 SUMMARY documents this exact failure mode and how the harvester was extended in response — Phase 3's wildcard glob in plan 03-03 closes the same surface for new format skeletons, but live testing here confirms it).

Output: 8 fixture-input files (one pair per type, 4 type subdirectories) + 1 FIXTURE-NOTES.md. The four generated HTMLs themselves are NOT committed (single-run artifacts in the verifier's CWD; outside the deshtml repo per Phase 2 convention).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-remaining-four-doc-types/03-CONTEXT.md
@.planning/phases/03-remaining-four-doc-types/03-RESEARCH.md
@.planning/phases/03-remaining-four-doc-types/03-01-PLAN.md
@.planning/phases/03-remaining-four-doc-types/03-02-PLAN.md
@.planning/phases/03-remaining-four-doc-types/03-03-PLAN.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-04-PLAN.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/expected-arc.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md
@skill/SKILL.md
@skill/story-arc.md
@skill/interview/handbook.md
@skill/audit/run.sh
@skill/audit/rules.md
@skill/design/SYSTEM.md
@skill/design/formats/handbook.html
@skill/design/formats/overview.html
@/Users/sperezasis/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write the 8 fixture-input files (4 types × {interview-answers.md, expected-arc.md}) and stage the local skill install</name>
  <files>.planning/phases/03-remaining-four-doc-types/fixture/pitch/interview-answers.md, .planning/phases/03-remaining-four-doc-types/fixture/pitch/expected-arc.md, .planning/phases/03-remaining-four-doc-types/fixture/technical-brief/interview-answers.md, .planning/phases/03-remaining-four-doc-types/fixture/technical-brief/expected-arc.md, .planning/phases/03-remaining-four-doc-types/fixture/presentation/interview-answers.md, .planning/phases/03-remaining-four-doc-types/fixture/presentation/expected-arc.md, .planning/phases/03-remaining-four-doc-types/fixture/meeting-prep/interview-answers.md, .planning/phases/03-remaining-four-doc-types/fixture/meeting-prep/expected-arc.md</files>
  <read_first>
    - .planning/phases/03-remaining-four-doc-types/03-CONTEXT.md §"D3-20, D3-21, D3-22" (the four fixture subjects, one human-verify, sequential-read mandate)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Test Strategy for Visual Gate → Wave 1: Per-type fixtures" (the table of fixture subjects + expected formats + reference HTMLs)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Visual Gate for Presentation (D3-21 rubric)" (the should-look-like / must-have / must-not-have / browser-test-matrix checklist for the Presentation fixture)
    - .planning/phases/03-remaining-four-doc-types/03-RESEARCH.md §"Open Questions → OQ-3" (Presentation fixture H1 wording — `Phase 3 status update — deshtml`)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md (Phase 2 fixture shape — Q1 doc type → Q2-Q6 answers → approve)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/expected-arc.md (Phase 2 expected-arc shape — row count, flowing paragraph, observable verifier criteria)
    - skill/interview/pitch.md (the per-type questions; the interview-answers.md must answer the questions in the order the file presents them — plan 03-02's output)
    - skill/interview/technical-brief.md (same)
    - skill/interview/presentation.md (same)
    - skill/interview/meeting-prep.md (same)
  </read_first>
  <action>
    Three sub-steps: stage the local skill install; write the 8 fixture-input files; verify the staging worked.

    ### Sub-step A — Stage the local skill install (preparation, not committed)

    Refresh the local working copy at `~/.claude/skills/deshtml/` so `/deshtml` resolves to the Wave-1 outputs (presentation.html, four interview files, updated audit). Same pattern as Phase 2 plan 02-04 Task 1:

    ```bash
    mkdir -p "$HOME/.claude/skills"
    rm -rf "$HOME/.claude/skills/deshtml"
    cp -R "/Users/sperezasis/projects/code/deshtml/skill" "$HOME/.claude/skills/deshtml"
    ```

    This bypasses `bin/install.sh` (Phase 4 LAUNCH-01 re-tests the curl-pipe-bash flow against the live URL; Phase 3 only needs the payload installed locally so `/deshtml` resolves to the Wave-1 changes).

    ### Sub-step B — Create the 4 fixture subdirectories

    ```bash
    mkdir -p .planning/phases/03-remaining-four-doc-types/fixture/pitch
    mkdir -p .planning/phases/03-remaining-four-doc-types/fixture/technical-brief
    mkdir -p .planning/phases/03-remaining-four-doc-types/fixture/presentation
    mkdir -p .planning/phases/03-remaining-four-doc-types/fixture/meeting-prep
    ```

    ### Sub-step C — Write `fixture/pitch/interview-answers.md`

    The pitch interview's 5 questions per plan 03-02: Audience → The ask → The problem → Your solution → Inclusions / exclusions. Use this exact content:

    ```markdown
    # Fixture: pitch — interview answers

    Reproducible inputs for the Phase 3 pitch fixture run (D3-20).
    The human verifier in plan 03-04 pastes these answers, in order, into the
    running `/deshtml` interview AFTER picking `pitch` as the doc type.

    Subject: "Pitching deshtml to a small-team CTO." Audience: a technical
    decision-maker (CTO of a 5-15-person product team). Ask: greenlight to
    build deshtml-equivalent tooling internally, OR pilot deshtml directly.
    Expected format: Overview (1-3 sections per D3-01 mapping).

    ## Pre-run

    Verifier confirms:
    1. `~/.claude/skills/deshtml/SKILL.md` exists and includes Step 5b (plan 03-01).
    2. `~/.claude/skills/deshtml/interview/pitch.md` exists (plan 03-02).
    3. Working directory is OUTSIDE the deshtml repo (recommended: `~/Desktop/` or `/tmp/deshtml-fixture/`).
    4. Claude Code session is fresh.

    ## Invocation

    ```
    /deshtml
    ```

    ## Q1 — Document type

    > pitch

    Expected: Step 3 reads `interview/pitch.md` (per plan 03-01's Step 3 substitution).

    ## Q2 — Audience

    > A small-team CTO (5-15 engineers). Mid-tenure, ships their own internal tooling. Hasn't seen deshtml; has seen one of my Caseproof handbooks before.

    ## Q3 — The ask

    > Greenlight to either pilot deshtml on our team for one quarter, or fund building an equivalent in our stack.

    ## Q4 — The problem

    > Our team's docs read like docs because no one has the time to make them read like products. Designed write-ups exist for big launches, never for everything else.

    ## Q5 — Your solution

    > deshtml is a Claude Code skill that turns ideas into Caseproof-designed HTML in one command. Story-arc gate keeps the writing tight. No design knowledge required.

    ## Q6 — Inclusions / exclusions

    > Include: the install one-liner. Avoid: any "10x productivity" or "revolutionary" framing — the audience reads pitch decks all day; that vocabulary triggers eyerolls.

    ## After Q6 — Approval

    Skill prints `Format: overview` (3 sections expected → matches D3-01 rule). Verifier compares arc against `expected-arc.md`. If acceptable:

    > approve

    Verifier expects the skill to:
    1. Compute filename: `2026-04-28-pitching-deshtml-pitch.html` (or with `-2` suffix on collision).
    2. Render with palette + typography + components inlined; format = overview (1440px linear, no sidebar).
    3. Run `audit/run.sh` (exit 0 expected).
    4. Run `open` on the absolute path.
    5. Print the absolute path on its own line as the LAST output.
    ```

    ### Sub-step D — Write `fixture/pitch/expected-arc.md`

    ```markdown
    # Fixture: pitch — expected arc shape

    Reference for the human verifier in plan 03-04 Task 2. The arc the skill
    proposes after the pitch interview should resemble this in shape.

    ## Expected number of rows

    3 rows. Pitch is problem → solution → ask. <3 means a beat is missing;
    >3 deserves scrutiny — pitch length is what makes pitch (not handbook).

    ## Expected format auto-selection (D3-01)

    Step 5b should print exactly one line:

    > Format: overview

    Because: type=pitch is NOT presentation; arc has <4 rows → falls through
    to overview. If verifier sees `Format: handbook`, the arc has too many
    rows (likely the interview answers leaked extra material — re-ask the
    user to trim, or accept the auto-selection and edit the arc to land
    overview).

    ## Expected flowing paragraph

    Reading the `One sentence` column top-to-bottom should read something like:

    > Designed docs are how my team's work gets noticed; we don't have time
    > to design every doc. deshtml is a Claude Code skill that produces
    > designed HTML from a 5-question interview. We pilot deshtml on our team
    > for one quarter, or fund building it in our stack.

    Acceptable variations: any phrasing that lands the three-beat narrative.
    NOT acceptable: a paragraph that reads as bullet points (causality chain
    broken).

    ## Expected section titles (handbook tone in TITLES; pitch tone OK in body)

    Should pass the three self-review checks (story-arc.md):
    - **Tone:** every TITLE describes what IS, never sells. No "10x", "revolutionary",
      "game-changing." (Pitch's BODY may read more direct/confident — that's the
      Pattern 8 divergence — but TITLES still inherit handbook tone.)
    - **Causality chain:** problem → solution → ask. Each follows from the previous.
    - **Name the thing:** every title contains a concrete noun (`docs`, `skill`, `pilot`).

    ## Visual diff target (Overview format)

    Side-by-side with `~/work/caseproof/gh-pm/pm/bnp/.planning/figma/bnp-overview.html`:
    same palette, same Inter font, same hero scale (1440px linear, no sidebar),
    same section spacing (80px / 120px container per overview.html). Components
    used in body should match those in `bnp-overview.html` (cards, hero, dividers).

    iOS Safari forced-dark-mode test: stays light (DESIGN-07 carry-over).

    ## What the verifier looks for

    Before typing `approve`:
    1. Five columns (canonical order from story-arc.md).
    2. EXACTLY 3 rows.
    3. `Format: overview` printed below the arc table BEFORE rendering.
    4. Status lines under each row showing tone/chain/named (auto-fix surfaces if needed).
    5. Flowing paragraph reads as a coherent ask, not a bullet list.

    Before opening the generated HTML:
    1. Filename matches `YYYY-MM-DD-<slug>-pitch.html`.
    2. `open` succeeded.
    3. Absolute path printed on its own line LAST (no banner, no emoji).

    In the browser:
    1. NO sidebar (Overview = no `<aside class="sidebar">`).
    2. Same palette as bnp-overview.html.
    3. Body tone may run direct/confident (pitch divergence) — section TITLES
       still describe what IS.
    ```

    ### Sub-step E — Write `fixture/technical-brief/interview-answers.md`

    Tech-brief interview's 5 questions per plan 03-02: Audience → The decision → Alternatives considered → Trade-offs → Inclusions / exclusions. Subject from D3-20 / RESEARCH §"Wave 1: Per-type fixtures": "Why we chose curl-pipe-bash for distribution."

    ```markdown
    # Fixture: technical-brief — interview answers

    Reproducible inputs for the Phase 3 technical-brief fixture run (D3-20).

    Subject: "Why we chose curl-pipe-bash for distribution." Audience: Caseproof
    engineers, mid-tenure, familiar with the codebase but not with this specific
    decision. Decision: pipe-bash over a packaged installer. Expected format:
    Handbook (5-6 sections per D3-01 mapping for ≥4-section docs).

    ## Pre-run

    Same as pitch fixture (skill installed at `~/.claude/skills/deshtml/`,
    fresh Claude Code session, CWD outside the deshtml repo).

    ## Invocation

    ```
    /deshtml
    ```

    ## Q1 — Document type

    > technical brief

    (User types `technical brief` with a space; SKILL.md normalizes to `technical-brief` per plan 03-01 Step 2 + Step 3 substitution.)

    ## Q2 — Audience

    > Caseproof engineers. Mid-tenure (1-3 years on the team), comfortable in bash and Node. Have seen GSD's installer; have NOT seen deshtml's design choices written up.

    ## Q3 — The decision

    > Ship deshtml's distribution as `curl https://… | bash`, not as an npm package or a Mac .pkg installer.

    ## Q4 — Alternatives considered

    > 1) npm package (`npm install -g deshtml`). 2) Homebrew formula. 3) Mac .pkg signed installer. 4) Manual git-clone + symlink.

    ## Q5 — Trade-offs that drove the choice

    > Curl-pipe-bash matches GSD's pattern (audience already knows it). Zero packaging overhead. No registry account / signing key / formula PR. Audit-friendly (script is human-readable). Trade-off: requires user trust at install time; mitigated by hosting on a known repo + the `set -euo pipefail` discipline. Not a fit if we ever add binary deps; deshtml is pure prompt + templates so binary deps are unlikely.

    ## Q6 — Inclusions / exclusions

    > Include: the explicit GSD-shaped install one-liner. Include: the trust-at-install-time rationale. Avoid: vague claims like "industry standard" — name the actual pattern (GSD).

    ## After Q6 — Approval

    Skill prints `Format: handbook` (5+ sections expected → matches D3-01 rule). Verifier compares arc against `expected-arc.md`. If acceptable:

    > approve

    Verifier expects: `2026-04-28-why-we-chose-curl-pipe-technical-brief.html` (or `-2`/`-3` on collision), audit exit 0, default browser opens, absolute path is LAST line.
    ```

    ### Sub-step F — Write `fixture/technical-brief/expected-arc.md`

    ```markdown
    # Fixture: technical-brief — expected arc shape

    ## Expected number of rows

    5-6 rows. Tech-brief beats: decision → context → alternatives → trade-offs
    → recommendation → next-steps (or compressed to 5: decision, context,
    alternatives, trade-offs, recommendation). <5 means alternatives/trade-offs
    were dropped — that's the heart of a tech brief; flag.

    ## Expected format auto-selection (D3-01)

    Step 5b should print exactly one line:

    > Format: handbook

    Because: type=technical-brief is NOT presentation; arc has ≥4 rows → handbook.

    ## Expected flowing paragraph

    > Distribution is the first thing a user touches; we made it match GSD.
    > A packaged installer (npm / Homebrew / .pkg) brings registry overhead and
    > signing keys. Curl-pipe-bash brings none of that. The trade-off is trust
    > at install time. We accept it because the audit script is the user-readable
    > review surface. The recommendation: ship pipe-bash; revisit if binary deps
    > land.

    Acceptable variations: any phrasing that lands the decision-alternatives-
    trade-offs spine. NOT acceptable: a "selling-the-decision" tone (engineers
    read for facts; that's why tech-brief uses verbatim CLAUDE.md tone — handbook
    in titles AND body).

    ## Expected section titles (verbatim CLAUDE.md handbook tone, BODY too)

    Engineers read for facts; selling is wrong here (RESEARCH §Pattern 8 tech-brief row).
    Self-review should show ≥0 auto-fixes; if more than 1 fix surfaces, the interview
    answers may have leaked sales tone (re-ask the user).

    Concrete-noun audit: every title names a thing (`distribution`, `package`,
    `installer`, `trade-offs`, `recommendation`).

    ## Visual diff target (Handbook format)

    Side-by-side with `~/work/caseproof/gh-pm/pm/pm-framework/diagrams/pm-system.html`:
    same palette, same Inter font, same H1 scale (56px / 800 / -2.5px tracking),
    same H2 scale (36px / 800), same 220px black sidebar (DOC-03 = Handbook
    format), same 70px / 30px section spacing.

    iOS Safari forced-dark-mode: stays light.

    ## What the verifier looks for

    Before typing `approve`:
    1. Five columns.
    2. 5-6 rows.
    3. `Format: handbook` printed below the arc.
    4. Status lines all green (handbook tone is the natural fit; any auto-fix
       suggests the answers slipped into pitch tone — flag).
    5. Flowing paragraph reads as a decision write-up.

    Before opening:
    1. Filename matches `YYYY-MM-DD-<slug>-technical-brief.html`.
    2. Sidebar is present (Handbook signature — 220px black).
    3. Compare to pm-system.html: every visual layer matches.
    ```

    ### Sub-step G — Write `fixture/presentation/interview-answers.md`

    Presentation interview's 5 questions per plan 03-02: Audience → The takeaway → Slide outline → Tone → Inclusions / exclusions. Subject from D3-20 + RESEARCH OQ-3: "Phase 3 status update — deshtml" (literal H1).

    ```markdown
    # Fixture: presentation — interview answers

    Reproducible inputs for the Phase 3 presentation fixture run (D3-20 + RESEARCH OQ-3).

    Subject: "Phase 3 status update — deshtml." Audience: Caseproof team in
    a recurring product-update meeting. Format: Presentation (always — D3-01
    forces format=presentation when type=presentation). Slide count: 5.

    ## Pre-run

    Same as pitch fixture. PLUS: confirm `~/.claude/skills/deshtml/design/formats/presentation.html`
    exists (plan 03-01 output) — without it, Step 6's variable skeleton load fails.

    ## Invocation

    ```
    /deshtml
    ```

    ## Q1 — Document type

    > presentation

    ## Q2 — Audience

    > Caseproof product team in a regular Tuesday product-update meeting. They've heard "deshtml" before but haven't seen what Phase 3 ships.

    ## Q3 — The takeaway

    > Phase 3 ships all five doc types end-to-end; v0.1.0 is on the runway after Phase 4 README + launch hardening.

    ## Q4 — Slide outline

    > Let Claude propose 5 slides. Suggested order: (1) status header, (2) what shipped in Phase 3, (3) the audit story, (4) what's next in Phase 4, (5) ask + close.

    ## Q5 — Tone

    > (default — handbook tone in titles, more energetic in body per pitch.md / presentation.md Pattern 8 calibration)

    ## Q6 — Inclusions / exclusions

    > Include: the slide counter visible on every slide. Include: the GSD-style install one-liner on the "what shipped" slide. Avoid: speaker notes (out-of-scope for v1 per CONTEXT.md "Deferred Ideas").

    ## After Q6 — Approval

    Skill prints `Format: presentation` (forced by type=presentation per D3-01). Verifier compares arc against `expected-arc.md`. If acceptable:

    > approve

    Verifier expects: `2026-04-28-phase-3-status-update-presentation.html`, audit exit 0 (Rule 5 schema check passes too — presentation.md follows DOC-06), default browser opens, scroll-snap works on first slide.
    ```

    ### Sub-step H — Write `fixture/presentation/expected-arc.md`

    Embed the RESEARCH §"Visual Gate for Presentation (D3-21 rubric)" checklist as the visual-gate criteria (no Caseproof reference exists; written rubric IS the gate).

    ```markdown
    # Fixture: presentation — expected arc shape

    Presentation has NO Caseproof reference. The visual gate IS the written
    rubric below (RESEARCH §"Visual Gate for Presentation (D3-21 rubric)").

    ## Expected number of rows = slide count

    5 rows = 5 slides. Each arc row maps to one `<section class="slide">` in
    the rendered output. Slide counter shows `1 / 5` … `5 / 5` per the
    `TOTAL_SLIDES_LITERAL` substitution from plan 03-01.

    ## Expected format auto-selection (D3-01)

    Step 5b should print exactly one line:

    > Format: presentation

    Because: type=presentation short-circuits at rule 1. Even if the arc had
    1 row or 7 rows, format=presentation always.

    ## Expected flowing paragraph

    > Phase 3 status: all five doc types ship end-to-end. The Phase 3 audit
    > extension picks up new format skeletons via wildcard glob; Rule 5
    > catches schema drift across interview files. Phase 4 wires the README,
    > launch hardening, and source-mode shortcut. v0.1.0 is the next tag.

    Acceptable variations: any phrasing that lands the 5-beat status arc.

    ## Expected slide titles (handbook tone — RESEARCH §Pattern 8 presentation row)

    Self-review (story-arc.md): TITLES describe what IS. Body may run more
    energetic (slides reward visual punch) but TITLES are still handbook —
    the arc-gate self-review enforces this regardless of doc type.

    ## Visual gate rubric (RESEARCH §"Visual Gate for Presentation")

    ### Should look like:
    - Each slide fills the viewport (100vh, no scrollbar within a slide).
    - Slide background is `var(--white)` — pure light, no off-white.
    - H1 uses Inter at ~80px, weight 800, tracking ~-3px. Visually larger than
      any handbook H1.
    - H2 (if used) at ~56px, weight 800.
    - Body text at ~22px, gray (`var(--g8)`), comfortable line-length.
    - Slide counter visible bottom-right, shows "current / total" (e.g., `2 / 5`)
      in muted gray.
    - Floating slide-nav present top-right, reuses `.nav-a` styling from
      sidebar (Pattern 5).
    - Palette identical to Handbook/Overview. NO new colors.

    ### Must have:
    - `<meta name="color-scheme" content="light">` and
      `<meta name="supported-color-schemes" content="light">` (Pitfall 7).
    - All three CSS files inlined (palette → typography → components per D2-15).
    - Inter `@import` present (typography.css inlined).
    - Zero `<script>` tags. Zero `on*=` handlers. Zero `javascript:` URLs.
    - All classes used appear in the audit allowlist (after wildcard harvest
      from plan 03-03).

    ### Must not have:
    - Any sidebar (D3-09 — full-viewport, no sidebar).
    - Any sticky bar.
    - Any custom transition or animation CSS beyond browser default snap (D3-07).
    - Hex literals outside `:root`.
    - Slide content overflowing 100vh.

    ### Browser test matrix:
    - **Chrome (latest stable on macOS)**: scroll wheel scrolls one slide at a time.
      Trackpad scroll snaps. Anchor click jumps cleanly.
    - **Safari (latest stable on macOS)**: same three behaviors. PRIMARY VERIFICATION.
    - **iOS Safari (Simulator OK)**: forced-dark-mode test stays light. Touch-swipe
      behavior acceptable.

    ### Acceptable diffs:
    - Different content per slide (this fixture is "Phase 3 status update").
    - Slight font tracking vs Caseproof references (Inter tracks differently
      at 80px than at 56px).

    ### Unacceptable:
    - Wrong palette color.
    - Custom font (Inter only).
    - Non-allowlisted class.
    - Snap fails on Chrome OR Safari (= spike from plan 03-01 was wrong, OR
      plan 03-01 shipped fallback but the fallback isn't documented in
      presentation.html's header — re-check the spike-outcome line).

    ## What the verifier looks for

    Before `approve`:
    1. Five columns.
    2. 5 rows.
    3. `Format: presentation`.
    4. Each `One sentence` reads slide-shaped (one beat per slide).

    Before opening:
    1. Filename matches `YYYY-MM-DD-<slug>-presentation.html`.
    2. Audit exit 0 (the wildcard harvester from plan 03-03 must accept
       `.slide`, `.slide-counter`, `.slide-nav`).

    In Chrome:
    1. Run the should-look-like checklist top-to-bottom.
    2. Scroll wheel → snaps. Trackpad fast-scroll → snaps to last slide
       without skipping.
    3. Click `<a href="#slide-3">` → jumps cleanly.

    In Safari:
    1. Same checklist. PRIMARY signal — if Safari is broken and Chrome
       works, the spike from plan 03-01 was a false PASS; revisit.

    iOS Safari forced-dark-mode:
    1. File stays light.
    ```

    ### Sub-step I — Write `fixture/meeting-prep/interview-answers.md`

    Meeting-prep interview's 5 questions per plan 03-02: Meeting purpose → Audience → Talking points → Open questions / risks → Inclusions / exclusions. Subject from D3-20: "Demo run-through with Delfi."

    ```markdown
    # Fixture: meeting-prep — interview answers

    Reproducible inputs for the Phase 3 meeting-prep fixture run (D3-20).

    Subject: "Demo run-through with Delfi." Audience: Delfi (target reader
    per PROJECT.md). Format: Overview (3 sections per D3-01 mapping for
    1-3-section docs). Sections: context, demo flow, anticipated questions.

    ## Pre-run

    Same as pitch fixture.

    ## Invocation

    ```
    /deshtml
    ```

    ## Q1 — Document type

    > meeting prep

    (User types `meeting prep` with a space; SKILL.md normalizes to `meeting-prep`.)

    ## Q2 — Meeting purpose

    > Walk Delfi through deshtml end-to-end so she can run it herself by the end of the call.

    ## Q3 — Audience

    > Delfi (technical-curious but non-developer). She's seen the install one-liner; hasn't used Claude Code skills before.

    ## Q4 — Talking points

    > 1) What deshtml is in 30 seconds. 2) Install + first run together. 3) Pick a doc type, run an interview, approve the arc. 4) Show the audit fire on a manually-broken file. 5) Open the generated HTML side-by-side with a Caseproof reference.

    ## Q5 — Open questions / risks

    > Risk: she may want to skip the arc gate ("can we just generate?"). Plan: explain the gate IS the value (Pitfall 10 mitigation surfaces here as a UX moment). Open question: does the install one-liner work on her macOS version? Confirm pre-call.

    ## Q6 — Inclusions / exclusions

    > Include: the install one-liner. Include: a screenshot or one-sentence summary of "what the audit looks like when it fires." Avoid: any reference to internal phase numbers or planning docs (Delfi shouldn't need to know about Phases 1-4).

    ## After Q6 — Approval

    Skill prints `Format: overview` (3 sections expected → matches D3-01 rule). Verifier compares arc against `expected-arc.md`. If acceptable:

    > approve

    Verifier expects: `2026-04-28-demo-run-through-with-delfi-meeting-prep.html`, audit exit 0, default browser opens, absolute path LAST line.
    ```

    ### Sub-step J — Write `fixture/meeting-prep/expected-arc.md`

    ```markdown
    # Fixture: meeting-prep — expected arc shape

    ## Expected number of rows

    3 rows: context → demo flow → anticipated questions. <3 means a beat is
    missing; >3 means the briefing is too long for a 30-min walkthrough.

    ## Expected format auto-selection (D3-01)

    Step 5b should print exactly one line:

    > Format: overview

    Because: type=meeting-prep is NOT presentation; arc has <4 rows → overview.

    ## Expected flowing paragraph

    > deshtml is a Claude Code skill that turns ideas into designed HTML
    > documents. We install it on Delfi's machine, run /deshtml on a topic
    > she picks, walk through the arc gate together, and open the generated
    > HTML side-by-side with a Caseproof reference. Anticipated questions
    > include "can I skip the arc step" (no — that's the moat) and "what
    > happens if the audit fails" (we show that live).

    Acceptable variations: any phrasing that lands the context-flow-questions
    structure. NOT acceptable: meeting-prep that reads as a marketing pitch
    (verbatim CLAUDE.md tone — Pattern 8 meeting-prep row).

    ## Expected section titles (verbatim CLAUDE.md handbook tone)

    Same calibration as tech-brief: handbook in TITLES and BODY. Briefings
    deliver facts; selling is wrong here. Self-review surfaces ≥0 fixes; >1
    suggests pitch tone leaked into the answers.

    Concrete-noun audit: every title names a thing (`context`, `walk-through`,
    `questions`).

    ## Visual diff target (Overview format)

    Side-by-side with `~/work/caseproof/gh-pm/pm/bnp/.planning/figma/bnp-overview.html`:
    same palette, same Inter font, same hero scale (1440px linear, no sidebar),
    same section spacing (80px / 120px container).

    Sequential-read note (D3-22): pitch.html and meeting-prep.html BOTH render as
    Overview. The verifier reads them in sequence — they should NOT feel
    interchangeable. The questions surface different material:
    - Pitch: ask + problem + solution.
    - Meeting prep: meeting purpose + talking points + open questions/risks.

    If the two reads feel the same, the interview-questions are too similar
    (Pitfall 19) — fix by tightening the question wording, not by changing
    the visual gate.

    iOS Safari forced-dark-mode: stays light.

    ## What the verifier looks for

    Before `approve`:
    1. Five columns.
    2. EXACTLY 3 rows.
    3. `Format: overview`.
    4. Status lines all green (any auto-fix suggests the meeting-prep tone
       slipped — re-ask).
    5. Flowing paragraph reads as a briefing, not a sales pitch.

    Before opening:
    1. Filename matches `YYYY-MM-DD-<slug>-meeting-prep.html`.
    2. NO sidebar (Overview).
    3. Compare to bnp-overview.html: visual layers match.

    Sequential-read check (after all four fixtures generated):
    1. Open pitch.html, technical-brief.html, presentation.html, meeting-prep.html
       side-by-side.
    2. Read top-to-bottom. Each should be distinguishable by H1, by section
       structure, by content density, by tone.
    3. If any two feel like the same document with different filenames →
       Pitfall 19 surfaced; fix the interview-questions in plan 03-02 and
       re-run that fixture only.
    ```

    ### Sub-step K — Verify the staging worked

    The verify block below tests:
    - `~/.claude/skills/deshtml/SKILL.md` exists with Step 5b (plan 03-01 output).
    - `~/.claude/skills/deshtml/design/formats/presentation.html` exists (plan 03-01).
    - `~/.claude/skills/deshtml/interview/{pitch,technical-brief,presentation,meeting-prep}.md` all exist (plan 03-02).
    - `~/.claude/skills/deshtml/audit/run.sh` exists, executable, contains `format_skels=` (plan 03-03).
    - All 8 fixture-input files exist with the expected content markers.
  </action>
  <behavior>
    - Four fixture subdirectories exist under `.planning/phases/03-remaining-four-doc-types/fixture/`: `pitch/`, `technical-brief/`, `presentation/`, `meeting-prep/`.
    - Each subdirectory contains `interview-answers.md` and `expected-arc.md` (8 files total).
    - Each `interview-answers.md` documents Q1 (doc type) plus the 5 type-specific questions plus the approval phrase.
    - Each `expected-arc.md` specifies expected row count, expected format auto-selection, expected flowing paragraph shape, visual-diff target (or written rubric for presentation), and the verifier's pre-`approve` checklist.
    - The presentation fixture's expected-arc.md embeds the RESEARCH §"Visual Gate for Presentation" rubric (no Caseproof reference exists for presentation).
    - Each fixture's expected `Format: <name>` line matches the D3-01 mapping: pitch → overview, technical-brief → handbook, presentation → presentation, meeting-prep → overview.
    - `~/.claude/skills/deshtml/` mirrors the working copy of `skill/` after the staging copy.
    - All Wave-1 outputs are visible in the local install: presentation.html, four new interview files, updated audit/run.sh with `format_skels=` (wildcard harvester from plan 03-03).
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -d .planning/phases/03-remaining-four-doc-types/fixture/pitch &&
      test -d .planning/phases/03-remaining-four-doc-types/fixture/technical-brief &&
      test -d .planning/phases/03-remaining-four-doc-types/fixture/presentation &&
      test -d .planning/phases/03-remaining-four-doc-types/fixture/meeting-prep &&
      for type in pitch technical-brief presentation meeting-prep; do
        test -s ".planning/phases/03-remaining-four-doc-types/fixture/${type}/interview-answers.md"
        test -s ".planning/phases/03-remaining-four-doc-types/fixture/${type}/expected-arc.md"
      done &&
      command grep -qF 'small-team CTO' .planning/phases/03-remaining-four-doc-types/fixture/pitch/interview-answers.md &&
      command grep -qF 'curl-pipe-bash' .planning/phases/03-remaining-four-doc-types/fixture/technical-brief/interview-answers.md &&
      command grep -qF 'Phase 3 status update' .planning/phases/03-remaining-four-doc-types/fixture/presentation/interview-answers.md &&
      command grep -qF 'Delfi' .planning/phases/03-remaining-four-doc-types/fixture/meeting-prep/interview-answers.md &&
      command grep -qF 'Format: overview' .planning/phases/03-remaining-four-doc-types/fixture/pitch/expected-arc.md &&
      command grep -qF 'Format: handbook' .planning/phases/03-remaining-four-doc-types/fixture/technical-brief/expected-arc.md &&
      command grep -qF 'Format: presentation' .planning/phases/03-remaining-four-doc-types/fixture/presentation/expected-arc.md &&
      command grep -qF 'Format: overview' .planning/phases/03-remaining-four-doc-types/fixture/meeting-prep/expected-arc.md &&
      command grep -qF 'pm-system.html' .planning/phases/03-remaining-four-doc-types/fixture/technical-brief/expected-arc.md &&
      command grep -qF 'bnp-overview.html' .planning/phases/03-remaining-four-doc-types/fixture/pitch/expected-arc.md &&
      command grep -qF 'bnp-overview.html' .planning/phases/03-remaining-four-doc-types/fixture/meeting-prep/expected-arc.md &&
      command grep -qiE 'Visual Gate|written rubric' .planning/phases/03-remaining-four-doc-types/fixture/presentation/expected-arc.md &&
      command grep -qF 'iOS Safari' .planning/phases/03-remaining-four-doc-types/fixture/presentation/expected-arc.md &&
      command grep -qiE 'Sequential-read|sequentially' .planning/phases/03-remaining-four-doc-types/fixture/meeting-prep/expected-arc.md &&
      test -f "$HOME/.claude/skills/deshtml/SKILL.md" &&
      command grep -q '## Step 5b' "$HOME/.claude/skills/deshtml/SKILL.md" &&
      test -f "$HOME/.claude/skills/deshtml/design/formats/presentation.html" &&
      test -f "$HOME/.claude/skills/deshtml/interview/handbook.md" &&
      test -f "$HOME/.claude/skills/deshtml/interview/pitch.md" &&
      test -f "$HOME/.claude/skills/deshtml/interview/technical-brief.md" &&
      test -f "$HOME/.claude/skills/deshtml/interview/presentation.md" &&
      test -f "$HOME/.claude/skills/deshtml/interview/meeting-prep.md" &&
      test -x "$HOME/.claude/skills/deshtml/audit/run.sh" &&
      command grep -qF 'format_skels=' "$HOME/.claude/skills/deshtml/audit/run.sh" &&
      command grep -qF 'Rule 5' "$HOME/.claude/skills/deshtml/audit/run.sh" &&
      test -f "$HOME/.claude/skills/deshtml/design/components.css" &&
      test -f "$HOME/.claude/skills/deshtml/design/components.html" &&
      test -f "$HOME/.claude/skills/deshtml/design/formats/handbook.html" &&
      test -f "$HOME/.claude/skills/deshtml/design/formats/overview.html" &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - All 8 fixture-input files exist with the documented per-type content (subjects from D3-20, questions answering the per-type interview file's order from plan 03-02).
    - Each `expected-arc.md` documents the expected `Format: <name>` line per the D3-01 mapping (pitch→overview, technical-brief→handbook, presentation→presentation, meeting-prep→overview).
    - The Presentation fixture's `expected-arc.md` embeds the RESEARCH §"Visual Gate for Presentation" rubric (no Caseproof reference exists; the written rubric IS the gate).
    - The meeting-prep fixture's `expected-arc.md` documents the sequential-read check (D3-22 — explicitly cross-references pitch.html since both render as Overview).
    - The local skill install at `~/.claude/skills/deshtml/` mirrors the working copy with all Wave-1 outputs present (presentation.html, four new interview files, updated audit with `format_skels=` and Rule 5).
    - Repo's `skill/audit/run.sh` is executable; the install's audit/run.sh inherits the executable bit.
  </acceptance_criteria>
  <done>
    Eight fixture-input files exist (4 types × 2 files), four subdirectories, all per-type content documented and committable. The local skill install mirrors the working copy with all Wave-1 changes visible. The 30+ enumerated automated checks pass. The human-verify checkpoint in Task 2 can now run.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Wave 1 of Phase 3 (plans 03-01, 03-02, 03-03) shipped every file Phase 3 needs:
    - `skill/design/formats/presentation.html` — slide-deck skeleton with scroll-snap (or `:target` fallback per the Task 1 spike outcome from plan 03-01).
    - `skill/SKILL.md` — Step 2 routes flipped, Step 3 reads per-type interview, new Step 5b auto-selects format, Step 6 loads `formats/${format}.html`.
    - `skill/interview/{pitch,technical-brief,presentation,meeting-prep}.md` — four type-tailored interviews, each ≤5 questions, DOC-06 schema preserved.
    - `skill/audit/run.sh` — wildcard harvester over `formats/*.html` (D3-18) + new Rule 5 schema-drift check (RESEARCH §Pattern 9).
    - `skill/audit/rules.md` — Rule 2 source list updated, Rule 5 documented, smoke-test table extended.

    Task 1 of this plan staged the working copy at `~/.claude/skills/deshtml/` and committed reproducible fixture inputs for all four Phase-3 doc types.

    This checkpoint runs all four canonical fixtures end-to-end through the running skill, reads the four outputs sequentially (D3-22 — Pitfall 19 mitigation), and verifies every Phase-3 contract holds: per-type interview routes fire, format auto-selects per D3-01, audit accepts new format-skeleton classes (wildcard harvester), Rule 5 is silently passing on all 5 interview files, sequential read confirms each output is distinct.
  </what-built>
  <how-to-verify>
    Run the four fixtures end-to-end. Total time: ~15-20 minutes (3-5 min per fixture + 5 min sequential read + 3 min audit smoke tests).

    ### Step 1 — Pre-flight (1 min)

    Open a fresh Claude Code session. Working directory: `/tmp/deshtml-fixture/` (recommended — outside the deshtml repo so the four .html files don't accidentally get committed).

    ```bash
    mkdir -p /tmp/deshtml-fixture
    cd /tmp/deshtml-fixture
    ls ~/.claude/skills/deshtml/SKILL.md
    ls ~/.claude/skills/deshtml/design/formats/presentation.html
    ls ~/.claude/skills/deshtml/interview/pitch.md
    ls ~/.claude/skills/deshtml/audit/run.sh
    ```

    All four `ls` commands should print paths. If any fails, re-run Task 1's `cp -R skill/ ~/.claude/skills/deshtml/`.

    ### Step 2 — Run the pitch fixture (4 min)

    Fresh conversation in Claude Code. Type:

    ```
    /deshtml
    ```

    Reply `pitch` to Q1. Then paste the answers from `.planning/phases/03-remaining-four-doc-types/fixture/pitch/interview-answers.md` (Q2-Q6), one per turn.

    After Q6, the skill should:
    1. Print `Format: overview` (D3-01 — pitch + 3 sections → overview).
    2. Show the arc table (3 rows expected).
    3. Show the flowing paragraph + status lines.

    Compare against `fixture/pitch/expected-arc.md`. If acceptable, type `approve`. Verify:
    1. Filename: `2026-04-28-pitching-deshtml-pitch.html` (or `-2`/`-3` on collision).
    2. The `Format: overview` line was printed BEFORE rendering.
    3. Default browser opens the file.
    4. The terminal's LAST line is the absolute path on its own line.
    5. Run `bash ~/.claude/skills/deshtml/audit/run.sh /tmp/deshtml-fixture/<filename>` → exit 0.

    Open side-by-side with `~/work/caseproof/gh-pm/pm/bnp/.planning/figma/bnp-overview.html`. Verify per the `expected-arc.md` checklist: same palette, same Inter, NO sidebar, hero shape matches.

    ### Step 3 — Run the technical-brief fixture (5 min)

    Fresh conversation. Type `/deshtml`. Reply `technical brief` (with space — SKILL.md normalizes to `technical-brief` for the file path).

    Paste Q2-Q6 from `fixture/technical-brief/interview-answers.md`.

    After Q6:
    1. Skill prints `Format: handbook` (D3-01 — tech-brief + 5+ sections → handbook).
    2. Arc table has 5-6 rows.
    3. All status lines should be green (handbook tone is the natural fit; auto-fix surfaces if pitch tone leaked).

    Reply `approve`. Verify:
    1. Filename matches `*-technical-brief.html`.
    2. Audit exit 0.
    3. Open side-by-side with `~/work/caseproof/gh-pm/pm/pm-framework/diagrams/pm-system.html`. Verify: 220px black sidebar (Handbook signature), H1 56px / 800 / -2.5px, same palette, same section spacing.

    ### Step 4 — Run the presentation fixture (5 min) — PRIMARY VERIFICATION FOR PHASE 3

    Fresh conversation. Type `/deshtml`. Reply `presentation`.

    Paste Q2-Q6 from `fixture/presentation/interview-answers.md`.

    After Q6:
    1. Skill prints `Format: presentation` (D3-01 forces — type=presentation always wins).
    2. Arc table has 5 rows = 5 slides.
    3. The arc reads as a 5-slide status update.

    Reply `approve`. Verify:
    1. Filename matches `*-presentation.html`.
    2. Audit exit 0 — this is the LIVE TEST for the wildcard harvester (plan 03-03). The classes `.slide`, `.slide-counter`, `.slide-nav` came from `formats/presentation.html` (plan 03-01) and must be in the allowlist. If audit fails with `unknown class "slide"`, the wildcard glob in `audit/run.sh` is broken — return `failed: presentation: harvester broken`.
    3. Default browser opens. **Run the RESEARCH §"Visual Gate for Presentation" checklist top-to-bottom** (embedded in `fixture/presentation/expected-arc.md`):
       - Each slide is 100vh (no scrollbar within a slide).
       - Slide counter visible bottom-right showing `1 / 5` … `5 / 5`.
       - Floating slide-nav top-right with anchor links.
       - Palette + Inter match.
       - NO sidebar, NO sticky bar.
    4. **Browser test matrix:**
       - Chrome: scroll wheel snaps. Trackpad fast-scroll lands on last slide. Anchor click jumps cleanly.
       - Safari: same three behaviors. **THIS IS THE PRIMARY VERIFICATION** — if Safari is broken and Chrome works, the spike from plan 03-01 was a false PASS. Read presentation.html's header comment — the `Spike outcome:` line should match what you see (PASS = full snap; FAIL = `:target`-only fallback, anchor clicks still work but no snap).
    5. iOS Safari forced-dark-mode (Simulator OK): file stays light.

    ### Step 5 — Run the meeting-prep fixture (4 min)

    Fresh conversation. Type `/deshtml`. Reply `meeting prep` (with space).

    Paste Q2-Q6 from `fixture/meeting-prep/interview-answers.md`.

    After Q6:
    1. Skill prints `Format: overview` (D3-01 — meeting-prep + 3 sections → overview).
    2. Arc table has 3 rows (context, demo flow, anticipated questions).

    Reply `approve`. Verify:
    1. Filename matches `*-meeting-prep.html`.
    2. Audit exit 0.
    3. Open side-by-side with `bnp-overview.html`. Verify same palette / Inter / NO sidebar / hero shape.

    ### Step 6 — Sequential-read check (D3-22, ~5 min) — Pitfall 19 mitigation

    This is the single most important Phase-3 gate. Open ALL FOUR generated HTMLs in tabs:

    ```bash
    open -a "Google Chrome" /tmp/deshtml-fixture/2026-04-28-pitching-deshtml-pitch.html
    open -a "Google Chrome" /tmp/deshtml-fixture/2026-04-28-why-we-chose-curl-pipe-technical-brief.html
    open -a "Google Chrome" /tmp/deshtml-fixture/2026-04-28-phase-3-status-update-presentation.html
    open -a "Google Chrome" /tmp/deshtml-fixture/2026-04-28-demo-run-through-with-delfi-meeting-prep.html
    ```

    Read all four in this order. For each, ask:
    - **By H1 alone:** can I tell which doc type this is?
    - **By section structure:** does the rhythm feel pitch / tech-brief / slides / briefing?
    - **By tone:** does pitch read direct/confident? does tech-brief read factual? does presentation read more energetic in body but handbook in titles? does meeting-prep read briefing-y?
    - **By content density:** does pitch land in 3 sections? does tech-brief land in 5+? does presentation land in 5 slides? does meeting-prep land in 3?

    **PASS:** each reads as a distinct document. Pitch and meeting-prep are both Overview but feel different — one asks for a yes/no, one walks through a meeting flow.

    **FAIL — Pitfall 19 surfaced:** any two read like the same document with different filenames. If pitch.html and meeting-prep.html feel interchangeable, the interview questions are too similar. Return `failed: <type>: <which-other-type-feels-the-same>`. Plan 03-02 owns the fix (rewrite the converged interview file's questions).

    ### Step 7 — Audit smoke test (2 min, optional but documented)

    Run the updated audit on each generated file with `--explain`:

    ```bash
    for f in /tmp/deshtml-fixture/2026-04-28-*.html; do
      echo "=== $f ==="
      bash ~/.claude/skills/deshtml/audit/run.sh --explain "$f" 2>&1 | tail -20
      echo "exit: $?"
    done
    ```

    All four should exit 0. The `--explain` output shows zero violations on each file.

    Run Rule 5 once more by listing interviews:

    ```bash
    ls ~/.claude/skills/deshtml/interview/
    # Should show: handbook.md, meeting-prep.md, pitch.md, presentation.md, technical-brief.md (5 files)
    bash ~/.claude/skills/deshtml/audit/run.sh /tmp/deshtml-fixture/2026-04-28-pitching-deshtml-pitch.html
    # Exit 0 confirms all 5 interview files pass Rule 5 schema check.
    ```

    ### Step 8 — Adversarial audit smoke test (1 min)

    Inject a violation into a copy of the pitch fixture:

    ```bash
    cp /tmp/deshtml-fixture/2026-04-28-pitching-deshtml-pitch.html /tmp/audit-test-bad.html
    perl -i -pe 's|<body|<body style="color:#ff0000"|' /tmp/audit-test-bad.html
    bash ~/.claude/skills/deshtml/audit/run.sh /tmp/audit-test-bad.html
    echo "audit exit (should be non-zero): $?"
    ```

    PASS: exit 1, stderr lists the hex violation. FAIL: exit 0 (Phase 2 audit regressed in Phase 3).

    ### What to type when done

    Reply with EXACTLY one of:
    - `approved` — every step passed; Phase 3 contract holds for all five doc types.
    - `approved with notes: <text>` — passed but with minor cosmetic deviations the human accepts (document them; folded into FIXTURE-NOTES.md).
    - `failed: <type>: <reason>` — describe what didn't work and which type. Examples:
      - `failed: presentation: snap broken in Safari` → re-spike plan 03-01.
      - `failed: pitch: feels like meeting-prep clone` → re-write plan 03-02 pitch.md questions.
      - `failed: technical-brief: audit rejected .ic class` → harvester broken in plan 03-03.
  </how-to-verify>
  <resume-signal>Reply `approved`, `approved with notes: <text>`, or `failed: <type>: <reason>`.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write fixture/FIXTURE-NOTES.md (single notes file capturing all four runs + sequential-read disposition + sign-off)</name>
  <files>.planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md</files>
  <read_first>
    - .planning/phases/03-remaining-four-doc-types/fixture/<type>/interview-answers.md (×4 — the inputs the run used)
    - .planning/phases/03-remaining-four-doc-types/fixture/<type>/expected-arc.md (×4 — the shapes compared against)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md (Phase 2 template — single doc, multiple sections; this Phase 3 file follows the same shape but covers four types in one file per D3-21)
    - The verifier's reply from Task 2 (the source of all "actual" data: filenames, audit exits, sequential-read disposition, deviations)
  </read_first>
  <action>
    Write a single `FIXTURE-NOTES.md` covering ALL four runs (per D3-21 — one notes file, not four) plus the sequential-read check (D3-22). Phase 4 maintainers read this to understand what "Phase 3 right" looked like across all five doc types.

    Use this exact template, filling every `<placeholder>` with real data from the verifier's reply:

    ```markdown
    # Fixture: Phase 3 four-fixture run — notes

    Run date: <YYYY-MM-DD of the actual run>
    Verifier: <Santiago or whoever ran the checkpoint>
    Verifier reply: <approved | approved with notes: ... | failed: ...>

    ## Per-type runs

    ### Pitch (pitch fixture, expected format=overview)

    - **Final filename:** `<actual-filename-from-pwd>` (collision suffix: `<none | -2 | -3>`)
    - **Working directory:** `<absolute path of CWD>`
    - **File size:** `<bytes>`
    - **Step 5b output:** `<actual `Format: <name>` line>` — expected: `Format: overview` per D3-01 (pitch + <4 rows). Match: `<yes | no>`.
    - **Arc rows proposed:** `<actual count>` (expected: 3)
    - **Self-review fixes:** `<count>` — `<summary, e.g., "0; or 1: section 2 title rewritten from 'It Easily Solves' to 'A skill that produces designed HTML'">`
    - **Audit exit (round 1):** `<0 | non-zero>`
    - **Audit retries triggered:** `<0, 1, 2, or 3>`
    - **Visual diff vs `bnp-overview.html`:** `<exact match | minor diff: ___ | wrong: ___>`
    - **Sequential-read disposition (vs other Overview = meeting-prep):** see "Sequential-read check" section below.

    ### Technical brief (technical-brief fixture, expected format=handbook)

    - **Final filename:** `<actual-filename>` (collision: `<none | -2 | -3>`)
    - **File size:** `<bytes>`
    - **Step 5b output:** `<actual line>` — expected `Format: handbook` (tech-brief + ≥4 rows). Match: `<yes | no>`.
    - **Arc rows proposed:** `<count>` (expected: 5-6)
    - **Self-review fixes:** `<count>` — `<summary>`
    - **Audit exit (round 1):** `<0 | non-zero>`
    - **Audit retries:** `<count>`
    - **Visual diff vs `pm-system.html`:** `<exact | minor: ___ | wrong: ___>`. Specifically: 220px sidebar present `<yes/no>`; H1 56px/800/-2.5px `<match/diff: ___>`; section spacing 70px/30px `<match/diff: ___>`.

    ### Presentation (presentation fixture, expected format=presentation, PRIMARY PHASE-3 SIGNAL)

    - **Final filename:** `<actual-filename>` (collision: `<none | -2 | -3>`)
    - **File size:** `<bytes>`
    - **Step 5b output:** `<actual line>` — expected `Format: presentation` (forced by type=presentation, regardless of section count). Match: `<yes | no>`.
    - **Arc rows proposed:** `<count>` (expected: 5)
    - **Self-review fixes:** `<count>` — `<summary>`
    - **Audit exit (round 1):** `<0 | non-zero>` — **THIS IS THE LIVE WILDCARD HARVESTER TEST.** If the audit rejects a `.slide`-prefixed class, plan 03-03's wildcard glob is broken. `<documented if any>`.
    - **Audit retries:** `<count>`
    - **Visual gate (RESEARCH §"Visual Gate for Presentation" checklist):**
      - Each slide 100vh, no internal scrollbar: `<yes/no>`.
      - Slide counter shows N / 5 (e.g., "2 / 5"): `<yes/no>`.
      - Slide nav top-right with anchor links: `<yes/no>`.
      - Palette identical to handbook/overview: `<yes/no>`.
      - NO sidebar, NO sticky bar: `<yes/no>`.
      - Inter @import present: `<yes/no>`.
    - **Browser matrix:**
      - Chrome scroll-snap: `<works | broken: ___>`.
      - Chrome anchor click: `<jumps cleanly | overshoots | broken>`.
      - **Safari scroll-snap (PRIMARY):** `<works | broken: ___>`.
      - **Safari anchor click:** `<jumps cleanly | broken>`.
      - iOS Safari forced-dark-mode: `<stayed light | inverted>`.
    - **Spike outcome line in `presentation.html`:** `<match the empirical browser behavior? — should match if plan 03-01's Task 1 spike was honest. If header says "PASS" but Safari snap is broken in this fixture, the spike was a false PASS — return failed.>`

    ### Meeting prep (meeting-prep fixture, expected format=overview)

    - **Final filename:** `<actual-filename>` (collision: `<none | -2 | -3>`)
    - **File size:** `<bytes>`
    - **Step 5b output:** `<actual line>` — expected `Format: overview`. Match: `<yes | no>`.
    - **Arc rows proposed:** `<count>` (expected: 3)
    - **Self-review fixes:** `<count>` — `<summary>`
    - **Audit exit (round 1):** `<0 | non-zero>`
    - **Audit retries:** `<count>`
    - **Visual diff vs `bnp-overview.html`:** `<exact | minor: ___ | wrong: ___>`.

    ## Sequential-read check (D3-22 — Pitfall 19 gate)

    The four generated HTMLs were opened in this order and read sequentially:

    1. pitch.html
    2. technical-brief.html
    3. presentation.html
    4. meeting-prep.html

    **By H1 alone, type was identifiable for:** `<all four | three of four: ___ couldn't be told from H1 | only two: ___>`.
    **By section structure, type was identifiable for:** `<all four | ___ failed>`.
    **By tone, type was identifiable for:** `<all four | ___ tone leaked>`.
    **By content density, type was identifiable for:** `<all four | ___>`.

    **Pitch vs meeting-prep (the highest-risk pair — both Overview):** `<distinct | felt-the-same: details>`.

    **Pitfall 19 disposition:** `<NONE — all four read as distinct types | SURFACED on <type>: details + recommended fix in plan 03-02>`.

    ## Phase-3 contracts verified

    - **DOC-01 (pitch):** `<verified | failed>` — pitch fixture ran end-to-end as Overview format.
    - **DOC-03 (technical brief):** `<verified | failed>` — tech-brief fixture ran end-to-end as Handbook format.
    - **DOC-04 (presentation):** `<verified | failed>` — presentation fixture ran end-to-end as Presentation format with scroll-snap (or `:target` fallback).
    - **DOC-05 (meeting prep):** `<verified | failed>` — meeting-prep fixture ran end-to-end as Overview format.
    - **DOC-06 (uniform schema):** `<verified | failed>` — all 5 interview files (handbook + 4 new) passed Rule 5 schema check on every audit invocation.
    - **DOC-07 (≤5 questions):** `<verified — all 5 interview files have 5 questions or fewer | failed>`.
    - **DESIGN-04 (auto-format selection):** `<verified — D3-01 mapping landed correct format on every fixture | failed>`.

    ## Wildcard harvester live test (plan 03-03 closure)

    - `.slide` class accepted by audit allowlist: `<yes/no>` (came from `formats/presentation.html`).
    - `.slide-counter` class accepted: `<yes/no>`.
    - `.slide-nav` class accepted: `<yes/no>`.
    - Empty-glob guard would fail-loud if `formats/` were empty: `<theoretical, not tested live | tested by deleting a temp formats dir>`.

    ## Adversarial audit smoke test

    - Injected hex literal `style="color:#ff0000"` into pitch.html copy: audit exit `<1 | 0>`.
    - Stderr message present: `<yes | no>`.

    ## Deviations and follow-ups

    Anything observed but accepted, or flagged for Phase 4:

    - `<example: "Presentation fixture's H1 wraps to 2 lines on a 1280-wide viewport at 80px Inter — visually fine but flag for V2 typography revisit">`.
    - `<example: "Audit's Rule 5 took ~80ms across 5 interview files — well under the 500ms budget; re-measure if V2 ships a 6th type">`.
    - `<example: "the kebab-case normalization in SKILL.md Step 2 worked first try for both `technical brief` and `meeting prep` — confirms plan 03-01's Step 3 substitution is correct">`.

    ## Phase 4 hand-off notes

    What Phase 4 needs to know:

    1. **All five doc types are working end-to-end on the LOCAL install.** Phase 4 LAUNCH-01 verifies the curl-pipe-bash flow against the LIVE URL — the same fixtures should be re-run there as part of LAUNCH-02 to prove cross-machine reproducibility.
    2. **Audit auto-grows.** New format skeletons under `formats/*.html` are picked up automatically. Phase 4 won't add formats, but if V2 does, no audit script change.
    3. **Rule 5 is the schema-drift moat.** If V2 ships a 6th interview file, Rule 5 enforces it follows DOC-06 — drift surfaces at the next audit run.
    4. **The Pitfall 19 mitigation is content-shaped, not mechanical.** If a future plan converges two interview files, only sequential-read catches it. Plan 03-04's pattern (one notes file capturing all type runs in one document) is the right shape for re-running this gate.
    5. **README target audience confirmed:** the meeting-prep fixture's "Demo run-through with Delfi" subject grounds Phase 4 DOCS-03 (README written for Delfi).

    ## Sign-off

    Verifier reply (Task 2 resume signal): `<paste the literal reply: "approved" | "approved with notes: ___" | "failed: <type>: <reason>">`.

    If `failed`: orchestrator routes back to the relevant Wave-1 plan for revision.
    - `failed: presentation: snap broken in Safari` → plan 03-01 (re-spike).
    - `failed: <type>: feels like clone of <other-type>` → plan 03-02 (rewrite questions).
    - `failed: <type>: audit rejected <class>` → plan 03-03 (harvester broken).
    - `failed: <type>: format mismatch (got X, expected Y)` → plan 03-01 Step 5b (decision tree broken) OR plan 03-02 (interview's expected section count drifted from the type's typical mapping).
    ```

    Fill in every `<placeholder>` with real data from the verifier's reply. If the reply was concise, ask one follow-up question to gather the missing data points. The file MUST contain zero `<placeholder>` strings after the fill — the verify block greps for unfilled placeholders.
  </action>
  <behavior>
    - File `.planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md` exists.
    - File contains a per-type section for each of the four Phase-3 doc types (Pitch, Technical brief, Presentation, Meeting prep).
    - Each per-type section documents: filename, file size, Step 5b output line, arc-row count, self-review fixes, audit exit, audit retries, visual diff disposition (or visual-gate rubric for presentation).
    - File contains a "Sequential-read check" section that documents the D3-22 disposition (PASS = all four distinct; FAIL = which two converged).
    - File contains a "Phase-3 contracts verified" section with disposition for each of DOC-01, DOC-03, DOC-04, DOC-05, DOC-06, DOC-07, DESIGN-04.
    - File contains a "Wildcard harvester live test" section confirming `.slide`-prefix classes are in the allowlist.
    - File contains an "Adversarial audit smoke test" section.
    - File contains a "Sign-off" section with the verifier's literal reply (`approved`, `approved with notes: <text>`, or `failed: <type>: <reason>`).
    - File contains zero unfilled `<placeholder>` strings.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF '## Per-type runs' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF '### Pitch' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF '### Technical brief' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF '### Presentation' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF '### Meeting prep' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF '## Sequential-read check' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF '## Phase-3 contracts verified' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF '## Wildcard harvester live test' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF '## Adversarial audit smoke test' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF '## Sign-off' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF '## Phase 4 hand-off notes' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'DOC-01' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'DOC-03' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'DOC-04' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'DOC-05' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'DOC-06' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'DOC-07' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'DESIGN-04' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qE '202[0-9]-[0-9]{2}-[0-9]{2}-.+-pitch\.html' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qE '202[0-9]-[0-9]{2}-[0-9]{2}-.+-technical-brief\.html' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qE '202[0-9]-[0-9]{2}-[0-9]{2}-.+-presentation\.html' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qE '202[0-9]-[0-9]{2}-[0-9]{2}-.+-meeting-prep\.html' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qE 'Format: (overview|handbook|presentation)' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      command grep -qE 'approved|approved with notes|failed:' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      ! command grep -qE '<actual-filename|<actual count>|<bytes>|<count>|<yes/no>|<yes \| no>|<actual line>|<paste the literal reply' .planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/03-remaining-four-doc-types/fixture/FIXTURE-NOTES.md` exists.
    - File contains the four per-type sections (Pitch, Technical brief, Presentation, Meeting prep) with documented filename, audit exit, format selection, retry count, visual disposition.
    - File contains the Sequential-read check section with the D3-22 disposition.
    - File contains the Phase-3 contracts verified section enumerating DOC-01, DOC-03, DOC-04, DOC-05, DOC-06, DOC-07, DESIGN-04.
    - File contains the Wildcard harvester live test section (plan 03-03 closure).
    - File contains the Adversarial audit smoke test section.
    - File contains the Phase 4 hand-off notes section.
    - File contains the Sign-off section with the verifier's literal Task 2 reply.
    - File contains the four actual generated filenames (regex: `202[0-9]-[0-9]{2}-[0-9]{2}-.+-(pitch|technical-brief|presentation|meeting-prep)\.html` for each type).
    - File contains `Format: <name>` lines documented from each fixture.
    - File contains ZERO unfilled `<placeholder>` slots.
  </acceptance_criteria>
  <done>
    `FIXTURE-NOTES.md` exists with four per-type runs documented, the sequential-read disposition recorded, all Phase-3 contracts mapped to disposition (verified / failed), the wildcard harvester live test confirmed, the adversarial smoke test confirmed, and the verifier's sign-off captured. All placeholders filled with real data from the Task 2 checkpoint reply.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working-copy `skill/` → local install at `~/.claude/skills/deshtml/` | Bypasses `bin/install.sh` for fixture purposes (Phase 4 LAUNCH-01 re-tests the curl-pipe-bash flow). The `cp -R` is a clean snapshot of the current working copy. |
| Running skill (4 invocations) → user's CWD | Each fixture writes a single `.html` file to the verifier's chosen working directory. Recommended `/tmp/deshtml-fixture/` per the fixture-input files. |
| Fixture artifacts → git | Reproducible inputs (8 fixture-input files + FIXTURE-NOTES.md) are committed. Generated HTML files are NOT — they are single-run artifacts in the verifier's CWD. |
| Sequential-read check → human judgment | D3-22 is content-shaped, not mechanical. The verifier's "PASS = distinct" or "FAIL = converged" disposition is a human signal that no automated check can replicate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-28 | Tampering | The local install at `~/.claude/skills/deshtml/` drifts between Wave 1 completion and the fixture run | mitigate | Task 1 explicitly does `rm -rf && cp -R` so the install is a clean snapshot. The verify block checks for Wave-1 markers (Step 5b in SKILL.md, presentation.html, four interview files, format_skels= in audit/run.sh). |
| T-03-29 | Tampering | The verifier rubber-stamps a sequential-read FAIL by typing `approved` instead of investigating Pitfall 19 (D3-22 acceptance theatrics) | mitigate | The Task 2 checkpoint instructions explicitly enumerate failure modes (`failed: <type>: feels like clone of <other-type>`). FIXTURE-NOTES.md's "Pitfall 19 disposition" line forces an explicit recording — rubber-stamping leaves a paper trail. |
| T-03-30 | Tampering | The Presentation fixture passes audit but fails the Safari scroll-snap test, and the verifier reports `approved` anyway | mitigate | Task 2's "Browser matrix" section calls out Safari as PRIMARY VERIFICATION. FIXTURE-NOTES.md template forces recording of the Safari scroll-snap result. The `Spike outcome:` line in presentation.html is the cross-check — if the file says `PASS` but Safari is broken in the fixture, the spike was a false PASS and the verifier is forced to choose between rerunning the spike (return `failed`) or accepting the false PASS (with-notes documentation). |
| T-03-31 | Information Disclosure | Generated HTML contains user input from the interview verbatim, including any sensitive content | accept | The user controls what they paste into the interview. Output goes to user's CWD only (no upload, no telemetry). PROJECT.md "Out of Scope" excludes telemetry. User responsibility (carry-over from T-02-23). |
| T-03-32 | Tampering | A future "improvement" reduces the fixture run from four types to one ("we already proved it works on handbook in Phase 2") | mitigate | The fixture set IS the gate for Phase 3 ROADMAP success criterion #4 ("none reads like a type-labeled clone"). Reducing the set defeats Pitfall 19 mitigation. The four interview files exist; running fewer than four is artificial. The plan's `files_modified` list enumerates all 8 fixture-input files; reducing the count fails plan acceptance at Task 1. |
| T-03-33 | Spoofing | The verifier types `presentation` at Step 2 and the skill loads `interview/presentation.md` BUT inadvertently runs the handbook flow because the install was stale | mitigate | Task 1 verify block confirms the four new interview files exist in the local install. Task 2 Step 4 instructions explicitly say `Reply: presentation` and check the `Format: presentation` line is printed — the format is the empirical signal that the right routing fired. |
| T-03-34 | Denial of Service | Total fixture-run time exceeds the 15-20 min budget, verifier abandons mid-run | mitigate | The four fixtures are independent; if budget runs out, the verifier can run remaining fixtures in a follow-up session and update FIXTURE-NOTES.md incrementally. Each fixture's pre-flight is the same `cp -R` install confirmation; restart is safe. |
| T-03-35 | Tampering | A future plan removes the sequential-read check section from FIXTURE-NOTES.md "for brevity" | mitigate | Task 3's verify block greps for `## Sequential-read check`. Removing the section fails plan acceptance. |
| T-03-36 | Repudiation | Future maintainer reads `approved` in FIXTURE-NOTES.md but cannot reproduce the fixture run | mitigate | The 8 fixture-input files are committed (interview answers, expected arcs, including the visual-gate rubric for presentation). Local-install command (`cp -R`) is documented in Task 1. Reference HTMLs (pm-system.html, bnp-overview.html) are at fixed paths in the user's working tree. Reproduction is deterministic. |
| T-03-37 | Elevation of Privilege | n/a (markdown / fixture artifacts only; no executable code added) | n/a | The plan modifies only markdown files plus the local install (which mirrors the working copy). No new permissions surface. |
</threat_model>

<verification>
All three task `<verify>` blocks must pass. Specifically:

- Task 1: 8 fixture-input files exist (4 types × 2 files), local install at `~/.claude/skills/deshtml/` mirrors the working copy with all Wave-1 outputs, Wave-1 markers (Step 5b in SKILL.md, presentation.html, four interview files, format_skels= in audit/run.sh) all present.
- Task 2 (human-verify checkpoint): the verifier replies `approved` (or `approved with notes: <text>`). A `failed: <type>: <reason>` reply routes back to the relevant Wave-1 plan for revision (presentation/snap → 03-01; interview-clone → 03-02; harvester-broken → 03-03).
- Task 3: `FIXTURE-NOTES.md` exists with all required sections (per-type runs, sequential-read, contracts verified, wildcard harvester, adversarial smoke test, sign-off), four actual generated filenames documented, `Format: <name>` lines per type recorded, zero unfilled placeholders.

Phase 3 ROADMAP success criteria (1-4) are verified holistically by Task 2's checkpoint:

- Criterion 1 (each doc type has its own interview file, ≤5 questions): mechanical verification by plan 03-02 + Rule 5 from plan 03-03; empirical verification at Step 2/3/4/5 of the checkpoint when each fixture's interview runs.
- Criterion 2 (format auto-selects per the D3-01 rule): empirical verification at the `Format: <name>` print line for each fixture.
- Criterion 3 (Presentation renders correctly with scroll-snap, anchor nav, CSS-only counter, in Chrome AND Safari): Step 4 of the checkpoint — PRIMARY VERIFICATION. Failures here route back to plan 03-01 (re-spike).
- Criterion 4 (none reads like a type-labeled clone): Step 6 of the checkpoint (D3-22 sequential read). Failures here route back to plan 03-02 (rewrite the converged interview's questions).
</verification>

<success_criteria>
1. **All five doc types work end-to-end on the local install.** Pitch / handbook (verified Phase 2; re-confirmed) / technical-brief / presentation / meeting-prep all run from `/deshtml` to printed-absolute-path. Phase 3 ROADMAP success criteria 1-4 closed.
2. **Format auto-selection is mechanical and correct.** The D3-01 mapping lands the expected format for each fixture: pitch → overview, technical-brief → handbook, presentation → presentation (forced), meeting-prep → overview. The `Format: <name>` print line is the only user signal — visible BEFORE rendering.
3. **Audit accepts every Phase-3 output.** The wildcard harvester (plan 03-03) picks up `.slide`, `.slide-counter`, `.slide-nav` from `formats/presentation.html`. Rule 5 silently passes on all 5 interview files. No false positives, no false negatives.
4. **Visual fidelity proven against Caseproof references where they exist.** Pitch + meeting-prep match `bnp-overview.html`. Technical-brief matches `pm-system.html`. Presentation passes the written rubric.
5. **Pitfall 19 mitigated empirically.** Sequential-read check confirms each of the four outputs reads as a distinct doc type. Pitch + meeting-prep (both Overview) feel different — one asks for a yes/no, one walks a meeting.
6. **Pitfall 21 mitigated.** The visual-gate fixtures' format auto-selection lands the expected format on every type. If a future fixture's content drifts the section count, the user sees the `Format: <name>` line and can fix the arc to flip the format. No `--format=` flag needed for V1.
7. **Single-source FIXTURE-NOTES.md is the Phase-4 hand-off.** Phase 4 maintainers read one file, not four, to understand the Phase-3 fixture results. The reproducibility pattern (per-type interview-answers.md + expected-arc.md + one consolidated notes file) is the template Phase 4 LAUNCH-02 follows when re-running fixtures against the live URL.
8. **Phase 3 closes; Phase 4 unblocked.** All 6 Phase-3 requirements (DOC-01, DOC-03, DOC-04, DOC-05, DOC-06, DESIGN-04) move to "Complete" in REQUIREMENTS.md traceability. Phase 4 inherits a working five-doc-type skill; only LAUNCH/DOCS/SKILL-02 remain.
</success_criteria>

<output>
After completion, create `.planning/phases/03-remaining-four-doc-types/03-04-SUMMARY.md` documenting:
- The verifier's final reply (approved / approved with notes / failed) with timestamp.
- Reference to `fixture/FIXTURE-NOTES.md` (the empirical record — do not duplicate; this SUMMARY is a brief).
- Phase 3 ROADMAP success criteria 1-4 mapped to checkpoint steps that verified each.
- The four format auto-selections empirically observed: pitch=overview, technical-brief=handbook, presentation=presentation, meeting-prep=overview. Each `Format: <name>` line was printed BEFORE rendering, matching D3-01.
- The Pitfall 19 disposition from the sequential-read check (NONE — all four read as distinct types; OR SURFACED on `<type>` with documented fix in plan 03-02).
- The wildcard harvester live test result (plan 03-03 closure): `.slide`-prefix classes accepted at audit time.
- The Rule 5 schema-drift check live test (plan 03-03 closure): all 5 interview files passed at audit time.
- The Presentation Safari scroll-snap result — primary signal for Phase 3 (PASS / FAIL with which step / FALL-BACK from spike if applicable).
- Any deviations the verifier accepted with notes, with rationale.
- Any violations the verifier flagged for re-planning (route to which Wave-1 plan).
- What Phase 4 inherits: all 5 doc types working end-to-end on the local install; the four Phase-3 fixtures are reproducible inputs for LAUNCH-02 cross-machine re-runs. The `cp -R` install pattern continues to bypass the curl-pipe-bash installer; LAUNCH-01 retests against the live URL.
- What Phase 4 must verify against the LIVE URL that this plan did NOT verify locally: cross-machine reproducibility of all four fixtures, the curl-pipe-bash installer flow, the GitHub release tag resolution, the README-as-only-doc-needed user journey (Delfi opens README, runs install, runs `/deshtml`, gets a Caseproof handbook).
</output>
</content>
