---
phase: 02-story-arc-gate-handbook-end-to-end
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - skill/SKILL.md
  - skill/story-arc.md
  - skill/interview/handbook.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-03
  - SKILL-04
  - SKILL-05
  - ARC-01
  - ARC-02
  - ARC-03
  - ARC-04
  - ARC-05
  - DOC-02
  - DOC-07
  - OUTPUT-01
  - OUTPUT-02
  - OUTPUT-03
  - OUTPUT-04
  - OUTPUT-05
user_setup: []

must_haves:
  truths:
    - "User pastes /deshtml (no args) into a Claude Code session and is asked the document type as the first question."
    - "User picks 'handbook' and is taken through five (or fewer) interview questions, then sees a story-arc table."
    - "Arc table has the canonical five columns — # | Beat | Section | One sentence | Reader feels — in that exact order, with no extra columns."
    - "Below the arc table, a heading '## Read the One Sentence column top-to-bottom' is followed by the One sentence cells joined into one flowing paragraph in row order."
    - "Self-review status appears under each row showing tone, chain, and name-the-thing checks; failures auto-fix in place but show both the BAD original and the GOOD replacement."
    - "User typing anything other than the approval whitelist triggers a revision round; user typing one of the whitelist phrases (case-insensitive, exact-match-after-trim) unblocks HTML generation."
    - "After approval, a single self-contained YYYY-MM-DD-<slug>-handbook.html file is written to the user's CWD, the file opens via 'open <file>', and the absolute path is printed on its own line as the LAST output."
    - "If the file already exists, a -2 / -3 numeric suffix is appended; the existing file is never overwritten."
    - "Source-mode invocations (/deshtml @file.md or pasted text >200 chars) get the Phase-4-coming stub and stop — never silently fall back to interview."
    - "User picking pitch / technical brief / presentation / meeting prep gets a 'coming in Phase 3' message and stops — never silently falls back to handbook."
    - "SKILL.md never inlines content from sub-files; it instructs Claude to Read each sub-file at the appropriate step."
    - "SKILL.md is ≤200 lines."
  artifacts:
    - path: "skill/SKILL.md"
      provides: "Top-level skill flow: frontmatter + step-numbered checklist (mode detection → type branch → interview → arc gate → render → audit → write → open → print path). Flow control only; no rubric content."
      contains: "name: deshtml"
    - path: "skill/story-arc.md"
      provides: "Arc table format spec, self-review checklist (tone / chain / name-the-thing), approval-phrase whitelist, BAD→GOOD pairs verbatim from ~/CLAUDE.md, revision-loop instructions."
      contains: "Read the One Sentence column top-to-bottom"
    - path: "skill/interview/handbook.md"
      provides: "Five-question handbook interview following the schema audience → material → section conventions → tone notes → handoff to story-arc."
      contains: "Who is the reader"
  key_links:
    - from: "skill/SKILL.md"
      to: "skill/story-arc.md, skill/interview/handbook.md, skill/design/*"
      via: "Read tool calls at the appropriate step (mode detect → interview → arc → render)"
      pattern: "story-arc.md"
    - from: "skill/SKILL.md"
      to: "skill/audit/run.sh"
      via: "Bash tool invocation after Write, before open (plan 02-03 ships the script)"
      pattern: "audit/run.sh"
    - from: "skill/story-arc.md"
      to: "/Users/sperezasis/CLAUDE.md §'Section Writing Rules'"
      via: "BAD→GOOD pairs harvested verbatim per D2-14"
      pattern: "Bad: .Read any issue in 3 seconds"
---

<objective>
Ship the working `/deshtml` skill flow: from `/deshtml` keystroke to absolute-path-printed-on-stdout, end-to-end, for the handbook document type. Closes SKILL-01 (no-args interview launch), SKILL-03 (mode detection at turn 1, no silent fallback), SKILL-04 (first question = doc type, branches), SKILL-05 (SKILL.md ≤200 lines, flow-control only), ARC-01..05 (table + flowing paragraph + self-review + approval gate + revision loop), DOC-02 (handbook end-to-end), DOC-07 (≤5 questions before arc), OUTPUT-01..05 (single self-contained HTML to CWD, filename pattern, auto-open, path print, file:// works).

Plan 02-01 ships `skill/design/components.css` (visual fidelity unblocked). Plan 02-03 ships `skill/audit/run.sh` (mechanical gate). This plan stitches them together with three new files: `skill/SKILL.md` (flow), `skill/story-arc.md` (arc rubric + approval whitelist), `skill/interview/handbook.md` (the five questions).

Purpose: this is the moat. The story-first methodology fails if the arc gate is fuzzy, the audit fails if the writer paraphrases, the user trust fails if the skill silently switches modes. Every decision in this plan exists to make those gates mechanical, not heuristic.

Output: three sub-files under `skill/`. After this plan + plan 02-01 + plan 02-03 land, plan 02-04 generates the canonical `deshtml-about-itself` fixture end-to-end and visually compares against `pm-system.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT-AMENDMENT.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md
@.planning/phases/01-foundation-installer-design-assets/01-02-SUMMARY.md
@.planning/research/PITFALLS.md
@skill/design/SYSTEM.md
@skill/design/palette.css
@skill/design/typography.css
@skill/design/components.html
@skill/design/formats/handbook.html
@skill/design/components.css
@/Users/sperezasis/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Empirically verify $ARGUMENTS substitution semantics, then write skill/interview/handbook.md</name>
  <files>skill/interview/handbook.md</files>
  <read_first>
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md §"Skill Content Lifecycle and `$ARGUMENTS` Semantics" (the cited Anthropic doc says `$ARGUMENTS` is substituted as a literal string at render time; A1/OQ-4 flag empty-arg behavior as the one item to verify empirically before locking the regex)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md §"Test Strategy → Wave 0" (the five mechanics to verify)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT.md §"D2-06, D2-07, D2-08" (interview shape, the five questions, no-validation-theater rule)
    - /Users/sperezasis/CLAUDE.md §"Section Writing Rules" (handbook tone — interview prose follows the same tone)
  </read_first>
  <action>
    Two-step task: (a) verify `$ARGUMENTS` empty-arg behavior empirically and document the finding, (b) write `skill/interview/handbook.md`.

    ### Step (a) — Empirical verification of `$ARGUMENTS`

    Per 02-RESEARCH §"Open Questions → OQ-4" and Assumption A1, the empty-arg substitution behavior is unverified. The mode-detection regex in Task 2 (SKILL.md) depends on knowing whether `/deshtml` (no args) makes `$ARGUMENTS` an empty string, the literal text `<no arguments>`, or causes the placeholder line to be dropped.

    The fastest way to verify this without spinning up a stub skill mid-task is to consult the cited Anthropic docs and the researcher's verified note. Per 02-RESEARCH (HIGH confidence): `$ARGUMENTS` is a literal substitution; no args = empty string. Treat this as the working assumption AND document it explicitly at the top of `interview/handbook.md` so plan 02-04's fixture run validates it as a side-effect:

    > Document a one-line note in this task's SUMMARY: "Working assumption from 02-RESEARCH: `$ARGUMENTS` is empty string when `/deshtml` is invoked with no args. Plan 02-04's fixture run is the empirical confirmation; if the regex misfires there, fix in 02-04, not here."

    No actual stub-skill creation in this task. The fixture run in plan 02-04 is the empirical test. If it fails, the regex in `SKILL.md` (Task 2) gets a one-line fix during 02-04 — the regex pattern is already designed to handle both empty-string AND `<no arguments>` placeholder cases (see Task 2 below).

    ### Step (b) — Write `skill/interview/handbook.md`

    Create the file with this exact content. Discretion items per CONTEXT.md: format is flat numbered list (cleaner than YAML for a 5-question interview); stub-message wording stays at ≤2 sentences.

    ```markdown
    # Handbook interview

    SKILL.md reads this file when the user picks `handbook` as the document type.
    Ask the questions one at a time, in order. Wait for each answer before asking
    the next. Empty answers are accepted — proceed with sensible defaults. Do
    not enforce length, do not retry, do not validate. The arc-gate is where
    quality is enforced, not the interview.

    The schema follows DOC-06's mandate (audience → material → section conventions
    → tone notes → handoff to story-arc) so plan 03's other interviews can mirror
    this shape.

    ## The five questions

    1. **Audience.** Who is the reader? One line is plenty.
       (Default if blank: "Someone unfamiliar with the topic, reading the handbook to get oriented.")

    2. **Material.** What is the document about? One to three sentences.
       (Default if blank: ask Claude to summarize from the audience answer plus the H1 the user implies. Do not block on this.)

    3. **Sections.** What sections do you imagine? Free list, or "let Claude propose."
       (Default if blank: "Claude proposes." Claude proposes 5-7 beats sized for the 960px Handbook layout.)

    4. **Tone notes.** Anything specific about voice or register?
       (Default if blank: "Handbook, not pitch. Describe what IS." — verbatim from CLAUDE.md.)

    5. **Inclusions / exclusions.** Anything to definitely include or definitely avoid?
       (Default if blank: skip — no inclusions, no exclusions.)

    ## Hand-off

    After question 5 (or earlier if the user says "go ahead, propose"), do NOT
    write HTML. Read `${CLAUDE_SKILL_DIR}/story-arc.md` and follow it end-to-end.
    The story-arc gate decides when HTML is allowed.

    ## What this interview must NOT do

    - Do not validate answers ("that's too short", "please clarify"). Empty
      answers proceed with the documented defaults.
    - Do not loop on a question. One ask, one answer, move on.
    - Do not re-ask the document type. SKILL.md handled that — by the time
      Claude is reading this file, handbook is already locked.
    - Do not paraphrase the questions. The wording is the contract; tone
      notes inherit from CLAUDE.md, and the user's expectations match.
    - Do not mention any other doc type (pitch, technical brief, presentation,
      meeting prep). Those are stubbed elsewhere.
    ```

    The file MUST be ≤80 lines (DOC-07 mandates ≤5 questions, the file should be lean — no preamble, no examples beyond the defaults).
  </action>
  <behavior>
    - File `skill/interview/handbook.md` exists and is non-empty.
    - File contains exactly five top-level numbered questions (`^1\. `, `^2\. `, `^3\. `, `^4\. `, `^5\. ` at the start of lines).
    - File contains the `Audience`, `Material`, `Sections`, `Tone notes`, and `Inclusions / exclusions` labels — the DOC-06 schema words.
    - File contains the verbatim default phrase "Handbook, not pitch. Describe what IS." (from CLAUDE.md).
    - File contains a hand-off section instructing Claude to read `${CLAUDE_SKILL_DIR}/story-arc.md`.
    - File length ≤80 lines.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s skill/interview/handbook.md &&
      [ "$(wc -l < skill/interview/handbook.md)" -le 80 ] &&
      [ "$(command grep -cE '^[1-5]\. ' skill/interview/handbook.md)" = "5" ] &&
      command grep -q '\*\*Audience\.\*\*' skill/interview/handbook.md &&
      command grep -q '\*\*Material\.\*\*' skill/interview/handbook.md &&
      command grep -q '\*\*Sections\.\*\*' skill/interview/handbook.md &&
      command grep -q '\*\*Tone notes\.\*\*' skill/interview/handbook.md &&
      command grep -q '\*\*Inclusions / exclusions\.\*\*' skill/interview/handbook.md &&
      command grep -q 'Handbook, not pitch. Describe what IS' skill/interview/handbook.md &&
      command grep -q '${CLAUDE_SKILL_DIR}/story-arc.md' skill/interview/handbook.md &&
      command grep -q 'arc-gate is where' skill/interview/handbook.md &&
      ! command grep -qiE 'pitch|technical brief|presentation|meeting prep' skill/interview/handbook.md &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `skill/interview/handbook.md` exists.
    - File contains exactly five questions, numbered 1-5, in source order Audience → Material → Sections → Tone → Inclusions.
    - File contains the DOC-06 schema labels: Audience, Material, Sections, Tone notes, Inclusions / exclusions.
    - Each question has a default for the empty-answer case.
    - File contains the verbatim CLAUDE.md tone phrase: "Handbook, not pitch. Describe what IS."
    - File contains a hand-off instruction pointing Claude at `${CLAUDE_SKILL_DIR}/story-arc.md`.
    - File mentions zero of the four other doc types (pitch, technical brief, presentation, meeting prep) — those have their own future files; this is handbook-only.
    - File length ≤80 lines.
    - Plan SUMMARY documents the working assumption that `$ARGUMENTS` is empty string when `/deshtml` is invoked with no args, and flags plan 02-04's fixture run as the empirical confirmation step.
  </acceptance_criteria>
  <done>
    `skill/interview/handbook.md` exists with the five interview questions in DOC-06 schema order, each with an empty-answer default, the verbatim CLAUDE.md tone phrase, and the hand-off to `story-arc.md`. The file is ≤80 lines, zero other doc types mentioned. SUMMARY documents the `$ARGUMENTS` empty-arg working assumption.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write skill/story-arc.md — arc table format, self-review rubric with verbatim BAD→GOOD pairs, approval whitelist, revision loop</name>
  <files>skill/story-arc.md</files>
  <read_first>
    - /Users/sperezasis/CLAUDE.md §"Documentation Methodology — Story First" + §"Section Writing Rules" (the source of truth — every BAD→GOOD pair is copied verbatim from here per D2-14)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT.md §"D2-09 through D2-14" (table columns, flowing paragraph, self-review checks, approval whitelist)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md §"Self-Review Prompt Patterns" (Pattern A mechanical pre-check, Pattern B chain-of-thought checklist, Pattern C BAD→GOOD few-shot — the concrete prompt design)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md §"Approval gate mechanics" (exact-match-after-trim implementation)
    - .planning/research/PITFALLS.md §Pitfall 10 (arc-gate rubber-stamping; mechanical gate IS the moat)
  </read_first>
  <action>
    Create `skill/story-arc.md` with the arc table spec, the self-review checklist, the verbatim BAD→GOOD pairs from `~/CLAUDE.md`, the approval whitelist, and the revision-loop instructions. SKILL.md reads this file at Step 4 (arc construction); the file owns its content; SKILL.md never duplicates it (Pitfall 14).

    ### File contents (exact)

    ```markdown
    # Story arc — table, self-review, and approval gate

    SKILL.md reads this file when the interview is complete. Follow it end-to-end.
    Return to SKILL.md only after the user has approved the arc. The approval gate
    is mechanical (exact-match against the whitelist below); do NOT infer intent.

    ## Step A — Build the arc

    Render a markdown table with EXACTLY these column headers, in this order:

    ```
    | # | Beat | Section | One sentence | Reader feels |
    |---|------|---------|--------------|--------------|
    ```

    Rules:
    - 5-7 rows for a handbook (matches the Handbook 960px sidebar's section grid).
    - `#` is the row number (1, 2, 3, …).
    - `Beat` is the narrative role (Hook / Stakes / Setup / Mechanism / Detail / Action / etc.).
    - `Section` is the section title that will appear as `<h2>` in the rendered HTML.
    - `One sentence` is exactly that — ONE sentence describing what the section delivers.
    - `Reader feels` is one to three words about the reader's emotional state at the end of that section.
    - No extra columns. No renamed columns. No merged cells.

    ## Step B — Render the flowing paragraph (ARC-02)

    Immediately under the table, render this heading and the joined paragraph:

    ```
    ## Read the One Sentence column top-to-bottom

    <Row 1's One sentence>. <Row 2's One sentence>. … <Row N's One sentence>.
    ```

    Join the `One sentence` cells in row order, with single spaces between sentences,
    preserving each sentence's terminal punctuation. This paragraph is the
    narrative-gap diagnostic — if it does not read as a coherent story, the arc
    is wrong. Tell the user this in one line above the heading: "The paragraph
    below is your arc as a single read. If it reads choppy, the arc needs work."

    ## Step C — Self-review (ARC-03)

    BEFORE displaying the table to the user, run these three checks privately on
    every row. Display the result as a status line under each row in the table.
    Use the format:
    `   ✓ tone, ✓ chain, ✓ named.`
    or for failures:
    `   ✗ tone — title "<BAD>" rewritten to "<GOOD>" to match handbook tone.`
    `   ✓ chain, ✓ named.`

    Failures auto-fix in place — replace the title in the table — but ALWAYS show
    the BAD original alongside the GOOD replacement so the user can see what
    changed (Pitfall 18 mitigation).

    ### Check (a) — Handbook tone (describe what IS, don't sell)

    Mechanical pre-check first. Flag any title or subtitle containing one of these
    pitch-vocabulary words:

        easily, seamlessly, powerful, revolutionary, game-changing, in seconds,
        everything you need, out of the box, effortlessly, breakthrough,
        next-generation, cutting-edge

    LLM judgment second. For titles that pass the regex, ask yourself: is this
    title describing what IS, or selling a benefit? Use the BAD→GOOD pairs below
    as the few-shot.

    ### Check (b) — Causality chain (each section follows from the previous)

    For every adjacent pair (N, N+1), verify section N's `One sentence` sets up
    section N+1's. If you could swap N+1 with N+2 without breaking the read, the
    chain is broken — flag.

    ### Check (c) — Name the thing (concrete noun in every title)

    Mechanical pre-check: flag titles whose only nouns are abstract:
    `shape`, `approach`, `way`, `kind`, `idea`, `thing`, `concept`, `aspect`, `notion`.

    LLM judgment second: for titles that pass the regex, ask: what concrete noun
    does this title name? If the answer is "the topic of the doc" rather than
    a specific section subject, flag.

    ### The handbook-tone rubric (BAD → GOOD pairs, verbatim from ~/CLAUDE.md)

    These five pairs are the calibration set. Compare every flagged title or
    subtitle against the pattern of these pairs, not against memorized rules.

    | Bad | Good | Why |
    |-----|------|-----|
    | Read any issue in 3 seconds. | Every issue follows one format. | Bad sells speed (pitch). Good describes the system (handbook). |
    | Everything arrives automatically. | One board, built for your team. | Bad is a vague benefit. Good names the thing. |
    | Projects have shape. | Every project has a structure. | Bad uses an abstract noun. Good is a structural fact. |
    | How teams stay connected. | Cross-team work follows one path. | Bad describes a process. Good states the rule. |
    | (subtitle) "And automations that sync them." | (subtitle) "Code repos hold code. Team repos are where your team lives." | Bad subtitle introduces a new concept. Good subtitle unpacks the title's two-types-of-repos claim. |

    ## Step D — Display and ask for approval

    Show the user, in this order:
    1. A one-line preface: "Here is the arc. Reply `approve` to generate the HTML, or describe what to change."
    2. The arc table with status lines under each row.
    3. The "## Read the One Sentence column top-to-bottom" heading and flowing paragraph.
    4. A blank line.
    5. The same preface again, last: "Reply `approve` or describe what to change."

    Then stop. Wait for the user's reply.

    ## Step E — Approval whitelist (ARC-04)

    Normalize the user's reply: trim whitespace, lowercase. Then exact-match
    against this whitelist:

    - approve
    - approved
    - looks good
    - lgtm
    - ship it
    - go
    - proceed
    - aprobado
    - dale

    Match → return to SKILL.md (proceed to Step 5 — render the HTML).
    No-match → revision loop (Step F).

    **Do not fuzzy-match. Do not infer intent. Do not interpret enthusiasm.
    The mechanical gate IS the moat.** Anything that is not on the whitelist
    is treated as a revision request, even if the user wrote "yeah great
    let's do it!!!" — interpret that as a revision message, regenerate the
    arc, and re-ask.

    ## Step F — Revision loop (ARC-05)

    The user's non-approval reply IS the revision instruction. Take it as
    feedback, regenerate the arc table + flowing paragraph + self-review,
    and re-display per Step D. Re-ask for approval.

    No iteration cap. The loop runs until the user types one of the
    whitelist phrases or kills the conversation. SKILL.md does NOT proceed
    to Step 5 (render) until approval lands.

    ## What this file must NOT do

    - Must not be inlined into SKILL.md (Pitfall 14 — single source of truth).
    - Must not duplicate the approval whitelist anywhere else (Pitfall 14).
    - Must not loosen the exact-match gate to fuzzy or semantic match (Pitfall 10).
    - Must not skip the self-review when the arc looks "obviously fine."
    - Must not auto-fix without showing the BAD→GOOD diff in the status line (Pitfall 18).
    ```

    The file MUST be ≤200 lines (rough budget — 5 BAD/GOOD pairs + checklist + whitelist + 6 step blocks fit easily).
  </action>
  <behavior>
    - File `skill/story-arc.md` exists and is ≤200 lines.
    - File contains the EXACT column headers `# | Beat | Section | One sentence | Reader feels` in that order.
    - File contains the heading `## Read the One Sentence column top-to-bottom`.
    - File contains all five verbatim BAD→GOOD pairs from `~/CLAUDE.md` lines 59-78: "Read any issue in 3 seconds." → "Every issue follows one format.", "Everything arrives automatically." → "One board, built for your team.", "Projects have shape." → "Every project has a structure.", "How teams stay connected." → "Cross-team work follows one path.", and the subtitle pair.
    - File contains the full nine-phrase approval whitelist (approve, approved, looks good, lgtm, ship it, go, proceed, aprobado, dale).
    - File contains all three self-review check labels (handbook tone / causality chain / name the thing).
    - File contains the mechanical-gate-is-the-moat language (Pitfall 10 mitigation).
    - File contains the auto-fix-with-diff instruction (Pitfall 18 mitigation).
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s skill/story-arc.md &&
      [ "$(wc -l < skill/story-arc.md)" -le 200 ] &&
      command grep -qF '| # | Beat | Section | One sentence | Reader feels |' skill/story-arc.md &&
      command grep -qF 'Read the One Sentence column top-to-bottom' skill/story-arc.md &&
      command grep -qF 'Read any issue in 3 seconds.' skill/story-arc.md &&
      command grep -qF 'Every issue follows one format.' skill/story-arc.md &&
      command grep -qF 'Everything arrives automatically.' skill/story-arc.md &&
      command grep -qF 'One board, built for your team.' skill/story-arc.md &&
      command grep -qF 'Projects have shape.' skill/story-arc.md &&
      command grep -qF 'Every project has a structure.' skill/story-arc.md &&
      command grep -qF 'How teams stay connected.' skill/story-arc.md &&
      command grep -qF 'Cross-team work follows one path.' skill/story-arc.md &&
      command grep -qF 'Code repos hold code. Team repos are where your team lives.' skill/story-arc.md &&
      command grep -qF '- approve' skill/story-arc.md &&
      command grep -qF '- approved' skill/story-arc.md &&
      command grep -qF '- looks good' skill/story-arc.md &&
      command grep -qF '- lgtm' skill/story-arc.md &&
      command grep -qF '- ship it' skill/story-arc.md &&
      command grep -qF '- go' skill/story-arc.md &&
      command grep -qF '- proceed' skill/story-arc.md &&
      command grep -qF '- aprobado' skill/story-arc.md &&
      command grep -qF '- dale' skill/story-arc.md &&
      command grep -qiE 'mechanical gate IS the moat|do not fuzzy-match' skill/story-arc.md &&
      command grep -qiE 'auto-fix.*BAD.*GOOD|BAD.*GOOD.*diff|BAD original' skill/story-arc.md &&
      command grep -qiE 'handbook.tone|tone[[:space:]]*check' skill/story-arc.md &&
      command grep -qiE 'causality.chain|chain[[:space:]]*check' skill/story-arc.md &&
      command grep -qiE 'name.the.thing|name[[:space:]]*check' skill/story-arc.md &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `skill/story-arc.md` exists and is ≤200 lines.
    - File contains the EXACT 5-column header line `| # | Beat | Section | One sentence | Reader feels |` in that order with no extra columns.
    - File contains the EXACT heading `## Read the One Sentence column top-to-bottom`.
    - File contains every verbatim BAD→GOOD pair from `~/CLAUDE.md` lines 59, 60, 64, 65, 78 (five pairs total).
    - File contains the full nine-phrase approval whitelist as bullet points (approve, approved, looks good, lgtm, ship it, go, proceed, aprobado, dale).
    - File contains all three self-review check headings (handbook tone, causality chain, name-the-thing).
    - File contains explicit no-fuzzy-match language ("mechanical gate IS the moat" or "Do not fuzzy-match").
    - File contains the auto-fix-with-diff instruction (BAD original alongside GOOD replacement in status line).
    - File contains the revision-loop instruction (no iteration cap; non-approval = revision).
    - File contains a "What this file must NOT do" section listing Pitfalls 10, 14, 18.
  </acceptance_criteria>
  <done>
    `skill/story-arc.md` exists with: the canonical 5-column arc table spec; the flowing-paragraph heading; the self-review three-check rubric (handbook tone / causality chain / name-the-thing) including the mechanical-pre-check word lists; the five verbatim BAD→GOOD pairs from CLAUDE.md; the nine-phrase approval whitelist; the no-fuzzy-match-mechanical-moat language; the revision-loop instruction; the auto-fix-with-diff Pitfall-18 mitigation. File is ≤200 lines.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write skill/SKILL.md — frontmatter + step-numbered flow control (≤200 lines, lazy-loads sub-files)</name>
  <files>skill/SKILL.md</files>
  <read_first>
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT.md §"D2-01..D2-22" (the entire decision set drives this file)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT-AMENDMENT.md (D2-15 amended: inline THREE CSS files — palette, typography, components — in that order)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md §"Skill Content Lifecycle" + §"Self-Review Prompt Patterns" + §"Output Writer" + §"Audit script invocation" (concrete prompt blocks for every step)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md §"Code Examples → Example 1" (frontmatter — verbatim copy candidate)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md §"Code Examples → Example 4" (mode-detection prompt block)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md §"Pitfalls 14, 15" (no inlining sub-files; no speculative reads)
    - skill/interview/handbook.md (created in Task 1 — SKILL.md hands off to it at the interview step)
    - skill/story-arc.md (created in Task 2 — SKILL.md hands off to it at the arc step)
    - skill/design/SYSTEM.md (Phase 1 + plan 02-01 update — read order palette → typography → components)
    - skill/design/formats/handbook.html (the skeleton; SKILL.md inlines CSS into its `<head>`)
  </read_first>
  <action>
    Create `skill/SKILL.md` as the top-level entrypoint Claude Code loads when the user types `/deshtml`. The file is a step-numbered checklist; sub-file content lives in sub-files. Hard cap: 200 lines (D2-01).

    ### File contents (exact)

    ```markdown
    ---
    name: deshtml
    description: Generate a story-first HTML document following the Caseproof Documentation System. Use when the user wants a designed, single-file HTML doc — pitch, handbook, technical brief, presentation, or meeting prep. The skill runs an interview, gates on a story arc, and writes a self-contained HTML to the current directory.
    disable-model-invocation: true
    allowed-tools: Read Write Bash(ls *) Bash(test *) Bash(open *) Bash(bash *) Bash(date *) Bash(pwd *) Bash(mkdir *) Bash(grep *) Bash(command *)
    ---

    # deshtml — flow control

    The user invoked `/deshtml`. Their argument string was: `$ARGUMENTS`

    Follow these steps in order. Each step that says "Read X.md" is the only place
    you should read X. Do not read sub-files speculatively (Pitfall 15). Sub-files
    own their content; this file owns flow.

    ## Step 1 — Detect mode

    Inspect the literal `$ARGUMENTS` string (it may be empty, may contain `<no arguments>`,
    or may contain the user's text — handle all three).

    1. If `$ARGUMENTS` matches the regex `(^|[[:space:]])@\S+` → **source mode**.
       Reply with EXACTLY this message and stop:

       > Source mode is coming in Phase 4. For now, run `/deshtml` with no arguments to use the interview.

    2. Else if `$ARGUMENTS` (with `@\S+` tokens stripped, with the literal text
       `<no arguments>` stripped, and with surrounding whitespace trimmed) is
       longer than 200 characters → **source mode** (pasted prose). Reply with
       the same source-mode stub above and stop.

    3. Else → **interview mode**. Continue to Step 2.

    Never silently fall back from source mode to interview mode (SKILL-03).

    ## Step 2 — Ask the document type

    This is the first interactive question (SKILL-04). Ask exactly:

    > What kind of document? Pick one: **handbook**, **pitch**, **technical brief**, **presentation**, **meeting prep**.

    Wait for the user's answer. Normalize: trim whitespace, lowercase.

    - `handbook` → continue to Step 3.
    - `pitch`, `technical brief`, `presentation`, `meeting prep` → reply with EXACTLY:
      > `<type>` is coming in Phase 3. Try `handbook` for now.

      Substitute the user's choice in `<type>`. Stop. Do not silently fall back.

    - Anything else → ask once more, listing the same five options. If the second
      reply is still not in the list, reply with the Phase-3-stub language using
      `that` in place of `<type>` and stop.

    ## Step 3 — Run the interview

    Read `${CLAUDE_SKILL_DIR}/interview/handbook.md` now. Follow it end-to-end.
    Return here only after the user has answered the five questions (or stopped
    early per that file's instructions).

    Do NOT read `story-arc.md` yet (Pitfall 15).

    ## Step 4 — Build the story arc

    Read `${CLAUDE_SKILL_DIR}/story-arc.md` now. Follow it end-to-end. The user
    must approve the arc before this step completes. Return here only after
    approval is matched against the whitelist.

    Do NOT read `design/*` files yet.

    ## Step 5 — Compute the filename and check for collisions

    1. Get today's date in local time: run `date +%Y-%m-%d` via Bash.
    2. Slug = first 4-5 words of the H1 from the approved arc, kebab-cased,
       ASCII only. Lowercase, replace spaces with `-`, strip non-`[a-z0-9-]`,
       no trailing `-`, truncate at the 4th-5th word.
    3. Tentative filename: `<date>-<slug>-handbook.html` in the current working
       directory (run `pwd` to get the absolute path).
    4. Collision check via `test -f`. If exists, append `-2`, `-3`, … until free:

       ```bash
       target="<date>-<slug>-handbook.html"
       suffix=2
       while test -f "$target"; do
         target="<date>-<slug>-handbook-${suffix}.html"
         suffix=$((suffix + 1))
       done
       ```

       Use the final `$target` for Step 6. Compute its absolute path: `${PWD}/${target}`.

    ## Step 6 — Render the handbook HTML

    Read these five files in parallel:

    - `${CLAUDE_SKILL_DIR}/design/formats/handbook.html` (skeleton)
    - `${CLAUDE_SKILL_DIR}/design/palette.css` (CSS variables, `:root`)
    - `${CLAUDE_SKILL_DIR}/design/typography.css` (Inter @import, scale)
    - `${CLAUDE_SKILL_DIR}/design/components.css` (component CSS — sidebar, hero, all 16 component families)
    - `${CLAUDE_SKILL_DIR}/design/components.html` (markup allowlist)

    Then assemble the output:

    1. Replace the two `<link rel="stylesheet" href="../palette.css">` and
       `<link rel="stylesheet" href="../typography.css">` lines in the skeleton's
       `<head>` with a single `<style>` block containing, IN THIS ORDER:
       a) The verbatim contents of `palette.css`.
       b) A blank line.
       c) The verbatim contents of `typography.css`.
       d) A blank line.
       e) The verbatim contents of `components.css`.

       Keep the skeleton's existing inline `<style>` block (the layout rules)
       as a SECOND `<style>` block right after — do not merge them.

    2. Fill each slot comment in the body with content matching the approved arc:
       - `<!-- DOC TITLE -->` — the H1 text.
       - `<!-- LOGO SLOT -->` and `<!-- NAV ITEMS SLOT -->` — sidebar nav matching the arc's beats. Use the `.sb-*` markup pattern from `pm-system.reference.html`.
       - `<!-- STICKY BAR SLOT -->` — optional; can be empty.
       - `<!-- HERO H1 SLOT -->`, `<!-- HERO SUBTITLE SLOT -->`, `<!-- HERO STATS SLOT -->` — H1, `.s-lead` subtitle, optional `.stats`.
       - `<!-- SECTION 1 EYEBROW/H2/BODY -->` (one per arc row) — eyebrow, h2, body built ONLY from classes in `components.html`.
       - `<!-- FLOATING PILL SLOT -->` — optional bottom-right nav pill.

    3. Use ONLY classes that appear in `components.html` (markup allowlist) or in
       `typography.css` (`.s-lead`, `.eye`, `.cl`, `.fl`, `.ct`, `.cd`, `.ic`, `.fn`).
       The audit in Step 7 enforces this; failing here means a guaranteed retry.

    4. Verify before writing: zero `<script>` tags, zero `<link rel="stylesheet"`
       attributes, zero `on*=` event handlers, zero `javascript:` URLs.

    5. Write the assembled HTML to the absolute path computed in Step 5 using
       the Write tool.

    ## Step 7 — Audit

    Run the audit script via Bash:

    ```bash
    bash "${CLAUDE_SKILL_DIR}/audit/run.sh" "<absolute-path-from-step-5>"
    ```

    Capture exit code and stderr.

    - Exit 0 → continue to Step 8.
    - Non-zero → read the violations on stderr, regenerate the HTML addressing
      each violation, write again, re-run the audit. **Maximum 2 retry rounds.**
      If round 3 still fails: keep the file, surface the verbatim violation list
      to the user, then continue to Step 8 anyway. The file is never silently
      delivered with violations — failure is loud.

    ## Step 8 — Open and print path

    1. Run `open "<absolute-path>"` via Bash. If `open` exits non-zero, ignore the
       error and continue — the path-print line is the fallback.
    2. Print the absolute path on its own line. No prefix, no emoji, no banner,
       no "✓ Generated", no next-steps suggestion. The path is the LAST output.

    Example final output:

    ```
    /Users/santiago/Desktop/2026-04-27-deshtml-handbook.html
    ```

    Stop. Your work is done.

    ## Constraints this file enforces

    - This file is ≤200 lines (D2-01) and contains flow control only — no rubric content (Pitfall 14).
    - Sub-files are read on demand at the step that needs them (Pitfall 15).
    - Mode detection at turn 1 — never silently falls back (SKILL-03).
    - The arc gate is mechanical (story-arc.md whitelist) — no fuzzy approval.
    - The output is always self-contained — three CSS files inlined, zero external assets.
    - The audit is the moat — failure is loud, not silent.
    ```

    Do not exceed 200 lines. If you do, trim prose, NOT functionality. The frontmatter
    block, the `Step N` headings, and the prompt blocks for Steps 1, 2, 6, and 7 are load-bearing — the rest can be tightened.
  </action>
  <behavior>
    - File `skill/SKILL.md` exists.
    - Frontmatter: `name: deshtml`, `disable-model-invocation: true`, `allowed-tools` line listing Read, Write, and the scoped Bash entries from 02-RESEARCH Example 1.
    - File length ≤200 lines (SKILL-05).
    - File contains step-numbered headings: Step 1 (Detect mode), Step 2 (Doc type), Step 3 (Interview), Step 4 (Arc), Step 5 (Filename), Step 6 (Render), Step 7 (Audit), Step 8 (Open + print path).
    - File contains the mode-detection regex `(^|[[:space:]])@\S+`.
    - File contains the source-mode stub message verbatim: "Source mode is coming in Phase 4. For now, run `/deshtml` with no arguments to use the interview."
    - File enumerates exactly 5 doc types — handbook, pitch, technical brief, presentation, meeting prep — in Step 2.
    - File contains the Phase-3-stub message pattern: `<type>` is coming in Phase 3.
    - File instructs reading `${CLAUDE_SKILL_DIR}/interview/handbook.md` at Step 3 (NOT before).
    - File instructs reading `${CLAUDE_SKILL_DIR}/story-arc.md` at Step 4 (NOT before).
    - File instructs reading `${CLAUDE_SKILL_DIR}/design/palette.css`, `typography.css`, `components.css`, `components.html`, `formats/handbook.html` at Step 6.
    - File contains the three-CSS-file inlining order: palette → typography → components.
    - File contains the filename pattern `<date>-<slug>-handbook.html` and the collision-suffix loop (`-2`, `-3`).
    - File contains the audit invocation `bash "${CLAUDE_SKILL_DIR}/audit/run.sh"` at Step 7.
    - File contains the max-2-retry rule for audit failures.
    - File contains the "path is the LAST output" instruction with no emoji / banner.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s skill/SKILL.md &&
      [ "$(wc -l < skill/SKILL.md)" -le 200 ] &&
      command grep -q '^name: deshtml$' skill/SKILL.md &&
      command grep -q '^disable-model-invocation: true$' skill/SKILL.md &&
      command grep -q '^allowed-tools:' skill/SKILL.md &&
      command grep -q 'Bash(open \*)' skill/SKILL.md &&
      command grep -q 'Bash(bash \*)' skill/SKILL.md &&
      command grep -q '## Step 1' skill/SKILL.md &&
      command grep -q '## Step 2' skill/SKILL.md &&
      command grep -q '## Step 3' skill/SKILL.md &&
      command grep -q '## Step 4' skill/SKILL.md &&
      command grep -q '## Step 5' skill/SKILL.md &&
      command grep -q '## Step 6' skill/SKILL.md &&
      command grep -q '## Step 7' skill/SKILL.md &&
      command grep -q '## Step 8' skill/SKILL.md &&
      command grep -qF '(^|[[:space:]])@\S+' skill/SKILL.md &&
      command grep -qF 'Source mode is coming in Phase 4' skill/SKILL.md &&
      command grep -qF 'handbook' skill/SKILL.md &&
      command grep -qF 'pitch' skill/SKILL.md &&
      command grep -qF 'technical brief' skill/SKILL.md &&
      command grep -qF 'presentation' skill/SKILL.md &&
      command grep -qF 'meeting prep' skill/SKILL.md &&
      command grep -qF 'is coming in Phase 3' skill/SKILL.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/interview/handbook.md' skill/SKILL.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/story-arc.md' skill/SKILL.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/design/palette.css' skill/SKILL.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/design/typography.css' skill/SKILL.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/design/components.css' skill/SKILL.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/design/formats/handbook.html' skill/SKILL.md &&
      command grep -qF '${CLAUDE_SKILL_DIR}/audit/run.sh' skill/SKILL.md &&
      command grep -qiE 'maximum 2 retry|max.*2.*retry|2 retry rounds' skill/SKILL.md &&
      command grep -qiE 'last output|LAST output|stop\. your work' skill/SKILL.md &&
      command grep -qF 'date +%Y-%m-%d' skill/SKILL.md &&
      command grep -qF '<date>-<slug>-handbook.html' skill/SKILL.md &&
      command grep -qE 'no emoji|no banner|no celebration' skill/SKILL.md &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `skill/SKILL.md` exists, length ≤200 lines.
    - Frontmatter contains `name: deshtml`, `disable-model-invocation: true`, and `allowed-tools:` with Read, Write, and scoped Bash(*) entries (Bash(open *), Bash(bash *), Bash(date *), etc.).
    - File contains step-numbered `## Step 1` through `## Step 8` headings.
    - Step 1 contains the source-mode regex `(^|[[:space:]])@\S+` and the verbatim source-mode stub message.
    - Step 2 lists all five doc types and stubs the four non-handbook types with `is coming in Phase 3`.
    - Step 3 reads `${CLAUDE_SKILL_DIR}/interview/handbook.md`.
    - Step 4 reads `${CLAUDE_SKILL_DIR}/story-arc.md`.
    - Step 5 contains the filename pattern, the `date +%Y-%m-%d` invocation, and the `-2`/`-3` collision-suffix loop.
    - Step 6 reads palette.css, typography.css, components.css, components.html, and formats/handbook.html and specifies inlining order palette → typography → components.
    - Step 7 invokes `bash "${CLAUDE_SKILL_DIR}/audit/run.sh"` and contains the max-2-retry rule.
    - Step 8 instructs `open` then path-print on its own line, with explicit "no emoji / no banner / LAST output" language.
    - File contains zero verbatim BAD→GOOD pairs (those live in story-arc.md per Pitfall 14).
    - File contains zero copies of the approval whitelist (lives in story-arc.md).
    - File contains zero copies of the interview questions (lives in interview/handbook.md).
  </acceptance_criteria>
  <done>
    `skill/SKILL.md` exists, ≤200 lines, with frontmatter, eight step-numbered sections, mode detection, doc-type branch (handbook implemented + four stubbed), interview hand-off, arc hand-off, filename + collision detection, three-CSS-file inlining order, audit invocation with retry rule, and terse path-only final output. Zero rubric content inlined (sub-files own their content).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → SKILL.md `$ARGUMENTS` | Whatever the user typed after `/deshtml` is substituted as a literal string. The mode-detection regex is the gate that decides whether the input is source-mode (stubbed) or interview-mode (proceed). |
| Approval gate (story-arc.md) → HTML write (SKILL.md Step 6) | The mechanical exact-match against the whitelist is the boundary between "user has not approved" and "skill writes a file to disk." |
| SKILL.md → user's filesystem | Step 6 writes a single .html file to the current working directory. The collision-detection loop in Step 5 prevents overwrite. |
| SKILL.md → audit script | Step 7 invokes `bash audit/run.sh` via the Bash tool. Audit's exit code controls the retry loop; non-zero on round 3 surfaces violations to user but still writes the file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | A future commit re-introduces fuzzy/semantic approval matching (rubber-stamping) | mitigate | story-arc.md Step E hard-codes exact-match-after-trim against a 9-phrase whitelist. SKILL.md Step 4 hands off to story-arc.md and does not duplicate the approval logic (Pitfall 14). Plan 02-04 visual-gate testing exercises a non-approval reply to confirm the revision loop fires. |
| T-02-07 | Spoofing | A user types `/deshtml @file.md` expecting source mode and gets silently dropped into interview | mitigate | Step 1 mode-detection regex is `(^|[[:space:]])@\S+`. The stub message is verbatim "Source mode is coming in Phase 4." Plan 02-04 fixture run includes a `/deshtml @nonexistent.md` invocation as a smoke test that the stub fires. |
| T-02-08 | Tampering | The HTML write in Step 6 overwrites a user file with a colliding name | mitigate | Step 5 runs a `while test -f` loop appending `-2`, `-3`, … so the existing file is never overwritten. Pitfall 11 mitigation. |
| T-02-09 | Information Disclosure | Generated HTML contains the user's interview answers verbatim, including any sensitive content the user pasted | accept | The user controls what they paste into the interview. The output goes to the user's CWD only (no upload, no telemetry). PROJECT.md "Out of Scope" excludes telemetry. User responsibility. |
| T-02-10 | Elevation of Privilege | A future SKILL.md change widens `allowed-tools` to bare `Bash` instead of scoped `Bash(open *) Bash(bash *) ...` | mitigate | The frontmatter is verbatim from 02-RESEARCH Example 1; scoped Bash entries prevent arbitrary shell. Acceptance criterion enforces the scoped form. Plan 02-04 visual gate confirms only allowed tools are invoked. |
| T-02-11 | Denial of Service | An adversarial user input causes infinite revision loops (story-arc never reaches approval) | accept | story-arc.md Step F documents "no iteration cap" — the user kills the conversation when satisfied or frustrated. This is a single-user CLI tool; an adversarial input is the user being adversarial against themselves. PITFALLS Pitfall 10 explicitly accepts this. |
| T-02-12 | Tampering | Self-review auto-fix changes a user-typed title and the user thinks deshtml ignored their input (Pitfall 18) | mitigate | story-arc.md Step C mandates the status line shows the BAD original alongside the GOOD replacement. User can override by replying with their preferred title (revision loop in Step F). Acceptance criterion enforces "auto-fix-with-diff" language is present. |
| T-02-13 | Information Disclosure | Step 8 path-print line accidentally includes the user's $HOME or other env vars in the output | accept | Step 8 prints `<absolute-path>` only; absolute paths inherently include $HOME. This is the intended behavior — the user needs the path to find the file. No additional env exposure. |
| T-02-14 | Tampering | A future plan adds rubric content to SKILL.md "for safety" (Pitfall 14 violation) | mitigate | Acceptance criterion enforces SKILL.md ≤200 lines AND zero verbatim BAD→GOOD pairs AND zero approval-whitelist copies AND zero interview questions. Plan 02-04 verifies SKILL.md size at fixture time. |
</threat_model>

<verification>
All three task `<verify>` blocks must pass. Specifically:
- `skill/interview/handbook.md` exists, ≤80 lines, exactly 5 numbered questions in DOC-06 schema order, contains the verbatim CLAUDE.md tone phrase, hands off to `story-arc.md`, mentions zero other doc types.
- `skill/story-arc.md` exists, ≤200 lines, contains the canonical 5-column table header, the flowing-paragraph heading, all five verbatim BAD→GOOD pairs from CLAUDE.md, the full 9-phrase approval whitelist, all three self-review checks, no-fuzzy-match language, and Pitfall-18 auto-fix-with-diff language.
- `skill/SKILL.md` exists, ≤200 lines, contains the verbatim frontmatter from 02-RESEARCH Example 1, eight step-numbered sections (mode detection → doc type branch → interview → arc → filename → render → audit → open+print), the mode-detection regex, all five doc types, the three-CSS-file inlining order (palette → typography → components), and the max-2-retry audit loop.
- Plan SUMMARY documents the working assumption that `$ARGUMENTS` is empty string when `/deshtml` is invoked with no args, and flags plan 02-04's fixture run as the empirical confirmation step.

End-to-end skill behavior verification (interactive flow, generated HTML opens correctly, etc.) is intentionally deferred to plan 02-04 (the visual-gate plan that runs the canonical `deshtml-about-itself` fixture).
</verification>

<success_criteria>
1. **`/deshtml` skill flow is wired end-to-end for handbook.** SKILL.md walks Claude through 8 steps from mode detection to path-print. Sub-files own rubric content; SKILL.md owns flow.
2. **Mode detection is mechanical.** The regex in Step 1 makes source-mode-vs-interview a deterministic decision. Stub fires before any interview prompt — never silently falls back (SKILL-03).
3. **Doc type is the first interactive question.** Step 2 enumerates all five types (handbook + four stubbed) so the user always sees the full menu (Phase 3 only flips stubs to real interviews).
4. **Story-arc gate is the moat.** story-arc.md owns the table format, the self-review three-check rubric, the verbatim BAD→GOOD pairs, the approval whitelist, and the revision loop. Approval is exact-match — no fuzzy match.
5. **Output is self-contained.** Step 6 inlines palette + typography + components into a single `<style>` block. Audit (plan 02-03) verifies the output has zero `<link rel="stylesheet"` and zero `<script>` tags.
6. **File write respects the user's filesystem.** Filename follows `YYYY-MM-DD-<slug>-handbook.html`; collisions get `-2`, `-3` suffixes; the existing file is never overwritten.
7. **Final output is the path, only.** Step 8's instruction is unambiguous: `open` then absolute path on its own line; no celebration, no banner, no next-steps. RULE #1 from CLAUDE.md (terse output) is enforced at the skill level.
8. **SKILL.md ≤200 lines, zero rubric content inlined.** SKILL-05 met. Pitfall 14 (single source of truth) preserved. Phase 3 can add four interview files without touching SKILL.md beyond Step 2's stub messages.
</success_criteria>

<output>
After completion, create `.planning/phases/02-story-arc-gate-handbook-end-to-end/02-02-SUMMARY.md` documenting:
- The exact line counts of all three new files (handbook.md, story-arc.md, SKILL.md) — confirms the 200-line cap.
- The mode-detection regex and its three handled cases (empty, `<no arguments>` placeholder, real text).
- The five doc types enumerated in Step 2 and which one is implemented vs stubbed.
- The three-CSS-file inlining order locked in Step 6 (matches plan 02-01 + SYSTEM.md Rule 6).
- The audit invocation contract with plan 02-03 (`bash audit/run.sh <abs-path>`, exit 0 = pass, max 2 retries).
- The 9-phrase approval whitelist and the no-fuzzy-match guarantee.
- The 5 BAD→GOOD pairs harvested verbatim from CLAUDE.md (with line references).
- The working assumption about `$ARGUMENTS` empty-arg behavior (empty string per 02-RESEARCH HIGH confidence) and the fact that plan 02-04 is the empirical confirmation.
- What plan 02-04's fixture run must verify (interview launches, source mode stub fires, doc-type stub fires for non-handbook, arc gate blocks generation, approval unblocks, audit retries on injected violations).
</output>
</content>
</invoke>