---
phase: 02-story-arc-gate-handbook-end-to-end
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md
  - .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/expected-arc.md
  - .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md
autonomous: false
requirements:
  - SKILL-01
  - SKILL-03
  - SKILL-04
  - ARC-01
  - ARC-02
  - ARC-03
  - ARC-04
  - ARC-05
  - DOC-02
  - DOC-07
  - DESIGN-06
  - OUTPUT-01
  - OUTPUT-02
  - OUTPUT-03
  - OUTPUT-04
  - OUTPUT-05
user_setup: []

must_haves:
  truths:
    - "The skill payload is installed at ~/.claude/skills/deshtml/ and `/deshtml` is callable from a fresh Claude Code session."
    - "Running `/deshtml` (no args) launches the interview; question 1 is the doc type; picking handbook runs five questions or fewer; the arc table is then proposed."
    - "The arc table has exactly five columns in the canonical order; below it is the heading and flowing paragraph; status lines under each row report the three self-review checks."
    - "Replying with anything not on the approval whitelist triggers a revision round; replying with `approve` (or any whitelist phrase) generates the file."
    - "After approval, a single self-contained YYYY-MM-DD-deshtml-handbook.html is written to the user's CWD; `open` opens it in the default browser; the absolute path is the LAST stdout line."
    - "The generated HTML opens correctly via file:// — no broken layout, no JS errors, no missing stylesheets, no <link rel=stylesheet> remaining."
    - "Running `bash skill/audit/run.sh <generated.html>` exits 0."
    - "Source-mode invocation (`/deshtml @nonexistent.md`) returns the Phase-4 stub message and exits — never falls back to interview."
    - "Picking pitch / technical brief / presentation / meeting prep returns the Phase-3 stub message and exits — never falls back to handbook."
    - "Side-by-side diff of the generated handbook against pm-system.html in Chrome AND Safari shows no wrong color, no wrong font, no wrong spacing, no extra/missing component."
    - "iOS Safari forced-dark-mode test on the generated handbook stays light (DESIGN-07 carry-over from Phase 1, applied to generated output)."
  artifacts:
    - path: ".planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md"
      provides: "The canonical interview answers for the deshtml-about-itself fixture (D2-24). Used at the human-verify checkpoint so the same input is reproducible."
      contains: "Who is the reader"
    - path: ".planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/expected-arc.md"
      provides: "The expected story arc shape (number of beats, narrative thread, key section titles) so the human verifier knows what 'right' looks like before approving."
      contains: "deshtml is a Claude Code skill"
    - path: ".planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md"
      provides: "Notes from the fixture run: actual filename written, audit exit code, browser screenshots' provenance, any deviation from expected arc, any Wave 0 mechanics observation ($ARGUMENTS empty-arg behavior, etc.)."
      contains: "Audit exit"
  key_links:
    - from: "fixture/interview-answers.md"
      to: "skill/interview/handbook.md"
      via: "human-verify checkpoint pastes these answers into the running skill"
      pattern: "Who is the reader"
    - from: "fixture/expected-arc.md"
      to: "skill/story-arc.md"
      via: "human-verify checkpoint compares the proposed arc against this expected shape before approval"
      pattern: "5-7 beats|five|six|seven"
    - from: "fixture/FIXTURE-NOTES.md"
      to: "skill/audit/run.sh, skill/SKILL.md, skill/design/components.css"
      via: "fixture run exercises all three Wave-1 plans end-to-end"
      pattern: "audit"
---

<objective>
Run the canonical `deshtml-about-itself` fixture end-to-end through the just-installed skill, side-by-side compare the output against `pm-system.html`, and prove the Phase 2 contract holds. Closes the Phase-2 ROADMAP success criterion #2 ("the file opens correctly via file://") and acts as the empirical confirmation step for the working assumptions plan 02-02 deferred (`$ARGUMENTS` empty-arg behavior, status-line readability at 80 columns, etc.).

Purpose: Wave 1 (plans 02-01, 02-02, 02-03) ships every file the skill needs. This plan proves they work together — that the user actually gets a Caseproof-faithful handbook from a single `/deshtml` invocation. It is the only plan in Phase 2 that exercises the running skill, and the only plan with a human-verify checkpoint.

This plan is non-autonomous (`autonomous: false`) because the visual diff against `pm-system.html` is a human judgment Claude cannot make alone. Every other check is automated.

Output: a fixture directory under this phase's planning folder containing the reproducible inputs and the run notes; the actual generated HTML is in the user's CWD (recommended: somewhere outside the deshtml repo to avoid committing fixture artifacts) and is NOT checked into git. The fixture's interview answers and expected arc shape ARE checked in so future regressions can be reproduced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT-AMENDMENT.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-01-PLAN.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-02-PLAN.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-03-PLAN.md
@skill/SKILL.md
@skill/story-arc.md
@skill/interview/handbook.md
@skill/audit/run.sh
@skill/audit/rules.md
@skill/design/SYSTEM.md
@skill/design/components.css
@skill/design/references/pm-system.reference.html
@/Users/sperezasis/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write the fixture inputs (interview-answers.md, expected-arc.md) and stage the local skill install</name>
  <files>.planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md, .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/expected-arc.md</files>
  <read_first>
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md §"Test Strategy → Test fixture per D2-24" (the canonical interview answers and expected arc paragraph)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT.md §"D2-23, D2-24, D2-25" (visual contract, fixture subject, browser matrix)
    - skill/interview/handbook.md (the question set the answers map to)
    - skill/story-arc.md (the table format the expected arc follows)
  </read_first>
  <action>
    Create the `fixture/` directory under the phase folder with two reference files. These are checked into git so future regressions can be reproduced; the actual generated HTML is not.

    ### File 1: `.planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md`

    The canonical answers per 02-RESEARCH §"Test fixture per D2-24". Use this exact content:

    ```markdown
    # Fixture: deshtml-about-itself handbook — interview answers

    Reproducible inputs for the Phase 2 visual-gate fixture run (D2-24).
    The human verifier in plan 02-04 pastes these answers, in order, into
    the running `/deshtml` interview.

    ## Pre-run

    Before invoking the skill, the human verifier confirms:
    1. `~/.claude/skills/deshtml/` exists (skill is installed locally for the verification — see Task 2 below).
    2. Working directory is somewhere OUTSIDE the deshtml repo (recommended: `~/Desktop/` or `/tmp/`) so the generated HTML doesn't accidentally get committed.
    3. Claude Code session is fresh (no prior `/deshtml` history in the conversation).

    ## Invocation

    ```
    /deshtml
    ```

    (No arguments — interview mode.)

    ## Q1 — Document type

    > handbook

    Expected: skill proceeds to Step 3 (interview).

    ## Q2 — Audience

    > Someone installing deshtml for the first time. They've heard of Claude Code but haven't used skills before.

    ## Q3 — Material

    > deshtml — a Claude Code skill that turns ideas into beautifully designed HTML documents following the Caseproof Documentation System and the Story-First methodology.

    ## Q4 — Sections

    > Let Claude propose. Suggested beats if useful: What it is → How it's installed → How a run works → The story-arc gate → The design system → What ships in v1 → Known limitations.

    ## Q5 — Tone notes

    > Handbook, not pitch. Describe what IS.

    ## Q6 — Inclusions / exclusions

    > Include: the install one-liner verbatim. Include: the 5 doc types. Avoid: any "why this is great" marketing copy.

    ## After Q6 — Approval

    Skill proposes the arc. Verifier compares against `expected-arc.md`. If acceptable:

    > approve

    Verifier expects the skill to:
    1. Compute filename: `2026-04-28-deshtml-handbook.html` (or with `-2`/`-3` suffix if a collision exists).
    2. Render the HTML with palette + typography + components inlined.
    3. Run `audit/run.sh` (exit 0 expected).
    4. Run `open` on the absolute path.
    5. Print the absolute path on its own line as the LAST output.
    ```

    ### File 2: `.planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/expected-arc.md`

    The shape the proposed arc should match. Use this exact content:

    ```markdown
    # Fixture: deshtml-about-itself handbook — expected arc shape

    Reference for the human verifier in plan 02-04. The arc the skill proposes
    after the interview should resemble this in shape — section count, narrative
    thread, terminal punctuation. Exact wording will vary; that's fine. What
    matters is the flowing-paragraph diagnostic reads as one coherent story.

    ## Expected number of rows

    6-8 rows. Anything outside that range deserves scrutiny — too few means
    sections will run too long for the 960px Handbook layout; too many means
    sections are over-fragmented and the causality chain weakens.

    ## Expected flowing paragraph

    Reading the `One sentence` column top-to-bottom, the paragraph should read
    something like:

    > deshtml is a Claude Code skill that generates story-first HTML documents.
    > You install it with one curl-pipe-bash line.
    > A run starts with a five-question handbook interview.
    > The skill proposes a story arc and gates on your approval.
    > Once approved, it generates a single self-contained HTML using the Caseproof Documentation System.
    > A post-generation audit rejects design-system drift mechanically.
    > Iteration after generation happens via normal Claude conversation.

    Acceptable variations: different verbs, different connectives, different
    sentence boundaries. NOT acceptable: a paragraph that reads as bullet-points
    glued together (causality chain broken), or that introduces a concept not
    set up by an earlier sentence.

    ## Expected section titles (handbook tone)

    Should pass the three self-review checks:
    - **Tone:** every title describes what IS, not what is great. No "easily,"
      "powerful," "everything you need."
    - **Causality chain:** each title follows from the previous. "What it is"
      → "How you install" → "How a run starts" → "The arc gate" → "Render
      and audit" → "Iteration."
    - **Name the thing:** every title contains a concrete noun (`skill`, `run`,
      `arc`, `audit`, `iteration`) — not abstract nouns (`shape`, `way`, `idea`).

    ## What the human verifier looks for

    Before typing `approve`:
    1. Five columns, in the canonical order — no extra columns.
    2. Status lines under each row showing tone/chain/named — at most one
       auto-fix; if more, the interview answers may have leaked pitch tone.
    3. The flowing paragraph reads as one story, not seven disconnected sentences.

    Before opening the generated HTML:
    1. Filename matches `YYYY-MM-DD-<slug>-handbook.html`.
    2. `open` succeeded; default browser tab is now showing the file.
    3. Absolute path printed on its own line as the LAST output (no banner,
       no celebration emoji, no next-steps suggestion).

    In the browser:
    1. Side-by-side with `~/work/caseproof/gh-pm/pm/pm-framework/diagrams/pm-system.html`:
       same palette (whites, grays, blue accent), same Inter font, same sidebar
       (220px black), same hero scale (56px h1 / 800 weight / -2.5px letter-spacing),
       same section spacing (70px top, 30px bottom).
    2. Components used in the body (cards, compare boxes, highlights, flow
       blocks, etc.) match those in `pm-system.html`.
    3. iOS Safari with system dark mode forced ON: file stays light.
    ```

    ### Stage the local skill install (preparation, not committed)

    Before the human-verify checkpoint can run, the local working copy of `skill/` must be visible at `~/.claude/skills/deshtml/`. The simplest path:

    ```bash
    mkdir -p "$HOME/.claude/skills"
    rm -rf "$HOME/.claude/skills/deshtml"
    cp -R "/Users/sperezasis/projects/code/deshtml/skill" "$HOME/.claude/skills/deshtml"
    ```

    This bypasses `bin/install.sh` (which requires a tagged release that does not yet exist — that's Phase 4 LAUNCH-03's job). Phase 4 LAUNCH-01 will validate the curl-pipe-bash flow against the live URL; Phase 2 only needs the payload installed locally so `/deshtml` resolves.

    Run this preparation as part of Task 1; document the commands you ran in `FIXTURE-NOTES.md` (Task 3 below) so the verifier can re-run them if the fixture is ever rerun.
  </action>
  <behavior>
    - Directory `.planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/` exists.
    - File `fixture/interview-answers.md` exists with all 5 question answers (Q2-Q6) plus the doc-type Q1 plus the approval phrase.
    - File `fixture/expected-arc.md` exists with the expected number of rows (6-8), the expected flowing paragraph, the three self-review checks the verifier should observe, and the visual-diff observations.
    - `~/.claude/skills/deshtml/SKILL.md` exists (the local install of the working-copy `skill/` payload).
    - `~/.claude/skills/deshtml/story-arc.md`, `~/.claude/skills/deshtml/interview/handbook.md`, `~/.claude/skills/deshtml/audit/run.sh`, `~/.claude/skills/deshtml/design/components.css` all exist (Wave 1 outputs were copied).
    - `~/.claude/skills/deshtml/audit/run.sh` is executable.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -d .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture &&
      test -s .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md &&
      test -s .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/expected-arc.md &&
      command grep -qF 'Who is the reader\|Audience\|installing deshtml for the first time' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md ||
      command grep -qF 'installing deshtml' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md &&
      command grep -qF 'handbook' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md &&
      command grep -qF 'approve' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md &&
      command grep -qF 'Caseproof Documentation System' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md &&
      command grep -qF 'Handbook, not pitch. Describe what IS.' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md &&
      command grep -qE '6-8 rows|6 to 8|six to eight' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/expected-arc.md &&
      command grep -qF 'flowing paragraph' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/expected-arc.md &&
      command grep -qF 'pm-system.html' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/expected-arc.md &&
      command grep -qF 'iOS Safari' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/expected-arc.md &&
      test -f "$HOME/.claude/skills/deshtml/SKILL.md" &&
      test -f "$HOME/.claude/skills/deshtml/story-arc.md" &&
      test -f "$HOME/.claude/skills/deshtml/interview/handbook.md" &&
      test -x "$HOME/.claude/skills/deshtml/audit/run.sh" &&
      test -f "$HOME/.claude/skills/deshtml/design/components.css" &&
      test -f "$HOME/.claude/skills/deshtml/design/palette.css" &&
      test -f "$HOME/.claude/skills/deshtml/design/typography.css" &&
      test -f "$HOME/.claude/skills/deshtml/design/components.html" &&
      test -f "$HOME/.claude/skills/deshtml/design/formats/handbook.html" &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/` exists.
    - `fixture/interview-answers.md` documents Q1 (doc type) plus the five interview question answers (audience, material, sections, tone, inclusions) plus the approval phrase.
    - `fixture/interview-answers.md` includes the verbatim CLAUDE.md tone phrase ("Handbook, not pitch. Describe what IS.") as the Q5 answer.
    - `fixture/expected-arc.md` specifies 6-8 expected rows, the expected flowing paragraph, the three self-review checks the verifier observes, and the visual-diff target (pm-system.html).
    - `fixture/expected-arc.md` mentions iOS Safari forced-dark-mode as a verification step.
    - `~/.claude/skills/deshtml/` is a working copy of the repo's `skill/` payload — SKILL.md, story-arc.md, interview/handbook.md, audit/run.sh (executable), audit/rules.md, design/{palette,typography,components}.css, design/components.html, and design/formats/handbook.html all present.
    - The repo's `skill/audit/run.sh` is executable (carry-over from plan 02-03; the install copies preserve the mode).
  </acceptance_criteria>
  <done>
    Fixture inputs exist and are committable; the local skill install at `~/.claude/skills/deshtml/` mirrors the working copy with the executable bit preserved on `audit/run.sh`. Eighteen automated checks pass. The human-verify checkpoint in Task 2 can now run.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Wave 1 of Phase 2 (plans 02-01, 02-02, 02-03) shipped the working `/deshtml` skill end-to-end for handbook:
    - `skill/design/components.css` — verbatim component CSS extracted from `pm-system.reference.html` (plan 02-01).
    - `skill/SKILL.md`, `skill/story-arc.md`, `skill/interview/handbook.md` — the skill flow, arc rubric, and interview (plan 02-02).
    - `skill/audit/run.sh`, `skill/audit/rules.md` — the post-generation audit and rules reference (plan 02-03).

    Task 1 of this plan staged a working copy at `~/.claude/skills/deshtml/` and committed reproducible fixture inputs.

    This checkpoint runs the canonical `deshtml-about-itself` fixture end-to-end through the running skill and verifies every Phase-2 contract holds: mode detection, doc-type branch, interview, arc gate, approval whitelist, render, audit, open, path-print, and visual fidelity against `pm-system.html`.
  </what-built>
  <how-to-verify>
    Run the fixture end-to-end. This takes about 10 minutes total.

    ### Step 1 — Pre-flight (1 min)

    Open a fresh Claude Code session in a terminal. Working directory: somewhere OUTSIDE the deshtml repo (recommended: `~/Desktop/` or `/tmp/deshtml-fixture/`). Confirm the skill is installed:

    ```bash
    ls ~/.claude/skills/deshtml/SKILL.md
    ```

    Should print the path. If not, re-run Task 1's `cp -R skill/ ~/.claude/skills/deshtml/` line.

    ### Step 2 — Source-mode stub fires (1 min)

    Type into Claude Code:

    ```
    /deshtml @nonexistent.md
    ```

    Expected: Claude responds with EXACTLY the source-mode stub message:

    > Source mode is coming in Phase 4. For now, run `/deshtml` with no arguments to use the interview.

    Then stops. **Pass:** that message appears and Claude does NOT proceed to ask the doc type. **Fail:** Claude asks the doc type or anything else.

    ### Step 3 — Doc-type stub fires for non-handbook (1 min)

    Fresh conversation. Type:

    ```
    /deshtml
    ```

    Claude asks doc type. Reply:

    > pitch

    Expected: Claude responds with EXACTLY:

    > pitch is coming in Phase 3. Try handbook for now.

    Then stops. **Pass:** that message appears, conversation ends. **Fail:** Claude proceeds to a pitch interview or falls back to handbook.

    ### Step 4 — Full handbook flow (5 min)

    Fresh conversation. Type:

    ```
    /deshtml
    ```

    Claude asks doc type. Reply: `handbook`. Then paste the answers from `.planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md` (Q2-Q6), one at a time.

    After Q6, Claude proposes the arc table. **Verify:**
    - Five columns, EXACTLY: `# | Beat | Section | One sentence | Reader feels`. No extra columns.
    - 6-8 rows.
    - A `## Read the One Sentence column top-to-bottom` heading is present below the table, followed by a flowing paragraph.
    - Status lines appear under each row (`✓ tone, ✓ chain, ✓ named.` or similar).
    - The flowing paragraph reads as ONE story, not bullet-points glued together.

    If anything is off, type a revision request (e.g., "make section 3 about installation, not setup") and verify the skill regenerates the arc and re-asks for approval. **Verify:** the revision loop fires and the arc is re-rendered.

    Then approve:

    > approve

    **Verify:**
    1. Filename matches `2026-04-28-deshtml-handbook.html` (or `-2` if a collision occurred).
    2. The default browser opens with the file rendered.
    3. The terminal's LAST line is the absolute path on its own line — no banner, no emoji, no celebration.

    ### Step 5 — Audit confirmation (1 min)

    Run the audit manually against the generated file:

    ```bash
    bash ~/.claude/skills/deshtml/audit/run.sh /path/to/2026-04-28-deshtml-handbook.html
    echo "audit exit: $?"
    ```

    **Pass:** exit 0. **Fail:** non-zero (read the violations on stderr; they shouldn't be there).

    Optionally run the audit with `--explain` to confirm the verbose mode works:

    ```bash
    bash ~/.claude/skills/deshtml/audit/run.sh --explain /path/to/2026-04-28-deshtml-handbook.html
    ```

    ### Step 6 — Visual diff (3 min)

    Open the generated handbook and `pm-system.html` side-by-side in Chrome:

    ```bash
    open -a "Google Chrome" /path/to/2026-04-28-deshtml-handbook.html
    open -a "Google Chrome" /Users/sperezasis/work/caseproof/gh-pm/pm/pm-framework/diagrams/pm-system.html
    ```

    Compare against `fixture/expected-arc.md`'s "What the human verifier looks for" checklist:
    - Same palette (white background, --black text, --blue accent, --g6 secondary).
    - Same Inter font, same h1 scale (56px / 800 / -2.5px), same h2 scale (36px / 800 / -1.5px).
    - Same sidebar (220px wide, black background, white nav).
    - Same section spacing (70px top, 30px bottom).
    - Components in the body match those in pm-system.html (cards, compare boxes, highlights — whatever the arc called for).

    Repeat in Safari:

    ```bash
    open -a Safari /path/to/2026-04-28-deshtml-handbook.html
    open -a Safari /Users/sperezasis/work/caseproof/gh-pm/pm/pm-framework/diagrams/pm-system.html
    ```

    ### Step 7 — iOS Safari forced-dark-mode test (1 min, optional but documented)

    AirDrop the file to an iPhone, OR use macOS Safari's Develop menu → User Agent → iOS to simulate. Force dark mode in iOS Settings → Accessibility → Display & Text Size → Smart Invert (or simulate via Develop menu).

    **Pass:** the file stays LIGHT — palette colors unchanged, no inversion. This validates DESIGN-07 carry-over.
    **Fail:** the file inverts to dark mode (would mean the `<meta name="color-scheme" content="light">` tag from `formats/handbook.html` was lost during inlining, OR the `color-scheme: light` declaration in `palette.css` did not make it into the inlined `<style>` block).

    ### Step 8 — Adversarial audit smoke test (1 min)

    Verify the audit's retry mechanism would fire if generation produced a violation. Make a copy of the generated file and inject a violation:

    ```bash
    cp /path/to/2026-04-28-deshtml-handbook.html /tmp/audit-test-bad.html
    # Inject a hex literal outside :root
    perl -i -pe 's|<body|<body style="color:#ff0000"|' /tmp/audit-test-bad.html
    bash ~/.claude/skills/deshtml/audit/run.sh /tmp/audit-test-bad.html
    echo "audit exit (should be non-zero): $?"
    ```

    **Pass:** exit 1, stderr lists the hex violation. **Fail:** exit 0 (the audit missed it).

    ### What to type when done

    Reply with one of:
    - `approved` — every step passed; the Phase 2 contract holds.
    - `approved with notes: <text>` — passed but with minor cosmetic deviations the human accepts (document them; will be folded into the SUMMARY).
    - `failed: <which step>` — describe what didn't work; orchestrator routes back to the relevant Wave-1 plan for revision.
  </how-to-verify>
  <resume-signal>Reply `approved`, `approved with notes: <text>`, or `failed: <step>: <description>`.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write FIXTURE-NOTES.md capturing the actual run (filename, audit exit, visual-diff observations, deviations)</name>
  <files>.planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md</files>
  <read_first>
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/interview-answers.md (the input set the run used)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/expected-arc.md (the shape the run was compared against)
    - The verifier's reply from the Task 2 checkpoint (the source of all "actual" data)
  </read_first>
  <action>
    Capture the empirical observations from the Task 2 fixture run in `FIXTURE-NOTES.md`. This is the artifact future regressions reference: when Phase 3 ships and a new doc type breaks something, the maintainer reads these notes to understand what "right" looked like in Phase 2.

    Use this exact template, filling in real values from the run:

    ```markdown
    # Fixture: deshtml-about-itself handbook — run notes

    Run date: <YYYY-MM-DD of the actual run>
    Verifier: Santiago (or whoever ran the checkpoint)
    Verifier reply: `approved` | `approved with notes` | `failed`

    ## What the skill produced

    - **Final filename:** `<actual-filename-from-pwd>` (collision suffix used: `<none>` | `-2` | `-3`)
    - **Working directory:** `<the absolute path of the CWD where the file was written>`
    - **File size:** `<bytes>`
    - **Audit exit code (round 1):** `<0 or non-zero>`
    - **Number of audit retries triggered:** `<0, 1, 2, or 3>`
    - **Final delivery:** `<clean (audit passed) | delivered with violations (round 3 fallback)>`

    ## Wave 0 mechanics observations

    Documenting what the run revealed about Claude Code skill behavior:

    - `$ARGUMENTS` empty-arg behavior: `<observed: empty string | <no arguments> placeholder | other>`. Plan 02-02 working assumption was empty string per 02-RESEARCH HIGH confidence; this is the empirical confirmation.
    - `${CLAUDE_SKILL_DIR}` substitution: `<resolved correctly to ~/.claude/skills/deshtml/ | did not resolve | other>`.
    - Sub-file Read discipline: `<Claude read sub-files only at the documented step | Claude read speculatively (Pitfall 15)>`.
    - Bash tool invocation of audit: `<worked first try | needed allowed-tools adjustment>`.

    ## Mode-detection smoke tests

    - `/deshtml @nonexistent.md` (Step 2 of checkpoint): `<stub fired | fell through to interview (FAIL)>`.
    - `/deshtml` then `pitch` (Step 3 of checkpoint): `<stub fired | fell through to handbook (FAIL)>`.

    ## Arc gate observations

    - Number of rows in proposed arc: `<actual count>`. Expected range: 6-8.
    - Self-review check failures (auto-fixed in place): `<count + summary, e.g., "2 — section 4 title rewritten from 'Generation has stages' to 'A run goes through six stages' (name-the-thing)">`.
    - Revision rounds before approval: `<count, 0 if approved on first display>`.
    - Flowing paragraph reads as one story: `<yes | no, with detail>`.

    ## Visual diff against pm-system.html

    Side-by-side observations in Chrome:
    - Palette match: `<exact | minor diff: ___ | wrong color: ___>`.
    - Typography match: `<exact | minor diff: ___ | wrong font: ___>`.
    - Sidebar shape: `<exact | minor diff: ___ | wrong shape: ___>`.
    - Hero scale: `<exact | minor diff: ___ | wrong scale: ___>`.
    - Section spacing: `<exact | minor diff: ___ | wrong spacing: ___>`.
    - Components used in body: `<list, e.g., ".s-lead, .stats stat, .hl hl-b, .cmp, .flow"; flag any that aren't in pm-system.html>`.

    Same observations in Safari: `<copy or note "same as Chrome">`.

    iOS Safari forced-dark-mode test: `<stayed light (PASS) | inverted to dark (FAIL — DESIGN-07 broken)>`.

    ## Adversarial audit smoke test

    - Injected hex literal `style="color:#ff0000"`: `<audit exit 1 (PASS) | exit 0 (FAIL)>`.
    - Stderr message present: `<yes (PASS) | no (FAIL)>`.

    ## Deviations and follow-ups

    Anything observed that doesn't break the contract but should be flagged for Phase 3 / Phase 4:

    - `<example: "Status lines below table rows wrap awkwardly at narrow terminal widths — consider extra-column format in V2.">`
    - `<example: "Audit harvest takes ~120ms on a 200-row handbook — well under the 500ms threshold but worth re-measuring as Phase 3 expands components.html.">`

    ## Phase 3 / Phase 4 hand-off notes

    What the next phase needs to know:

    1. The skill flow (mode detection → doc-type branch → interview → arc → render → audit → open) is proven on handbook. Phase 3 only flips the four type-stubs in SKILL.md Step 2 to real interview hand-offs and ships their `interview/<type>.md` files.
    2. The audit's class allowlist auto-grows when `components.html` is updated. Phase 3's presentation type may add scroll-snap classes; add them to `components.html` first, audit catches up automatically.
    3. The visual diff target was `pm-system.html`. Phase 3's overview/presentation/pitch/meeting-prep types will compare against `bnp-overview.html` and the future presentation reference (no canonical reference exists yet for presentation; Phase 3's 30-min spike per ROADMAP creates one).
    4. Phase 4 LAUNCH-01 still needs to verify the curl-pipe-bash installer against the live URL on a fresh machine — this fixture used a local `cp -R` install, NOT the public installer.
    ```

    Fill in every `<placeholder>` with real data from the verifier's reply at Task 2. Do NOT leave placeholders. If the verifier's reply was concise, ask one follow-up question to gather the missing data points (filename, audit exit, visual-diff specifics).
  </action>
  <behavior>
    - File `.planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md` exists.
    - File contains every section: What the skill produced, Wave 0 mechanics, Mode-detection smoke tests, Arc gate observations, Visual diff, Adversarial audit smoke test, Deviations and follow-ups, Phase 3/4 hand-off.
    - File contains an actual filename (matches `^2026-[0-9]{2}-[0-9]{2}-.+-handbook.html$` or with `-2`/`-3` suffix).
    - File documents the audit exit code from round 1.
    - File documents the verifier's reply (approved / approved with notes / failed).
    - File documents the empirical `$ARGUMENTS` observation (Wave 0 confirmation).
    - File contains zero `<placeholder>` strings — every angle-bracketed slot has been replaced with real data.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'What the skill produced' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'Wave 0 mechanics' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'Mode-detection smoke tests' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'Arc gate observations' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'Visual diff' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'Adversarial audit smoke test' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'Deviations and follow-ups' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'Phase 3' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qF 'Phase 4' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qE '202[0-9]-[0-9]{2}-[0-9]{2}-.+-handbook\.html' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qE 'Audit exit code|audit exit' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qE 'approved|approved with notes|failed' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      command grep -qE 'ARGUMENTS' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      ! command grep -qE '<actual-filename-from-pwd>|<the absolute path of the CWD>|<bytes>|<0 or non-zero>|<observed:|<resolved correctly|<count' .planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-story-arc-gate-handbook-end-to-end/fixture/FIXTURE-NOTES.md` exists.
    - File contains all eight sections: What the skill produced, Wave 0 mechanics, Mode-detection smoke tests, Arc gate observations, Visual diff, Adversarial audit smoke test, Deviations, Phase 3/4 hand-off.
    - File documents the actual generated filename (regex: `202[0-9]-[0-9]{2}-[0-9]{2}-.+-handbook.html`).
    - File documents the audit's round-1 exit code (0 or non-zero, with retry count).
    - File documents the verifier's reply (approved / approved with notes / failed).
    - File documents the empirical observation about `$ARGUMENTS` empty-arg behavior (closes the working assumption from plan 02-02).
    - File contains the visual-diff observations (palette, typography, sidebar, hero, spacing, components).
    - File documents the iOS Safari forced-dark-mode result.
    - File documents the adversarial audit smoke-test result.
    - File contains ZERO unfilled `<placeholder>` slots.
  </acceptance_criteria>
  <done>
    `FIXTURE-NOTES.md` exists with all eight sections filled in with real run data — filename, audit exits, retry count, verifier reply, mode-detection results, arc-gate observations, visual-diff observations, dark-mode test, adversarial smoke test, and Phase 3/4 hand-off notes. Zero placeholders remain.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working-copy `skill/` → local install at `~/.claude/skills/deshtml/` | Bypasses `bin/install.sh` for fixture purposes. Phase 4 LAUNCH-01 re-tests with the public installer; this plan does not. |
| Running skill → user's CWD | The fixture writes a single .html file to the verifier's chosen working directory. Outside the deshtml repo by recommendation; documented in fixture/interview-answers.md. |
| Fixture artifacts → git | Reproducible inputs (interview answers, expected arc, run notes) ARE committed. Generated HTML is NOT — it's a single-run artifact in the verifier's CWD. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-22 | Tampering | The local install at `~/.claude/skills/deshtml/` drifts from the working copy between Wave 1 completion and the fixture run | mitigate | Task 1 explicitly does `rm -rf ~/.claude/skills/deshtml && cp -R skill/ ~/.claude/skills/deshtml` so the install is a clean snapshot of the current working copy. The verifier confirms the install timestamp at Step 1 of the checkpoint. |
| T-02-23 | Information Disclosure | Generated handbook contains content that reveals user's system path or environment in the printed absolute path | accept | Step 8 of SKILL.md prints the absolute path on its own line — that's the intended behavior (user needs the path). The path naturally includes `$HOME`. The generated HTML itself contains only the user's interview answers, which they explicitly typed. No silent env exposure. |
| T-02-24 | Spoofing | Verifier types `approve` to skip a flawed arc to "save time" — the gate becomes ceremonial | mitigate | The plan documents (in expected-arc.md) what to look for BEFORE typing approve. Pitfall 10 (rubber-stamping) acceptance: the verifier can choose to approve a flawed arc, but the FIXTURE-NOTES.md template forces them to record what they accepted, so future regressions can be traced to "Phase 2 verifier accepted X" rather than "skill silently failed." |
| T-02-25 | Denial of Service | Visual diff in iOS Safari requires a physical iPhone the verifier may not have | accept | The macOS Safari Develop → User Agent → iOS simulator is documented as the fallback. PROJECT.md "Out of Scope" already accepts macOS-first; iOS is a sanity check, not a blocking requirement. |
| T-02-26 | Tampering | The fixture run is "successful" but the generated HTML is committed to the deshtml repo by accident | mitigate | fixture/interview-answers.md explicitly recommends a CWD outside the deshtml repo. `.gitignore` could be extended to ignore `*.html` at repo root in Phase 4, but for V1 the recommendation is enough. |
| T-02-27 | Repudiation | Future maintainer reads "approved" in FIXTURE-NOTES.md but cannot reproduce the visual fidelity | mitigate | Reproducible inputs (interview-answers.md) are committed; the local-install command (`cp -R`) is documented; the `pm-system.html` source path is committed in expected-arc.md. Future maintainers can re-run the same inputs against a re-cloned repo and re-extract `~/.claude/skills/deshtml/`. |
</threat_model>

<verification>
All three task `<verify>` blocks must pass. Specifically:
- Task 1: fixture inputs exist, local install at `~/.claude/skills/deshtml/` mirrors the working copy with executable bit preserved, all expected files present.
- Task 2 (human-verify checkpoint): the verifier replies `approved` (or `approved with notes: <text>`). A `failed` reply routes back to the relevant Wave-1 plan for revision.
- Task 3: FIXTURE-NOTES.md exists with all eight sections filled in with real data, zero placeholders remaining.

The Phase 2 ROADMAP success criteria (1-6) are verified holistically by Task 2's checkpoint:
- Criterion 1 (interview launch, doc-type first, ≤5 questions): Steps 4 of checkpoint.
- Criterion 2 (arc table + flowing paragraph + approval gate): Step 4.
- Criterion 3 (revision loop): Step 4 (verifier triggers a revision before final approval).
- Criterion 4 (filename pattern + open + path-print + file:// works): Steps 4 + 6.
- Criterion 5 (audit rejects violations confirmed by side-by-side compare): Steps 5 + 6 + 8.
- Criterion 6 (SKILL.md ≤200 lines, lazy-loads sub-files): Static check enforced by plan 02-02; observed at runtime as "Claude read sub-files at the documented step" in FIXTURE-NOTES.md Wave 0 section.
</verification>

<success_criteria>
1. **End-to-end skill flow validated.** The canonical fixture runs from `/deshtml` to printed-absolute-path with no manual fixes mid-flow. Mode detection works, doc-type branch works, interview works, arc gate works, render works, audit works, open works, path-print works.
2. **Visual fidelity proven against pm-system.html.** The generated handbook is, at structural-shell level, indistinguishable from the Caseproof reference in Chrome AND Safari. Acceptable diff: different content. Unacceptable: any wrong color, font, spacing, or component.
3. **Mode-detection contracts hold.** `/deshtml @file.md` returns the source-mode stub; `/deshtml` then `pitch` returns the Phase-3 stub. Neither silently falls back. SKILL-03 is empirically verified.
4. **Audit fires on adversarial input.** A copy of the generated file with an injected hex literal triggers exit 1 with the correct stderr message. The retry mechanism described in plan 02-03 is proven mechanically functional.
5. **iOS Safari forced-dark-mode is preserved.** DESIGN-07 carries through inlining; the generated handbook stays light.
6. **Working assumptions discharged.** `$ARGUMENTS` empty-arg behavior is empirically observed (plan 02-02's deferred A1). The status-line format reads cleanly at 80 columns (plan 02-02 A2). The `cp -R` install pattern works (preempts any Phase 4 install-flow surprises).
7. **Reproducible.** Future regressions can replay the fixture from `interview-answers.md`, compare arcs against `expected-arc.md`, and read `FIXTURE-NOTES.md` to understand what "Phase 2 right" looked like.
</success_criteria>

<output>
After completion, create `.planning/phases/02-story-arc-gate-handbook-end-to-end/02-04-SUMMARY.md` documenting:
- The verifier's final reply (approved / approved with notes / failed) and the timestamp.
- Reference to `fixture/FIXTURE-NOTES.md` (the empirical record — do not duplicate; this SUMMARY is a brief).
- Phase 2 ROADMAP success criteria 1-6 mapped to checkpoint steps that verified each.
- The empirical Wave 0 observations in one paragraph (`$ARGUMENTS` behavior, sub-file read discipline, status-line readability).
- Any deviations the verifier accepted with notes, with rationale.
- Any violations the verifier flagged for re-planning (route to which Wave-1 plan).
- What Phase 3 inherits: SKILL.md flow proven; audit auto-grows with `components.html`; visual-diff target pattern (`<reference>.html` side-by-side); the `cp -R` install pattern as a Phase 3 testing shortcut (Phase 4 still does the real curl-pipe-bash test).
- What Phase 4 must verify against the LIVE URL that this plan did NOT verify locally: the curl-pipe-bash installer flow, the GitHub release tag resolution, the cross-machine reproducibility of the fixture run.
</output>
</content>
</invoke>