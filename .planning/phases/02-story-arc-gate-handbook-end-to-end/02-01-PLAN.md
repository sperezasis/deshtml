---
phase: 02-story-arc-gate-handbook-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skill/design/components.css
  - skill/design/SYSTEM.md
autonomous: true
requirements:
  - DESIGN-06
user_setup: []

must_haves:
  truths:
    - "skill/design/components.css exists and contains the verbatim component CSS rules extracted from skill/design/references/pm-system.reference.html (lines 7-697 — the single <style> block)."
    - "Every selector in components.css matches a class enumerated in skill/design/components.html OR is a structural selector required by the Handbook layout (.sidebar, .sb-*, .main, .hero, .system-bar, etc.) — no hex literals appear outside the :root block (none should appear at all; :root lives in palette.css)."
    - "skill/design/SYSTEM.md is updated so the Tokens table includes components.css with a 'When SKILL.md loads it' column that says: 'At HTML-assembly time, after the arc is approved. Inlined into the output's <style> block alongside palette.css and typography.css.'"
    - "components.css is byte-for-byte derivable from pm-system.reference.html — no paraphrase, no condensation, no editorial reformatting (D-14 verbatim discipline applies recursively per Phase 2 CONTEXT.md §'Established Patterns')."
    - "components.css contains zero @import statements (Inter is imported by typography.css; layered imports break the inlining contract) and zero color hex literals (every color is var(--token-name) — palette.css owns the tokens)."
  artifacts:
    - path: "skill/design/components.css"
      provides: "All component, sidebar, hero, sticky-bar, layout, and section CSS rules from pm-system.reference.html, ready to be inlined into generated handbook output."
      contains: ".sidebar"
    - path: "skill/design/SYSTEM.md"
      provides: "Updated index that points Phase 2's SKILL.md at components.css and documents the three-file inlining contract (palette.css + typography.css + components.css)."
      contains: "components.css"
  key_links:
    - from: "skill/design/components.css"
      to: "skill/design/references/pm-system.reference.html"
      via: "verbatim extraction of the <style> block on lines 7-697"
      pattern: ".sidebar"
    - from: "skill/design/SYSTEM.md"
      to: "skill/design/components.css"
      via: "Tokens table row + Rules section reference"
      pattern: "components.css"
---

<objective>
Extract the component CSS Phase 1 left implicit. `pm-system.reference.html` carries every visual rule that makes a Caseproof handbook look like a Caseproof handbook — sidebar, hero, sticky bar, section grid, and 75+ component classes (`.tag`, `.ic`, `.tip`, `.hl`, `.cmp`, `.flow`, `.donut`, etc.). Phase 1 shipped the markup allowlist (`components.html`) and the tokens (`palette.css`, `typography.css`) but did NOT extract the component CSS into its own file. Without this plan, every generated handbook has correct colors and correct typography but every component class is unstyled — the visual diff against `pm-system.html` (D2-23) is impossible to pass.

Closes the Phase 2 CSS-inlining gap flagged as RESEARCH §"Open Question OQ-1" (Option a — strong recommendation). Closes DESIGN-06's prerequisite (the audit's class allowlist needs a CSS source for every approved class, not just markup).

Purpose: this is mechanical extraction with verbatim discipline (D-14 from Phase 1, applies recursively per CONTEXT.md §"Established Patterns"). The reference HTML is the source of truth. No paraphrase, no condensation, no editorial reorganization. If a future component is added to the design system, the source is updated first, then re-extracted — never edited in place here.

Output: `skill/design/components.css` (one file, ~600 lines of verbatim CSS) and an updated `skill/design/SYSTEM.md` that documents the three-file inlining contract (palette + typography + components). After this plan, plan 02-02's output writer can construct a self-contained handbook by inlining three CSS files into one `<style>` block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT-AMENDMENT.md
@.planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md
@.planning/phases/01-foundation-installer-design-assets/01-02-SUMMARY.md
@skill/design/SYSTEM.md
@skill/design/palette.css
@skill/design/typography.css
@skill/design/components.html
@skill/design/references/pm-system.reference.html
@/Users/sperezasis/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract components.css verbatim from pm-system.reference.html</name>
  <files>skill/design/components.css</files>
  <read_first>
    - skill/design/references/pm-system.reference.html (the source — the full `<style>` block lives at lines 7-697; this is what gets extracted)
    - skill/design/palette.css (you must NOT redefine `:root` — palette.css owns it)
    - skill/design/typography.css (you must NOT re-import Inter or redefine body/h1/h2/h3 — typography.css owns those)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-RESEARCH.md §"Open Questions → OQ-1" (rationale for this entire plan)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT-AMENDMENT.md (this plan is the prerequisite that makes D2-15 feasible)
    - .planning/phases/01-foundation-installer-design-assets/01-CONTEXT.md §"D-14 verbatim discipline" — applies recursively
  </read_first>
  <action>
    Create `skill/design/components.css` by extracting the entire `<style>` block from `skill/design/references/pm-system.reference.html`, with three (and only three) deletions for de-duplication. Everything else is byte-for-byte verbatim, in source order.

    ### Step 1 — Read the source block

    Read `skill/design/references/pm-system.reference.html` lines 7-697 (the single `<style>...</style>` block). This is the extraction source. Do NOT attempt to summarize, reorder, rename, or merge selectors. Do NOT add new comments beyond the file header.

    ### Step 2 — Create `skill/design/components.css` with this exact header

    The first ~10 lines of the file are a header comment block. Use this content exactly:

    ```css
    /* Caseproof Documentation System — Component CSS
     * Verbatim extraction from skill/design/references/pm-system.reference.html (lines 7-697).
     * This file ships every visual rule that makes a handbook look Caseproof: sidebar,
     * hero, sticky bar, section grid, and the 16 component families enumerated in
     * skill/design/components.html (.tag, .ic, .tip, .hl, .cmp, .flow, .donut, etc.).
     *
     * Inlined into generated handbook output by Phase 2's SKILL.md alongside
     * palette.css (tokens) and typography.css (Inter @import + scale).
     *
     * Do not edit values here. Update the source first (pm-system.reference.html
     * via the Caseproof Documentation System), then re-extract.
     */
    ```

    ### Step 3 — Paste the verbatim CSS, with these THREE de-duplication deletions

    Paste the contents of the `<style>` block from `pm-system.reference.html` into `components.css` AFTER the header. THREE rules from the source MUST be deleted because palette.css and typography.css already own them:

    1. **DELETE the `@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap');` line** (line 8 of pm-system.reference.html). Reason: typography.css already contains this `@import`. Two `@import` statements for the same font is harmless but violates the single-source-of-truth pattern (CONTEXT.md §"Established Patterns" rule 2). The audit's hex-literal-outside-`:root` regex doesn't trip on `@import`, but the planner's discipline does.

    2. **DELETE the entire `:root { ... }` block** (lines 13-33 of pm-system.reference.html — `--black`, `--g9`, ... `--teal-l`). Reason: palette.css owns `:root`. Re-declaring the tokens here would duplicate them at inline time and create two sources of truth for the palette. The audit's hex-literal regex strips one `:root` block per audit run; a second `:root` would put hex literals OUTSIDE the stripped range and fail the audit on a clean handbook.

    3. **DELETE the `body { ... }` block** (lines 35-40 of pm-system.reference.html — `font-family: 'Inter', -apple-system, sans-serif; background: var(--white); color: var(--black); -webkit-font-smoothing: antialiased;`). Reason: typography.css already defines `body` (font-family, font-size, font-weight, color, line-height, antialiasing). The reference's `body { background: var(--white); color: var(--black) }` adds two declarations the format skeleton (`skill/design/formats/handbook.html` line 15) ALSO sets. Three sources for the same rule is the OPPOSITE of single-source-of-truth.

    Everything else in the `<style>` block (lines 7, 9-12, 41-697 — the universal reset `* { margin: 0; padding: 0; box-sizing: border-box; }`, the `html { scroll-behavior: smooth; }`, the `.sidebar` block and all sub-selectors, all section comment dividers `/* ══...══ */`, all 16 component families) is copied **byte-for-byte verbatim**, including:
    - Whitespace, indentation, and blank lines exactly as in the source.
    - All `/* ══════════════════════════════════════ */` comment dividers and their `/*  HEADER  */` labels.
    - All comments inside CSS rules (e.g., `/* Primary text */`, `/* DAD pattern */`).
    - All vendor prefixes (`-webkit-font-smoothing`, etc.).
    - All redundant declarations (some rules in the source repeat properties — DO NOT optimize them; the source shape is the contract).

    ### Step 4 — Verify zero hex literals leaked

    The reference file's component CSS uses `var(--token-name)` everywhere except inside `:root`. After deleting `:root` per Step 3, NO hex literal should remain. If `command grep -nE '#[0-9a-fA-F]{3,8}' skill/design/components.css` returns anything, you missed a deletion. Re-check.

    Edge case the audit will catch later: the reference HTML has the `.sb-div { background: #3a3a3c; }` rule (line 58 of pm-system.reference.html). This is a hex literal NOT in the palette. **Convert it to a token** by replacing `#3a3a3c` with `var(--g9)` — the closest palette neighbor. Document this single deviation in the file header (append a "Deviations" line) AND in `01-02-SUMMARY.md`'s deviation log style. This is the ONLY content edit. Everything else is verbatim.

    Append this line to the header comment after the existing block:

    ```css
    /*
     * Deviations from source (one):
     *   - .sb-div background: #3a3a3c → var(--g9). The reference file has one
     *     stray hex literal outside :root (sb-div divider color). Closest palette
     *     neighbor is --g9 (#525256); the visual difference is < 2 luminance points
     *     on a dark background and is invisible at the divider's 1px height.
     *     Source: pm-system.reference.html line 58.
     */
    ```

    ### Step 5 — File length sanity check

    The reference file's `<style>` block is 697 - 7 = 690 lines. After deleting the three rules (≈26 lines combined: 1 import + 21-line `:root` + 6-line body), `components.css` should be approximately 690 - 26 + 12 (header) ≈ 676 lines, ±10. If your file is dramatically shorter (< 600 lines), you missed content. If dramatically longer (> 720 lines), you duplicated something. Verify with `wc -l skill/design/components.css`.
  </action>
  <behavior>
    - `wc -l skill/design/components.css` returns a value between 600 and 720 (verbatim block minus three deletions plus header).
    - `command grep -c '@import' skill/design/components.css` returns 0 (typography.css owns the @import).
    - `command grep -c '^:root' skill/design/components.css` returns 0 (palette.css owns :root).
    - `command grep -nE '^body[[:space:]]*\{' skill/design/components.css` returns nothing (typography.css owns body).
    - `command grep -nE '#[0-9a-fA-F]{3,8}' skill/design/components.css` returns NOTHING (zero hex literals — every color is `var(--token)`).
    - `command grep -c '\.sidebar' skill/design/components.css` returns >= 1 (sidebar selectors present).
    - `command grep -c '\.tag' skill/design/components.css` returns >= 1 (tag component present — proves component CSS extracted).
    - `command grep -c '\.cmp' skill/design/components.css` returns >= 1 (compare component present).
    - `command grep -c '\.hl' skill/design/components.css` returns >= 1 (highlight component present).
    - `command grep -c '\.flow' skill/design/components.css` returns >= 1 (flow component present).
    - First non-blank, non-comment line of `components.css` is `* { margin: 0; padding: 0; box-sizing: border-box; }` (the universal reset that survives the three deletions and is the first real rule in the source).
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s skill/design/components.css &&
      LINES=$(wc -l < skill/design/components.css) && [ "$LINES" -ge 600 ] && [ "$LINES" -le 720 ] &&
      [ "$(command grep -c '@import' skill/design/components.css)" = "0" ] &&
      [ "$(command grep -c '^:root' skill/design/components.css)" = "0" ] &&
      ! command grep -nE '^body[[:space:]]*\{' skill/design/components.css &&
      ! command grep -nE '#[0-9a-fA-F]{3,8}' skill/design/components.css &&
      command grep -q '\.sidebar' skill/design/components.css &&
      command grep -q '\.tag' skill/design/components.css &&
      command grep -q '\.cmp' skill/design/components.css &&
      command grep -q '\.hl' skill/design/components.css &&
      command grep -q '\.flow' skill/design/components.css &&
      command grep -q 'var(--g9)' skill/design/components.css &&
      command grep -q '/\* Caseproof Documentation System — Component CSS' skill/design/components.css &&
      command grep -q 'Verbatim extraction from skill/design/references/pm-system.reference.html' skill/design/components.css &&
      command grep -q 'Deviations from source' skill/design/components.css &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `skill/design/components.css` exists and is non-empty.
    - File length is between 600 and 720 lines (verbatim source minus three deletions plus header).
    - File header contains: `Caseproof Documentation System — Component CSS`, `Verbatim extraction from skill/design/references/pm-system.reference.html`, AND `Deviations from source` (the documented `--g9` substitution).
    - File contains ZERO `@import` statements.
    - File contains ZERO `:root` blocks (palette.css owns `:root`).
    - File contains ZERO `body { ... }` rule blocks (typography.css owns `body`).
    - File contains ZERO hex literals (`#XXXXXX`) — every color is via `var(--token-name)`.
    - File contains the universal reset rule `* { margin: 0; padding: 0; box-sizing: border-box; }`.
    - File contains the sidebar block (`.sidebar { position: fixed; ... }`).
    - File contains every major component family from `components.html`: `.tag`, `.cmp`, `.hl`, `.flow` at minimum (full coverage validated in plan 02-04 visual gate).
    - File replaces the source's `#3a3a3c` (sb-div divider) with `var(--g9)` and documents this single deviation in the header.
  </acceptance_criteria>
  <done>
    `skill/design/components.css` exists with the full verbatim component CSS from `pm-system.reference.html` (lines 7-697 of the `<style>` block) MINUS the three de-duplication deletions (`@import`, `:root`, `body`), PLUS the documented `#3a3a3c → var(--g9)` substitution, PLUS the file header. The seven enumerated automated checks pass. Visual confirmation that components.css renders the same as the reference is deferred to plan 02-04 (visual gate).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update skill/design/SYSTEM.md to document components.css and the three-file inlining contract</name>
  <files>skill/design/SYSTEM.md</files>
  <read_first>
    - skill/design/SYSTEM.md (the current Phase-1 version — Tokens, Components, Format Skeletons, References, Rules sections; see the table-row format it already uses)
    - skill/design/components.css (just created in Task 1 — referenced from the new row)
    - .planning/phases/02-story-arc-gate-handbook-end-to-end/02-CONTEXT-AMENDMENT.md (D2-15 amendment that drives this update)
  </read_first>
  <action>
    Edit `skill/design/SYSTEM.md` in place. Two changes; everything else stays exactly as Phase 1 shipped it.

    ### Change 1 — Add a `components.css` row to the "Tokens" table

    Currently the Tokens table has two rows (`palette.css`, `typography.css`). Add a third row for `components.css`. The row MUST follow the same format and tone as the existing rows. Use this exact row markdown:

    ```markdown
    | `components.css` | All component, sidebar, hero, sticky-bar, layout, and section CSS rules from `pm-system.reference.html`. Every selector resolves colors via `var(--token-name)` from `palette.css`. | At HTML-assembly time, after the arc is approved. Inlined into the output's `<style>` block alongside `palette.css` and `typography.css`. |
    ```

    Place it as the **third row** of the Tokens table (after `typography.css`).

    ### Change 2 — Replace Rule 6 to reflect the three-file inlining contract

    The current Rule 6 in SYSTEM.md reads (lines 45 in the current file): "Inline palette + typography at paste time. The format skeletons … currently load CSS via `<link rel="stylesheet" href="../palette.css">` and `<link rel="stylesheet" href="../typography.css">`. The end-product must be a single self-contained HTML file …".

    Replace Rule 6 with this exact content:

    ```markdown
    6. **Inline palette + typography + components at paste time.** The format skeletons (`formats/handbook.html`, `formats/overview.html`) currently load CSS via `<link rel="stylesheet" href="../palette.css">` and `<link rel="stylesheet" href="../typography.css">`. The end-product must be a single self-contained HTML file (PROJECT.md "Constraints" — no asset folders). Phase 2 MUST replace those two `<link>` tags with a single `<style>...</style>` block containing the verbatim contents of `palette.css`, then `typography.css`, then `components.css` — in that order. Phase 2's audit pass MUST grep the generated output for zero `<link rel="stylesheet"` occurrences (acceptance check) AND verify the three-file CSS payload is present (palette `:root` block, typography `@import url('https://fonts.googleapis.com/css2?family=Inter`, and at least one component selector like `.sidebar` or `.tag`).
    ```

    The order — palette → typography → components — is load-bearing:
    - `palette.css` defines `:root` tokens FIRST so typography and components can resolve `var(--token)`.
    - `typography.css` SECOND so it owns body/h1/h2/h3 before any component overrides.
    - `components.css` LAST so component-specific rules cascade correctly (the source order in `pm-system.reference.html` is the design intent).

    ### What does NOT change

    - The "Components" table (with `components.html`).
    - The "Format Skeletons" table.
    - The "References" table.
    - Rules 1, 2, 3, 4, 5 in the "Rules Phase 2 must respect" section.
    - The opening paragraph about verbatim discipline.

    Do not add new sections. Do not reorder existing ones. The file is an index, not a redesign target.
  </action>
  <behavior>
    - `skill/design/SYSTEM.md` Tokens table contains 3 data rows (was 2): `palette.css`, `typography.css`, `components.css`.
    - The `components.css` row references "pm-system.reference.html" as the source.
    - Rule 6 mentions all three CSS files: palette, typography, components.
    - Rule 6 specifies the load order: palette FIRST, typography SECOND, components LAST.
    - The file still contains the original Rules 1-5 and 7 (if any) and the References table unchanged.
    - The opening paragraph about verbatim discipline is unchanged.
  </behavior>
  <verify>
    <automated>cd /Users/sperezasis/projects/code/deshtml && set -e &&
      test -s skill/design/SYSTEM.md &&
      command grep -q '`components.css`' skill/design/SYSTEM.md &&
      command grep -q 'pm-system.reference.html' skill/design/SYSTEM.md &&
      command grep -q 'Inline palette + typography + components' skill/design/SYSTEM.md &&
      command grep -q 'palette.css.*typography.css.*components.css\|palette\.css.*typography\.css.*components\.css' skill/design/SYSTEM.md &&
      command grep -q 'palette.css' skill/design/SYSTEM.md &&
      command grep -q 'typography.css' skill/design/SYSTEM.md &&
      command grep -q 'components.html' skill/design/SYSTEM.md &&
      command grep -q 'formats/handbook.html' skill/design/SYSTEM.md &&
      command grep -q 'pm-system.reference.html' skill/design/SYSTEM.md &&
      command grep -q 'bnp-overview.reference.html' skill/design/SYSTEM.md &&
      command grep -q 'Verbatim discipline' skill/design/SYSTEM.md &&
      [ "$(command grep -c '^| \`' skill/design/SYSTEM.md)" -ge "5" ] &&
      echo "ALL CHECKS PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `skill/design/SYSTEM.md` Tokens table includes a row for `components.css` (third row, after `typography.css`).
    - The `components.css` row's "When SKILL.md loads it" cell mentions inlining alongside `palette.css` and `typography.css`.
    - Rule 6 in the "Rules Phase 2 must respect" section is updated to mention all three CSS files (palette + typography + components).
    - Rule 6 specifies the load order: palette FIRST, typography SECOND, components LAST.
    - All Phase-1 rows in the Tokens, Components, Format Skeletons, and References tables remain unchanged (palette.css, typography.css, components.html, formats/handbook.html, formats/overview.html, pm-system.reference.html, bnp-overview.reference.html).
    - The opening paragraph (`Phase 2's SKILL.md reads from this directory…`) is unchanged.
    - No new top-level sections are added; the file is still an index, not a tutorial.
  </acceptance_criteria>
  <done>
    `skill/design/SYSTEM.md` has been updated with one new Tokens-table row (`components.css`) and a rewritten Rule 6 that mandates the three-file inlining contract in the correct order (palette → typography → components). All Phase-1 content is preserved. The thirteen enumerated automated checks pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caseproof reference HTML → components.css | The source `pm-system.reference.html` is a verbatim Phase-1 copy of an internal Caseproof file — already trusted. The boundary is between "design system source of truth" and "deshtml's pasted-into-output payload." |
| components.css → generated handbook | Every byte of components.css will be inlined into every handbook deshtml ships. A bad rule here propagates to every output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | A future PR edits `components.css` instead of `pm-system.reference.html` (drifts from the design system source) | mitigate | The file header explicitly states "Do not edit values here. Update the source first, then re-extract." Verbatim discipline (D-14) applies. The Phase 2 audit's class allowlist harvest reads `components.html` (markup source) so silent CSS drift can be detected via plan 02-04's visual gate. |
| T-02-02 | Tampering | A future extraction misses the `#3a3a3c → var(--g9)` deviation and silently re-introduces a hex literal outside :root | mitigate | The Phase 2 audit (plan 02-03) regexes `#[0-9a-fA-F]{3,8}` outside `:root` and fails any output. Catches this category at audit time even if components.css is updated badly. |
| T-02-03 | Information Disclosure | components.css inadvertently leaks PII or internal Caseproof references via comments | accept | Verbatim source is `pm-system.reference.html`, already a public-pattern reference Caseproof has approved as an external example. No PII expected; planner reviews the extracted file before commit. |
| T-02-04 | Denial of Service | components.css grows past ~150KB and bloats every handbook output (PITFALLS Pitfall 17) | accept | Source `<style>` block is ~30KB. Even after every Phase-3 component family is added, output stays well under 100KB. PITFALLS Pitfall 17's "warning sign" of 150KB+ is for a worst-case future, not this plan. |
| T-02-05 | Elevation of Privilege | n/a (no executable code in this plan) | n/a | components.css is pure CSS. SYSTEM.md is markdown. No script invocation, no shell, no install plumbing. |
</threat_model>

<verification>
Both task `<verify>` blocks must pass. Specifically:
- `skill/design/components.css` exists, is 600-720 lines, and contains zero `@import`, zero `:root`, zero `body` rules, zero hex literals.
- `components.css` contains every major component family (`.sidebar`, `.tag`, `.cmp`, `.hl`, `.flow`) and the universal reset.
- `components.css` documents the single deviation (`#3a3a3c → var(--g9)`) in the file header.
- `skill/design/SYSTEM.md` includes a `components.css` row in the Tokens table.
- `SYSTEM.md` Rule 6 mentions palette, typography, AND components, in that load order.
- All Phase-1 rows in `SYSTEM.md` are preserved.

Visual confirmation that `components.css` renders identically to `pm-system.reference.html`'s `<style>` block is intentionally deferred to plan 02-04 (visual gate). At that point, a fixture handbook is generated end-to-end and side-by-side compared with `pm-system.html` — that is when this plan's contract is fully validated. For 02-01, the mechanical checks above are sufficient.
</verification>

<success_criteria>
1. **Component CSS now has a single source of truth file.** `skill/design/components.css` exists with the verbatim component CSS rules from `pm-system.reference.html`. The Phase 2 SKILL.md (built in plan 02-02) can inline three CSS files into a single `<style>` block and produce a self-contained handbook that renders identically to the reference.
2. **Three-file inlining contract documented.** `skill/design/SYSTEM.md` Rule 6 specifies palette → typography → components as the load order; plan 02-02's output writer follows this order exactly.
3. **Verbatim discipline preserved.** The single deviation (`#3a3a3c → var(--g9)`) is documented in both the file header and (later) the plan's SUMMARY. Future re-extractions follow the same pattern.
4. **Phase 2's audit becomes mechanically possible.** Plan 02-03's audit script harvests classes from `components.html` and `typography.css` (per 02-RESEARCH §"Class allowlist harvest"). The CSS source for those classes is now `components.css` — a closed file the audit can also reference if Phase 3 ever needs CSS-level checks.
5. **Visual diff target unblocked.** Plan 02-04's side-by-side compare with `pm-system.html` becomes feasible: every visual rule that makes a Caseproof handbook look Caseproof is now in `skill/design/`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-story-arc-gate-handbook-end-to-end/02-01-SUMMARY.md` documenting:
- The exact line range extracted from `pm-system.reference.html` (7-697).
- The three de-duplication deletions made (`@import` line 8, `:root` lines 13-33, `body` lines 35-40) and why each is owned elsewhere.
- The single content deviation (`#3a3a3c → var(--g9)` for `.sb-div`) and its visual rationale.
- The three-file load order locked in `SYSTEM.md` Rule 6 (palette → typography → components).
- Final line count of `components.css` and key components verified present (`.sidebar`, `.tag`, `.cmp`, `.hl`, `.flow`).
- What plan 02-02's output writer must do with this file (inline as the third `<style>` block source, in order, after palette and typography).
</output>
</content>
</invoke>